Reading additional input from stdin...
OpenAI Codex v0.141.0
--------
workdir: /mnt/x/project/sub2api
model: gpt-5.5
provider: fkcodex
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: detailed
session id: 019ee868-3289-7430-afb0-f914862fd7ee
--------
user
Active task: .trellis/tasks/06-21-proxy-realism-enhancement

你是 Go 后端工程师，为 sub2api 实施 **R1 第一步：IdentityRegistry 与版本 fetcher 框架**。

## 上下文
- 仓库根 `/mnt/x/project/sub2api`，后端 `backend/`，分支 `feat/proxy-realism`。
- R2（ws 一键全开）✓、R3（TLS 家族化框架）✓ 已完成。
- 目标：建立 IdentityRegistry（atomic.Pointer 持有 claude+codex snapshot）+ VersionFetcher 周期任务框架，暂不真联网。

## 关键设计点（来自 design.md，codex 审查已修正）

- **atomic 一致性**：一次更新 = 一个完整一致 snapshot（claude + codex 两族基线）；任一字段缺失则整体丢弃保留旧值。
- **联动落点全集**（暂不改造，只预留接口）：
  - Claude：`pkg/claude.DefaultHeaders` + `service/gateway_billing_block.go:73`(cc_version) + `service/identity_service.go:28` + `service/gateway_service.go:4322`
  - Codex：`service/openai_gateway_service.go:45/1268/71`(UA/originator/x-codex)
- **不在请求热路径联网**：fetch 在后台 goroutine，带超时/重试/频率限制。
- **开关与回滚**：config `gateway.ua_auto_fetch.{enabled(默认false),interval,sources}`；关闭=回退硬编码常量（今日行为）。

## 任务步骤

### C1. IdentityRegistry（已有 clientidentity，扩展之）
在 `backend/internal/pkg/clientidentity/registry.go`：

```go
package clientidentity

import "sync/atomic"

// Registry 持有当前生效的 claude + codex 两族完整 snapshot
type Registry struct {
	snapshotPtr atomic.Pointer[Snapshots]
}

// Snapshots 是两族的完整基线
type Snapshots struct {
	Claude ClaudeSnapshot
	Codex  CodexSnapshot
}

type ClaudeSnapshot struct {
	Headers       map[string]string  // UA, X-Stainless-*, X-App, anthropic-beta
	VersionFields VersionFields      // cli/sdk/cc_version/fp
	TLSProfileName string
}

type CodexSnapshot struct {
	Headers       map[string]string  // UA, originator, x-codex-*
	VersionFields VersionFields
	TLSProfileName string
}

func NewRegistry() *Registry {
	r := &Registry{}
	// 初始化为内置兜底（现有硬编码常量）
	r.snapshotPtr.Store(&Snapshots{
		Claude: r.defaultClaudeSnapshot(),
		Codex:  r.defaultCodexSnapshot(),
	})
	return r
}

// Get 只读 getter（原子）
func (r *Registry) Get() *Snapshots {
	return r.snapshotPtr.Load()
}

// Swap 原子更新（仅 VersionFetcher 调用）
func (r *Registry) Swap(snapshots *Snapshots) {
	r.snapshotPtr.Store(snapshots)
}

func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
	// TODO: 从 pkg/claude.DefaultHeaders / CLICurrentVersion 读取
	return ClaudeSnapshot{
		Headers: map[string]string{
			"User-Agent": "claude-code/...", // 占位
		},
		VersionFields: VersionFields{
			CLIVersion: "0.x.x",
			SDKVersion: "0.y.y",
		},
		TLSProfileName: "claude-cli-default",
	}
}

func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
	// TODO: 从 service/openai_gateway_service.go:45 读取
	return CodexSnapshot{
		Headers: map[string]string{
			"User-Agent": "codex_cli_rs/...", // 占位
		},
		VersionFields: VersionFields{
			CLIVersion: "0.x.x",
		},
		TLSProfileName: "codex-cli-default",
	}
}
```

### C2. VersionFetcher service（框架，不真联网）
在 `backend/internal/service/version_fetcher_service.go`：

```go
package service

import (
	"context"
	"time"
	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
)

type VersionFetcherService struct {
	registry *clientidentity.Registry
	cfg      *config.Config
	stopCh   chan struct{}
}

func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
	return &VersionFetcherService{
		registry: registry,
		cfg:      cfg,
		stopCh:   make(chan struct{}),
	}
}

// Start 后台周期任务（仅当 cfg.Gateway.UAAutoFetch.Enabled 时）
func (s *VersionFetcherService) Start() {
	if !s.cfg.Gateway.UAAutoFetch.Enabled {
		return // 关闭开关 = 不启动，registry 保持内置兜底
	}

	interval := s.cfg.Gateway.UAAutoFetch.Interval
	if interval == 0 {
		interval = 1 * time.Hour // 默认间隔
	}

	go func() {
		ticker := time.NewTicker(interval)
		defer ticker.Stop()

		for {
			select {
			case <-ticker.C:
				s.fetchAndUpdate()
			case <-s.stopCh:
				return
			}
		}
	}()
}

func (s *VersionFetcherService) Stop() {
	close(s.stopCh)
}

func (s *VersionFetcherService) fetchAndUpdate() {
	ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
	defer cancel()

	// TODO: 真实拉取逻辑（npm @anthropic-ai/claude-code、github openai/codex）
	// 现在只是框架，不真联网
	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)

	if claudeVersion == "" || codexVersion == "" {
		// 失败丢弃保留旧值
		return
	}

	// 组装完整一致 snapshot（任一字段缺失则整体丢弃）
	snapshots := &clientidentity.Snapshots{
		Claude: clientidentity.ClaudeSnapshot{
			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
			VersionFields: clientidentity.VersionFields{
				CLIVersion: claudeVersion,
				SDKVersion: claudeSDKVersion,
			},
			TLSProfileName: "claude-cli-default",
		},
		Codex: clientidentity.CodexSnapshot{
			Headers: s.buildCodexHeaders(codexVersion),
			VersionFields: clientidentity.VersionFields{
				CLIVersion: codexVersion,
			},
			TLSProfileName: "codex-cli-default",
		},
	}

	// 原子 swap
	s.registry.Swap(snapshots)
}

func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
	// TODO: 实际 npm/github API 调用
	// 占位：直接返回空（触发丢弃）
	return "", "", ""
}

func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
	// TODO: 按 design 完整构造 UA / X-Stainless-* / X-App / anthropic-beta
	return map[string]string{
		"User-Agent": "claude-code/" + cliVer,
	}
}

func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
	// TODO: 按 design 完整构造 UA / originator / x-codex-*
	return map[string]string{
		"User-Agent": "codex_cli_rs/" + cliVer,
	}
}
```

### C3. 配置（config.yaml / config.go）
在 `backend/internal/config/config.go` 扩展 `Gateway` 结构：

```go
type Gateway struct {
	// ... 现有字段
	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
}

type UAAutoFetchConfig struct {
	Enabled  bool          `mapstructure:"enabled"`
	Interval time.Duration `mapstructure:"interval"`
	// Sources map[string]string  // 预留：可配置 npm registry / github API
}
```

### C4. Wire 注入
在 `backend/internal/service/wire.go` 的 `ProviderSet` 追加：

```go
clientidentity.NewRegistry,
NewVersionFetcherService,
```

在 `backend/cmd/server/wire.go` 的 `initializeApplication` 参数列表追加 `versionFetcherService *service.VersionFetcherService`，并在启动后调用 `versionFetcherService.Start()`。

### C5. 单测
`backend/internal/pkg/clientidentity/registry_test.go`：

```go
func TestRegistryAtomicSwap(t *testing.T) {
	registry := NewRegistry()
	initial := registry.Get()

	newSnapshots := &Snapshots{
		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
	}
	registry.Swap(newSnapshots)

	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
	assert.NotEqual(t, initial, registry.Get()) // 指针已变
}
```

`backend/internal/service/version_fetcher_service_test.go`：

```go
func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
	cfg := &config.Config{Gateway: config.Gateway{UAAutoFetch: config.UAAutoFetchConfig{Enabled: false}}}
	registry := clientidentity.NewRegistry()
	svc := NewVersionFetcherService(registry, cfg)

	svc.Start()
	time.Sleep(50 * time.Millisecond)
	// 验证 registry 未被更新（仍是初始值）
}
```

## 验证
```bash
cd backend
go build ./internal/pkg/clientidentity
go build ./internal/service
go test ./internal/pkg/clientidentity -run TestRegistryAtomicSwap -v
go test ./internal/service -run TestVersionFetcherDisabledDoesNotStart -v
```

## 输出
完成后报告：
1. IdentityRegistry 结构（文件 + 关键方法）
2. VersionFetcherService 框架（Start/Stop/fetchAndUpdate，但 fetchVersions 占位）
3. 配置扩展（UAAutoFetchConfig）
4. Wire 注入点
5. 验证结果

## 注意
- **这步只搭框架**，不真联网（`fetchVersions` 返回空触发丢弃）。
- **不改全落点**（DefaultHeaders / billing block / identity_service / codex headers）—— 那是下一步的事。
- **开关默认 false**：现有行为不变，registry 保持内置兜底。

开始执行。
hook: SessionStart
hook: SessionStart Completed
hook: UserPromptSubmit
hook: UserPromptSubmit Completed
codex
// DECISION: skill_needed=true reason=项目有 Trellis 规则，编码前需读取相关规范。我会先读取任务和项目规范，再按影响分析要求确认改动范围。
exec
/bin/bash -lc "sed -n '1,240p' /mnt/x/project/sub2api/.agents/skills/trellis-before-dev/SKILL.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
---
name: trellis-before-dev
description: "Discovers and injects project-specific coding guidelines from .trellis/spec/ before implementation begins. Reads spec indexes, pre-development checklists, and shared thinking guides for the target package. Use when starting a new coding task, before writing any code, switching to a different package, or needing to refresh project conventions and standards."
---

Read the relevant development guidelines before starting your task.

Execute these steps:

1. **Read current task artifacts**:
   - `prd.md` for requirements and acceptance criteria
   - `design.md` if present for technical design
   - `implement.md` if present for execution order and validation plan

2. **Discover packages and their spec layers**:
   ```bash
   python3 ./.trellis/scripts/get_context.py --mode packages
   ```

3. **Identify which specs apply** to your task based on:
   - Which package you're modifying (e.g., `cli/`, `docs-site/`)
   - What type of work (backend, frontend, unit-test, docs, etc.)
   - Any spec/research paths referenced by the task artifacts

4. **Read the spec index** for each relevant module:
   ```bash
   cat .trellis/spec/<package>/<layer>/index.md
   ```
   Follow the **"Pre-Development Checklist"** section in the index.

5. **Read the specific guideline files** listed in the Pre-Development Checklist that are relevant to your task. The index is NOT the goal — it points you to the actual guideline files (e.g., `error-handling.md`, `conventions.md`, `mock-strategies.md`). Read those files to understand the coding standards and patterns.

6. **Always read shared guides**:
   ```bash
   cat .trellis/spec/guides/index.md
   ```

7. Understand the coding standards and patterns you need to follow, then proceed with your development plan.

This step is **mandatory** before writing any code.

exec
/bin/bash -lc 'rg -n "sub2api|proxy-realism|IdentityRegistry|VersionFetcher|ua_auto_fetch" /home/mci777/.codex/memories/MEMORY.md' in /mnt/x/project/sub2api
 succeeded in 0ms:
769:- oauth_transport, oauth_token_missing, oauth_state, retry=1/10, Local OAuth service is not reachable, localhost:1455, proxy recovery, application/tasks.py, scripts/chatgpt_relogin_at_sub2api.py
802:- `scripts/chatgpt_relogin_at_sub2api.py` 里 `Local OAuth service is not reachable` 这类 1455 本地故障需要被 `is_local_oauth_service_error()` 单独识别，并从 `is_relogin_network_error()` 排除 [Task 1]
807:- 这条分支的脚本层和 in-flow 层都要补：`scripts/chatgpt_relogin_at_sub2api.py` 负责识别 existing-phone OTP 并保留 no-callback fallback，`AuthFlow` 负责真实 OAuth 流内部不再提前抛 `SecondaryVerificationRequired` [Task 3]
1004:- sync free accounts to img, access_token, accessToken, token, mail.topiao.com-team, team.feelopencode.com, pr.piao.edu.pl-team, CpaAuthFilesService.deep_sync, application/sub2api_repair.py, ImgSiteSyncPlugin.push, 127.0.0.1:3002
1038:- CPA pull 现在可以走 `CpaAuthFilesService.deep_sync()` 拉原始 auth JSON，再在本地创建/更新 ChatGPT 账号；`application/sub2api_repair.py` 也能接受 `access_token`、`accessToken`、`token` 三种 access-token alias，并从文件名/名称推断 plan 类型 [Task 5]
1172:- smsauth, oauth, phase c, session/select, unified session cookie, usc_, us_, 1455, sub2api, oauth_upload_status, chatgpt_session, tests/test_chatgpt_sms_registration.py
1192:- 这条线成功路径已经明确：复用 Phase A ChatGPT session -> 启动 Codex OAuth -> `session/select` -> 创建 OAuth 绑定邮箱 -> `add-email/send` -> OTP 校验 -> `consent.data` -> `workspace/select` -> 本地 1455 callback exchange -> `sync_openai_oauth_token_info_to_sub2api(...)` [Task 1]
1194:- OAuth 绑定邮箱必须是新建的 OAuth mailbox email，不是原手机号标识；1455/Sub2API 上传结果会写回 `oauth_auth_method`、`oauth_upload_method`、`oauth_upload_status`、`oauth_upload_sub2api_account_id`、`oauth_upload_updated_at` 这些概览字段 [Task 1]

ERROR: Reconnecting... 1/5
codex
我先按 Trellis 要求读当前任务材料和后端规范；这一步只是确认边界，避免把后续“全落点改造”提前做进去。
exec
/bin/bash -lc 'ls -la .trellis/tasks/06-21-proxy-realism-enhancement' in /mnt/x/project/sub2api
 succeeded in 0ms:
total 1764
drwxrwxrwx 1 mci777 mci777    4096 Jun 21 12:20 .
drwxrwxrwx 1 mci777 mci777    4096 Jun 21 11:50 ..
-rwxrwxrwx 1 mci777 mci777    8640 Jun 21 12:19 R1_step1_registry_framework.txt
-rwxrwxrwx 1 mci777 mci777   13577 Jun 21 12:22 R1_step1_result.txt
-rwxrwxrwx 1 mci777 mci777    6567 Jun 21 11:51 R2_prompt.txt
-rwxrwxrwx 1 mci777 mci777 1295306 Jun 21 12:06 R2_result.txt
-rwxrwxrwx 1 mci777 mci777    6408 Jun 21 12:08 R3_prompt.txt
-rwxrwxrwx 1 mci777 mci777  419813 Jun 21 12:18 R3_result.txt
-rwxrwxrwx 1 mci777 mci777     254 Jun 21 01:26 check.jsonl
-rwxrwxrwx 1 mci777 mci777    8994 Jun 21 01:59 design.md
-rwxrwxrwx 1 mci777 mci777     254 Jun 21 01:26 implement.jsonl
-rwxrwxrwx 1 mci777 mci777    5269 Jun 21 02:01 implement.md
-rwxrwxrwx 1 mci777 mci777    5746 Jun 21 01:35 prd.md
-rwxrwxrwx 1 mci777 mci777     727 Jun 21 11:50 task.json

exec
/bin/bash -lc 'find .trellis/spec -maxdepth 4 -type f | sort' in /mnt/x/project/sub2api
 succeeded in 0ms:
.trellis/spec/README.md
.trellis/spec/backend/ai-sdk-integration.md
.trellis/spec/backend/authentication.md
.trellis/spec/backend/database.md
.trellis/spec/backend/directory-structure.md
.trellis/spec/backend/index.md
.trellis/spec/backend/logging.md
.trellis/spec/backend/orpc-usage.md
.trellis/spec/backend/performance.md
.trellis/spec/backend/quality.md
.trellis/spec/backend/type-safety.md
.trellis/spec/big-question/index.md
.trellis/spec/big-question/postgres-json-jsonb.md
.trellis/spec/big-question/sentry-nextintl-conflict.md
.trellis/spec/big-question/turbopack-webpack-flexbox.md
.trellis/spec/big-question/webkit-tap-highlight.md
.trellis/spec/frontend/ai-sdk-integration.md
.trellis/spec/frontend/api-integration.md
.trellis/spec/frontend/authentication.md
.trellis/spec/frontend/components.md
.trellis/spec/frontend/css-layout.md
.trellis/spec/frontend/directory-structure.md
.trellis/spec/frontend/hooks.md
.trellis/spec/frontend/index.md
.trellis/spec/frontend/orpc-usage.md
.trellis/spec/frontend/quality.md
.trellis/spec/frontend/state-management.md
.trellis/spec/frontend/type-safety.md
.trellis/spec/guides/cross-layer-thinking-guide.md
.trellis/spec/guides/index.md
.trellis/spec/guides/pre-implementation-checklist.md
.trellis/spec/shared/code-quality.md
.trellis/spec/shared/dependencies.md
.trellis/spec/shared/index.md
.trellis/spec/shared/typescript.md

exec
/bin/bash -lc 'python3 ./.trellis/scripts/get_context.py --mode packages' in /mnt/x/project/sub2api
 succeeded in 0ms:
Single-repo project (no packages configured)

Spec layers: backend, big-question, frontend, shared

codex
任务目录里已有一个 `R1_step1_result.txt`，我会读取它确认是否有前序尝试，但仍以当前仓库实际状态为准。
exec
/bin/bash -lc "sed -n '1,220p' .trellis/tasks/06-21-proxy-realism-enhancement/prd.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
# 反代真实性增强：UA 拉取 / codex-ws 全开 / 家族化 UA↔TLS 指纹

## Goal

让 sub2api 发往上游官方端点的请求更贴近**真实官方客户端**，降低被判第三方的风险。三个子能力构成一个连贯的「客户端身份」体系：

1. **UA 自动拉取**（codex + claude）：从官方发布源识别最新客户端版本，自动更新对外伪装的 UA 及其联动指纹字段。
2. **codex websocket 一键全开**：admin 侧一次操作即可为账号开启整组 websocket 长连接能力。
3. **家族化 UA↔TLS 配对指纹库**：为真实客户端家族（codex-cli / codex-desktop / claude-cli / claude-desktop）分别提供真实 TLS 指纹，且 UA 与 TLS 按「产品家族 + 变体」成对一致。

## 背景与现状（已调研）

- **UA 现状**：硬编码常量。`pkg/claude/constants.go` 的 `CLICurrentVersion = "2.1.161"`、`User-Agent: claude-cli/<ver> (external, cli)`；codex 侧 `repository/openai_oauth_service.go` 用 `codex-cli/0.91.0`。注释明确警告：**UA 版本必须与 `X-Stainless-Package-Version`、`cc_version` 等一组指纹字段严格一致，不一致会被 Anthropic 判第三方**。→ UA 拉取不是改单个字符串，而是改**一组联动字段**。
- **codex ws 现状**：账号 `Extra` 上一组开关由 `service/account.go` 解析：`openai_ws_enabled`、`responses_websockets_v2_enabled`、`openai_oauth_responses_websockets_v2_enabled`、`openai_apikey_responses_websockets_v2_enabled`、对应 `*_mode`、`openai_ws_force_http`、`openai_ws_allow_store_recovery` 等。底层用 `coder/websocket`。
- **TLS 现状**：`pkg/tlsfingerprint/dialer.go` 用 utls + `HelloCustom`，从 `Profile`/DB `model.TLSFingerprintProfile` 构造 ClientHello；默认模拟 Claude Code（固定 JA3 `44f88fca...`）。已有 admin `tls_fingerprint_profile_handler.go` 管理 profile。→ 已具备「按 profile 定制指纹」的底座，需扩展为**家族化预设 + 与账号/UA 的配对绑定**。

## Requirements

### R1 — UA 自动拉取（codex + claude）
- 从官方发布源（GitHub release / npm 等）周期性识别 codex CLI 与 claude CLI 的最新版本号。
- 拉取到新版本后，更新对外 UA 及**全部联动指纹字段**，保证版本号在 UA / X-Stainless-* / cc_version 等处严格一致。
- 必须可关闭（开关）并有**内置兜底版本**：联网失败 / 解析失败时回退到内置默认，绝不发出残缺或不一致的头。
- 拉取结果可观测（当前生效版本、来源、最后更新时间、上次失败原因）。

### R2 — codex websocket 一键全开
- 提供一个「一键全开」入口（admin 操作或账号级开关），一次性把目标 codex 账号整组 `openai_*ws*` Extra 字段置为启用/期望模式。
- 明确「全部 ws 功能」覆盖的字段集合与各自取值（在 design.md 固化清单）。
- 操作幂等、可回退（能一键关回默认）。

### R3 — 家族化 UA↔TLS 配对指纹库
- 内置真实客户端家族预设：至少 codex-cli / codex-desktop / claude-cli / claude-desktop，各自包含「真实 UA（族）」+「真实 TLS 指纹」。
- UA 与 TLS 指纹**成对一致**：选定某家族变体后，该连接发出的 UA 头与 TLS ClientHello 同属该家族，不出现「codex desktop 的 UA 配 claude cli 的 TLS」这类错配。
- 复用现有 `tlsfingerprint.Profile` / `model.TLSFingerprintProfile` 底座；新增的是**家族预设 + 配对关系**，不是另起炉灶。
- 选择/绑定方式（账号级 or profile 级 or 全局默认）在 design.md 确定；**默认保持现有 Claude 指纹行为不变**。

## 约束

- 默认（不开启任何新开关）行为与今日一致：完整版现有 UA / 指纹 / ws 解析逻辑不被破坏。
- 本能力在**完整版与精简版**下都要可用，因此实现**不得依赖 payment / subscription 模块**。
- UA 联动字段一致性是硬约束：任何自动更新都要么整组一致更新，要么整组回退兜底，禁止半更新。
- 联网拉取需有超时、失败重试与频率限制，不阻塞启动与请求热路径。
- 遵循仓库现有分层（handler / service / repository / pkg）与 wire 注入约定。

## Acceptance Criteria

- [ ] R1：开启自动拉取后，能在不重启的情况下把 codex/claude UA 及联动字段更新到最新版本；联网失败时稳定回退内置兜底且日志可见；关闭开关后行为回到硬编码默认。
- [ ] R1：存在测试覆盖「版本解析 + 联动字段一致性 + 失败回退」。
- [ ] R2：对一个 codex 账号执行「一键全开」后，`service/account.go` 各 ws 解析方法返回期望启用态；执行「一键关回」后恢复默认；操作幂等。
- [ ] R3：内置 ≥4 个家族变体预设（codex-cli/desktop、claude-cli/desktop），每个变体 UA 与 TLS 指纹成对；选定变体后实际发出的 UA 与 ClientHello 同族（有测试/抓包验证手段）。
- [ ] R3：默认配置下指纹与 UA 行为与改动前完全一致（无回归）。
- [ ] 完整版与精简版均能编译并启用本能力；全量 `go test ./...`（完整版）通过。

## 开放问题（design 阶段解决）

- UA 发布源的确切端点与解析方式（GitHub API vs npm registry vs 官方下载页），以及联动字段（X-Stainless-* 等）如何随版本推导。
- 家族变体的「真实 TLS 指纹」数据如何获得与维护（抓包采集 / 已知 JA3 库 / 内置常量）。
- 家族变体的选择粒度：账号级绑定 / profile 扩展 / 请求级路由。

## Notes

- 复杂任务：本 prd 之后需补 `design.md`（技术设计）与 `implement.md`（执行清单）方可 `task.py start`。

exec
/bin/bash -lc "sed -n '1,220p' .trellis/tasks/06-21-proxy-realism-enhancement/R1_step1_result.txt" in /mnt/x/project/sub2api
 succeeded in 0ms:
Reading additional input from stdin...
OpenAI Codex v0.141.0
--------
workdir: /mnt/x/project/sub2api
model: gpt-5.5
provider: fkcodex
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: detailed
session id: 019ee868-3289-7430-afb0-f914862fd7ee
--------
user
Active task: .trellis/tasks/06-21-proxy-realism-enhancement

你是 Go 后端工程师，为 sub2api 实施 **R1 第一步：IdentityRegistry 与版本 fetcher 框架**。

## 上下文
- 仓库根 `/mnt/x/project/sub2api`，后端 `backend/`，分支 `feat/proxy-realism`。
- R2（ws 一键全开）✓、R3（TLS 家族化框架）✓ 已完成。
- 目标：建立 IdentityRegistry（atomic.Pointer 持有 claude+codex snapshot）+ VersionFetcher 周期任务框架，暂不真联网。

## 关键设计点（来自 design.md，codex 审查已修正）

- **atomic 一致性**：一次更新 = 一个完整一致 snapshot（claude + codex 两族基线）；任一字段缺失则整体丢弃保留旧值。
- **联动落点全集**（暂不改造，只预留接口）：
  - Claude：`pkg/claude.DefaultHeaders` + `service/gateway_billing_block.go:73`(cc_version) + `service/identity_service.go:28` + `service/gateway_service.go:4322`
  - Codex：`service/openai_gateway_service.go:45/1268/71`(UA/originator/x-codex)
- **不在请求热路径联网**：fetch 在后台 goroutine，带超时/重试/频率限制。
- **开关与回滚**：config `gateway.ua_auto_fetch.{enabled(默认false),interval,sources}`；关闭=回退硬编码常量（今日行为）。

## 任务步骤

### C1. IdentityRegistry（已有 clientidentity，扩展之）
在 `backend/internal/pkg/clientidentity/registry.go`：

```go
package clientidentity

import "sync/atomic"

// Registry 持有当前生效的 claude + codex 两族完整 snapshot
type Registry struct {
	snapshotPtr atomic.Pointer[Snapshots]
}

// Snapshots 是两族的完整基线
type Snapshots struct {
	Claude ClaudeSnapshot
	Codex  CodexSnapshot
}

type ClaudeSnapshot struct {
	Headers       map[string]string  // UA, X-Stainless-*, X-App, anthropic-beta
	VersionFields VersionFields      // cli/sdk/cc_version/fp
	TLSProfileName string
}

type CodexSnapshot struct {
	Headers       map[string]string  // UA, originator, x-codex-*
	VersionFields VersionFields
	TLSProfileName string
}

func NewRegistry() *Registry {
	r := &Registry{}
	// 初始化为内置兜底（现有硬编码常量）
	r.snapshotPtr.Store(&Snapshots{
		Claude: r.defaultClaudeSnapshot(),
		Codex:  r.defaultCodexSnapshot(),
	})
	return r
}

// Get 只读 getter（原子）
func (r *Registry) Get() *Snapshots {
	return r.snapshotPtr.Load()
}

// Swap 原子更新（仅 VersionFetcher 调用）
func (r *Registry) Swap(snapshots *Snapshots) {
	r.snapshotPtr.Store(snapshots)
}

func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
	// TODO: 从 pkg/claude.DefaultHeaders / CLICurrentVersion 读取
	return ClaudeSnapshot{
		Headers: map[string]string{
			"User-Agent": "claude-code/...", // 占位
		},
		VersionFields: VersionFields{
			CLIVersion: "0.x.x",
			SDKVersion: "0.y.y",
		},
		TLSProfileName: "claude-cli-default",
	}
}

func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
	// TODO: 从 service/openai_gateway_service.go:45 读取
	return CodexSnapshot{
		Headers: map[string]string{
			"User-Agent": "codex_cli_rs/...", // 占位
		},
		VersionFields: VersionFields{
			CLIVersion: "0.x.x",
		},
		TLSProfileName: "codex-cli-default",
	}
}
```

### C2. VersionFetcher service（框架，不真联网）
在 `backend/internal/service/version_fetcher_service.go`：

```go
package service

import (
	"context"
	"time"
	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
)

type VersionFetcherService struct {
	registry *clientidentity.Registry
	cfg      *config.Config
	stopCh   chan struct{}
}

func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
	return &VersionFetcherService{
		registry: registry,
		cfg:      cfg,
		stopCh:   make(chan struct{}),
	}
}

// Start 后台周期任务（仅当 cfg.Gateway.UAAutoFetch.Enabled 时）
func (s *VersionFetcherService) Start() {
	if !s.cfg.Gateway.UAAutoFetch.Enabled {
		return // 关闭开关 = 不启动，registry 保持内置兜底
	}

	interval := s.cfg.Gateway.UAAutoFetch.Interval
	if interval == 0 {
		interval = 1 * time.Hour // 默认间隔
	}

	go func() {
		ticker := time.NewTicker(interval)
		defer ticker.Stop()

		for {
			select {
			case <-ticker.C:
				s.fetchAndUpdate()
			case <-s.stopCh:
				return
			}
		}
	}()
}

func (s *VersionFetcherService) Stop() {
	close(s.stopCh)
}

func (s *VersionFetcherService) fetchAndUpdate() {
	ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
	defer cancel()

	// TODO: 真实拉取逻辑（npm @anthropic-ai/claude-code、github openai/codex）
	// 现在只是框架，不真联网
	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)

	if claudeVersion == "" || codexVersion == "" {
		// 失败丢弃保留旧值
		return
	}

	// 组装完整一致 snapshot（任一字段缺失则整体丢弃）
	snapshots := &clientidentity.Snapshots{
		Claude: clientidentity.ClaudeSnapshot{
			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
			VersionFields: clientidentity.VersionFields{
				CLIVersion: claudeVersion,
				SDKVersion: claudeSDKVersion,
			},
			TLSProfileName: "claude-cli-default",
		},
		Codex: clientidentity.CodexSnapshot{
			Headers: s.buildCodexHeaders(codexVersion),
			VersionFields: clientidentity.VersionFields{
				CLIVersion: codexVersion,
			},
			TLSProfileName: "codex-cli-default",
		},
	}

	// 原子 swap
	s.registry.Swap(snapshots)
}

func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
	// TODO: 实际 npm/github API 调用
	// 占位：直接返回空（触发丢弃）
	return "", "", ""
}

func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
	// TODO: 按 design 完整构造 UA / X-Stainless-* / X-App / anthropic-beta
	return map[string]string{
		"User-Agent": "claude-code/" + cliVer,
	}
}

func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
	// TODO: 按 design 完整构造 UA / originator / x-codex-*
	return map[string]string{

exec
/bin/bash -lc "sed -n '1,220p' .trellis/tasks/06-21-proxy-realism-enhancement/implement.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
# 子1 执行计划：反代真实性增强

> 配套 `prd.md` + `design.md`。三块（R1/R2/R3）可独立交付；建议按 R2 → R3 → R1 顺序（由易到难、由低风险到高风险）。每步带验证命令与回滚点。

## 前置
- [ ] 工作分支：`feat/proxy-realism-enhancement`（从 main）。
- [ ] 基线：`cd backend && go build ./... && go test ./... ` 全绿，记录基线。

---

## 阶段 A — R2：codex websocket 一键全开（最低风险，先做）

- [ ] A1. service 层加 `EnableAllOpenAIWS(ctx, accountID)` / `ResetOpenAIWS(ctx, accountID)`：按 design「全开目标态」**8 字段表**写/删 `accounts.extra`（**含两个分类型 `*_enabled=true`**，否则已有 false 覆盖通用 true）；非 OpenAI 账号返回错误。
- [ ] A1b.（codex-C）enable-all 前校验全局 `gateway.openai_ws.responses_websockets_v2` 未禁用；禁用时返回明确提示（账号 extra 无法覆盖全局禁用）。OAuth/API Key 类型分别处理。
- [ ] A2. admin handler 加动作 + 路由（`routes/admin.go`），仅 admin 鉴权可调用。
- [ ] A3. 单测：enable-all 后 `account.go` 各 ws 解析方法返回期望值（**覆盖已有分类型 false 被纠正**）；全局禁用时拒绝；reset 回默认；重复执行幂等。
- [ ] A4.（可选）前端账号管理加「一键全开 WS / 重置」按钮。
- **验证**：`go test ./internal/service/... ./internal/handler/admin/...`
- **回滚点**：纯新增动作，删除新增文件/路由即可。

---

## 阶段 B — R3：家族化 UA↔TLS 配对（中风险）

- [ ] B1. 新建 `internal/pkg/clientidentity`：定义 `ClientFamily` + 上层 `Resolve(account) -> snapshot{headers, version fields, tls profile name}`（codex-D：family 解析在上层，**不进 dialer**）；默认 family 行为等价今日。
- [ ] B2. 采集真实指纹/UA（数据依赖）：抓包或查已知库得到 codex-cli/desktop、claude-cli/desktop 的 TLS 指纹与 UA（codex 注意真实族是 `codex_cli_rs`，UA 含 OS/arch 后缀）；落为 `tlsfingerprint.Profile` 预设 + UA 模板，**注明来源与采集日期**。codex family 必须有自己的 profile，不可复用 Node/Claude 默认指纹。
- [ ] B3. TLS profile 选择路径对接上层 resolver（复用/对接现有 `ResolveTLSProfile(account)`，`account_usage_service.go:1165`）；`tlsfingerprint.NewDialer` 保持低层只收 profile，无 family 时用今日默认。
- [ ] B4. header / billing / identity 注入点统一从 resolver 取 snapshot，保证与 TLS 同族（杜绝错配）。
- [ ] B5. 测试：family→snapshot 配对断言（header 与 tls profile 同族）；**无 family = 现有 profile 回归**；可选 dialer 抓包集成测试。
- **验证**：`go test ./internal/pkg/clientidentity/... ./internal/pkg/tlsfingerprint/...`
- **review gate**：B2 指纹数据需人工核对真实性后再继续（错误指纹危害大于不改）。
- **回滚点**：清空账号 `client_family` 即回今日行为。

---

## 阶段 C — R1：UA 自动拉取（最高风险，最后做）

- [ ] C1. `IdentityRegistry`（pkg/clientidentity）：`atomic.Pointer` 持有 **claude + codex 两族完整 snapshot**，只读 getter；初始化为内置兜底（现有常量）。
- [ ] C2. 注入点改造（codex-A：**全落点**，非仅 DefaultHeaders）：`pkg/claude.DefaultHeaders`、`service/gateway_billing_block.go:73`(cc_version/fp/cch)、`service/gateway_service.go:4322`、`service/identity_service.go:28`、codex `openai_gateway_service.go:45/1268/71`(UA/originator/x-codex) 全部改为读 snapshot，缺省回退常量。**先在 snapshot 恒等于常量前提下跑通（零行为变化）**，再接 fetcher。
- [ ] C3. `VersionFetcher` service：claude-cli(npm @anthropic-ai/claude-code)、claude-sdk(**优先解析 claude-code 包内 sdk 依赖**，latest 兜底)、codex(github/npm，真实族 codex_cli_rs)；组装**完整一致快照**；失败丢弃保留旧值。处理解析陷阱（v 前缀/预发布/latest 不同步/UA 后缀/originator 同族）。
- [ ] C4. 周期调度（后台 goroutine，不阻塞启动/请求），超时+重试+频率限制。
- [ ] C5. config 开关 `gateway.ua_auto_fetch.{enabled,interval,sources}`（默认 enabled=false）；admin setting 暴露生效版本/来源/最后状态。
- [ ] C6. 测试：版本解析（mock 响应）、联动字段一致性、失败回退、关闭开关=常量。
- **验证**：`go test ./internal/service/... ./internal/pkg/claude/... ./internal/pkg/clientidentity/...`
- **回滚点**：`enabled=false` 即回硬编码；或回退 commit。

---

## 收尾（全部完成后）
- [ ] 全量 `go build ./... && go vet ./... && go test ./...`（完整版）通过。
- [ ] 确认本能力不引用 payment/subscription（`go list` 依赖检查），为子2 精简版可用性背书。
- [ ] 3.3 spec 更新：把「UA 联动字段一致性」「ws 全开字段清单」「family 指纹采集来源」沉淀到 `.trellis/spec`。
- [ ] 3.4 提交。

## 验证命令汇总
```bash
cd backend
go build ./... && go vet ./...
go test ./internal/service/... ./internal/handler/admin/... \
        ./internal/pkg/clientidentity/... ./internal/pkg/tlsfingerprint/... ./internal/pkg/claude/...
```

exec
/bin/bash -lc "sed -n '1,260p' .trellis/tasks/06-21-proxy-realism-enhancement/design.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
# 子1 设计：反代真实性增强

> 配套 prd：`prd.md`。本文件聚焦技术设计：边界、契约、数据流、权衡、兼容性、灰度/回滚。
> 已整合 codex 交叉验证（gpt-5.5）must-fix，见末尾「修订记录」。
> 三个能力相对独立，可分别实现、分别灰度，故按 R1/R2/R3 分节设计。

## 总体边界

- 新增一个轻量 package：`internal/pkg/clientidentity`（暂定名），统一承载「客户端身份快照」概念：
  - **家族（family）枚举**：`codex-cli` / `codex-desktop` / `claude-cli` / `claude-desktop`。
  - 每个家族解析为一个**完整 identity snapshot**：`{ Headers(UA + X-Stainless-* + X-App + anthropic-beta 组合), VersionFields(cli/sdk/cc_version 及 fp), TLSProfileName }`。
- **核心架构原则（codex-D）**：family → snapshot 的解析放在**上层 resolver**，header 注入点、billing block、identity fingerprint、TLS profile 选择**全部从同一个 resolver 取值**，从结构上杜绝「header 切了 family、TLS/billing 仍是旧默认」的错配。
- 该 package **不依赖** payment / subscription（满足精简版可用约束）。
- 复用既有底座：`pkg/tlsfingerprint`（保持纯 TLS 执行层）、既有 `ResolveTLSProfile(account)` 路径、`model.TLSFingerprintProfile`、`pkg/claude/constants.go`、`pkg/openai`、`service/identity_service.go`。

---

## R1 — UA 自动拉取（codex + claude）

### A. 联动落点全集（codex-A：不止 DefaultHeaders）
版本/身份字段硬编码分散在多处，**必须由同一 snapshot 统一驱动**，否则半更新会更易被判第三方：

| 落点 | 文件:行 | 联动内容 |
|------|---------|----------|
| Claude 默认头 | `pkg/claude/constants.go:68,91` | `CLICurrentVersion`、`User-Agent`、`X-Stainless-Package-Version`、`X-Stainless-Runtime-Version`、`X-Stainless-OS/Arch`、`X-App` |
| Claude billing 归因 | `service/gateway_billing_block.go:73` | `cc_version=X.Y.Z.{fp}`、`cc_entrypoint=cli`、`cch` 占位与**签名顺序**（须在 body 最终修改后签名，见 `gateway_service.go:6711`） |
| Claude 模板替换 | `service/gateway_service.go:4322` | 硬读 `claude.CLICurrentVersion` |
| 默认 identity 指纹 | `service/identity_service.go:28` | 硬编码同一组 Stainless 字段 |
| Codex 客户端身份 | `service/openai_gateway_service.go:45,1268,71` | `codexCLIUserAgent`、`codexCLIVersion`、`originator` 默认值、`session_id`/`conversation_id`/`x-codex-turn-state`/`x-codex-turn-metadata`/`OpenAI-Beta` 透传白名单 |

设计：把 **Claude identity snapshot** 与 **Codex identity snapshot** 作为两个完整快照对象，上述落点改为读快照（缺省回退现有硬编码常量）。

### B. 数据源（codex-B 修正）
| 目标 | 来源 | 注意 |
|------|------|------|
| claude-cli 版本 | npm `@anthropic-ai/claude-code` dist-tags.latest | 仅作版本提示 |
| claude SDK 版本 | **优先**解析 claude-code 发布包内实际依赖的 `@anthropic-ai/sdk` 版本；npm sdk@latest 仅兜底 | CLI 未必用 sdk latest，直接拉 latest 有超前/滞后风险 |
| codex 版本 | github releases `openai/codex` / npm `@openai/codex` | 真实伪装对象是 `codex_cli_rs/<ver>`（见 `openai_gateway_service.go:45`），非泛 `@openai/codex` |

解析陷阱（必须处理）：release tag 带 `v`/预发布、npm latest 与 github latest 不同步、UA 需 OS/arch/terminal 后缀（非纯 semver）、`originator` 与 UA 前缀须同族否则 `IsCodexOfficialClientByHeaders`（`pkg/openai/request.go:12`）误判。
**真实指纹/头以抓包校验为准**，自动拉取只负责版本号，头模板的非版本部分仍以人工核验的内置模板为准。

### 组件
- `VersionFetcher`（service）：后台周期任务，产出 claude-cli / claude-sdk / codex 版本。
- `IdentityRegistry`（clientidentity）：`atomic.Pointer` 持有当前 snapshot（claude + codex 两族基线），只读 getter。
- 原子一致性（硬约束）：一次更新 = 一个完整一致 snapshot；任一字段缺失则整体丢弃保留旧值/兜底，**禁止半更新**。
- 不在请求热路径联网；fetch 在后台 goroutine，带超时/重试/频率限制。

### 开关与回滚
- config `gateway.ua_auto_fetch.{enabled(默认false),interval,sources}`；admin setting 暴露生效版本/来源/最后状态。
- 关闭开关 = 回退硬编码常量（今日行为）。

---

## R2 — codex websocket 一键全开

### 「全开目标态」完整字段清单（codex-C 修正：补齐分类型 enabled）
写 `accounts.extra`，以 `service/account.go` 解析方法判定为准：

| 字段 | 目标值 | 必要性 |
|------|--------|--------|
| `openai_oauth_responses_websockets_v2_enabled` | `true` | **必须**（分类型 enabled 优先级最高，已有 false 会覆盖通用 true） |
| `openai_oauth_responses_websockets_v2_mode` | `ctx_pool` | OAuth 模式 |
| `openai_apikey_responses_websockets_v2_enabled` | `true` | **必须**（同上） |
| `openai_apikey_responses_websockets_v2_mode` | `ctx_pool` | API Key 模式 |
| `responses_websockets_v2_enabled` | `true` | 兼容总开关 |
| `openai_ws_enabled` | `true` | 历史总开关 |
| `openai_ws_allow_store_recovery` | `true` | store 恢复 |
| `openai_ws_force_http` | `false` | 不强制降级 |

- `ctx_pool`/`passthrough` 均为有效值（`account.go:1338` 归一）；`shared/dedicated` 兼容归并 ctx_pool。
- **前置校验**：全局 `gateway.openai_ws.responses_websockets_v2` 若被禁用，账号 extra 无法单独开启 → enable-all 前需检查全局开关并在响应中提示。
- OAuth / API Key 账号类型分别处理（resolver 分类型读取，见 `account.go:1419`）。

### 接口
- admin：`POST /admin/accounts/:id/ws/enable-all` + `.../ws/reset`；service `EnableAllOpenAIWS` / `ResetOpenAIWS`，幂等，仅 OpenAI 账号。

---

## R3 — 家族化 UA↔TLS 配对指纹库

### 落点（codex-D 修正：family 放上层 resolver，不进 dialer）
- `clientidentity` 解析 `account.extra.client_family` → 返回完整 snapshot `{headers, version fields, tls profile name}`。
- **outbound request builder（header/billing/identity）与 TLS profile 选择都从这个 resolver 取值**。
- `tlsfingerprint.NewDialer(profile,...)`（`dialer.go:119`）保持低层执行，**不感知 family**；family→profile 在上层（复用/对接现有 `ResolveTLSProfile(account)`，`account_usage_service.go:1165`）。
- 默认 TLS 注释为 Node.js/Claude Code 指纹（`dialer.go:55`），**不可拿它覆盖 codex family** —— codex family 必须有自己的 profile。

### 模型与默认
- 内置 ≥4 family 预设（codex-cli/desktop、claude-cli/desktop），各含 UA 模板（版本由 R1 snapshot 填充）+ 专属 `tlsfingerprint.Profile`。
- 缺省 `client_family` → 维持现状（claude 账号=现有 claude 指纹、codex 账号=现有 codex 指纹），**默认行为不变**。

### 真实指纹数据（implement 数据依赖）
- 各 family 真实 TLS 指纹（cipher/curve/ext 顺序、JA3）+ desktop UA 字面值需**抓包采集**，落为 Profile 常量/迁移，注明来源与采集日期。design 只定结构与配对契约，不编造 JA3。

---

## 跨能力数据流

```
启动 → IdentityRegistry 载入内置兜底 snapshot(claude+codex)
  └ (R1 开启) VersionFetcher 周期拉取版本 → 校验完整 → 原子 swap
请求 → resolver(account.extra.client_family) → snapshot
  ├ header 注入：snapshot.Headers (UA/X-Stainless/X-App/anthropic-beta)
  ├ billing block：snapshot.VersionFields (cc_version/fp/cch 顺序)
  ├ identity fingerprint：snapshot.Headers (Stainless 组)
  └ TLS dialer：snapshot.TLSProfileName → tlsfingerprint 执行
账号管理 → 一键全开 ws (R2) 批量写 extra(8 字段)
```

## 测试策略
- R1：版本解析（mock npm/github）、**全落点联动一致性**（UA/cc_version/identity/codex 同步）、失败回退、开关关闭=常量。
- R2：enable-all 后各解析方法返回期望值（含分类型 enabled）、已有 false 被纠正、全局禁用时拒绝、reset 回默认、幂等。
- R3：family→snapshot 配对断言（header 与 tls profile 同族）、默认（无 family）回归、可选 dialer 抓包集成测试。

## 风险与权衡
- 半更新比不更新更危险 → snapshot 原子化 + 全落点统一读取。
- 错误指纹比不改更易被识别 → 预设先少而准，以抓包为准。
- 联网在后台，绝不入请求热路径。

## 修订记录（codex 交叉验证采纳）
- A：identity 联动扩展到 billing block / identity_service / codex headers，统一 snapshot 驱动。
- B：数据源修正（codex_cli_rs 真实族、sdk 版本优先从 claude-code 包解析、UA 后缀/ originator 同族）。
- C：ws 全开补齐两个分类型 enabled 字段 + 全局开关前置校验。
- D：family resolver 上移，tlsfingerprint 保持低层。

**Inspecting tools and resources**

I need to keep things moving to ensure we're on the right track. It seems like another agent may have already worked on this, so I should inspect the repo carefully. I need to follow the instructions for Trellis and understand the requirements of GitNexus before making any edits. However, I don't see the necessary tools listed. Maybe I could use the CLI, or if that's not available, I'll need to report that. Lastly, I should check the spec index and other relevant files to gather more information.
exec
/bin/bash -lc "sed -n '1,220p' .trellis/spec/backend/index.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
# Backend Development Guidelines Index

> **Tech Stack**: Next.js 15 API Routes + oRPC + Drizzle ORM + PostgreSQL

## Related Guidelines

| Guideline                 | Location     | When to Read                 |
| ------------------------- | ------------ | ---------------------------- |
| **Shared Code Standards** | `../shared/` | Always - applies to all code |

---

## Documentation Files

| File                                                 | Description                                        | When to Read                       |
| ---------------------------------------------------- | -------------------------------------------------- | ---------------------------------- |
| [directory-structure.md](./directory-structure.md)   | Module organization and directory layout           | Starting a new feature             |
| [orpc-usage.md](./orpc-usage.md)                     | oRPC router, procedures, middleware patterns       | Creating/modifying API endpoints   |
| [type-safety.md](./type-safety.md)                   | Zod schemas, type narrowing, response patterns     | Type-related decisions             |
| [database.md](./database.md)                         | Drizzle ORM, queries, transactions, SQL patterns   | Database operations                |
| [authentication.md](./authentication.md)             | better-auth, sessions, OAuth, protected procedures | Auth-related features              |
| [logging.md](./logging.md)                           | Structured logging, Sentry tracing, telemetry      | Debugging, observability           |
| [performance.md](./performance.md)                   | Concurrency, caching, batch processing, streaming  | Performance optimization           |
| [ai-sdk-integration.md](./ai-sdk-integration.md)     | Vercel AI SDK, tool calling, prompt patterns       | AI-powered features                |
| [quality.md](./quality.md)                           | Pre-commit checklist for backend code              | Before committing                  |

---

## Quick Navigation

### Service Module Structure

| Task                          | File                                               |
| ----------------------------- | -------------------------------------------------- |
| Project structure             | [directory-structure.md](./directory-structure.md) |
| Domain module pattern         | [directory-structure.md](./directory-structure.md) |
| Write types.ts                | [directory-structure.md](./directory-structure.md) |
| Write procedure               | [directory-structure.md](./directory-structure.md) |
| Write lib/ helpers            | [directory-structure.md](./directory-structure.md) |
| Router setup                  | [orpc-usage.md](./orpc-usage.md)                   |
| Middleware composition        | [orpc-usage.md](./orpc-usage.md)                   |
| Naming conventions            | [directory-structure.md](./directory-structure.md) |

### Type Safety

| Task                 | File                               |
| -------------------- | ---------------------------------- |
| Type safety patterns | [type-safety.md](./type-safety.md) |
| Discriminated unions | [type-safety.md](./type-safety.md) |
| Zod-first types      | [type-safety.md](./type-safety.md) |
| Zod error handling   | [type-safety.md](./type-safety.md) |
| Standard response    | [type-safety.md](./type-safety.md) |

### Database (Drizzle + PostgreSQL)

| Task                    | File                         |
| ----------------------- | ---------------------------- |
| Query organization      | [database.md](./database.md) |
| Batch queries (inArray) | [database.md](./database.md) |
| Conflict handling       | [database.md](./database.md) |
| Transactions            | [database.md](./database.md) |
| JSON column operations  | [database.md](./database.md) |
| Raw SQL camelCase       | [database.md](./database.md) |
| Enum comparison         | [database.md](./database.md) |

### Error Handling / Logging

| Task                        | File                           |
| --------------------------- | ------------------------------ |
| Structured logging          | [logging.md](./logging.md)     |
| Sentry span tracing         | [logging.md](./logging.md)     |
| Error capture               | [logging.md](./logging.md)     |
| oRPC error codes            | [orpc-usage.md](./orpc-usage.md) |
| Batch operation logging     | [logging.md](./logging.md)     |

### Performance

| Task                          | File                               |
| ----------------------------- | ---------------------------------- |
| Parallel execution            | [performance.md](./performance.md) |
| Concurrency control (p-limit) | [performance.md](./performance.md) |
| Exponential backoff retry     | [performance.md](./performance.md) |
| Redis caching                 | [performance.md](./performance.md) |
| Distributed locks             | [performance.md](./performance.md) |
| Chunked batch processing      | [performance.md](./performance.md) |
| Streaming large datasets      | [performance.md](./performance.md) |

### Authentication

| Task                        | File                                   |
| --------------------------- | -------------------------------------- |
| Protected procedures        | [authentication.md](./authentication.md) |
| Admin procedures            | [authentication.md](./authentication.md) |
| Session caching (Redis)     | [authentication.md](./authentication.md) |
| OAuth integration           | [authentication.md](./authentication.md) |
| Role-based access control   | [authentication.md](./authentication.md) |
| Client-side auth            | [authentication.md](./authentication.md) |

### AI Integration

| Task                        | File                                           |
| --------------------------- | ---------------------------------------------- |
| generateText / generateObject | [ai-sdk-integration.md](./ai-sdk-integration.md) |
| Streaming (streamText)      | [ai-sdk-integration.md](./ai-sdk-integration.md) |
| Tool calling                | [ai-sdk-integration.md](./ai-sdk-integration.md) |
| Telemetry configuration     | [ai-sdk-integration.md](./ai-sdk-integration.md) |
| Prompt engineering          | [ai-sdk-integration.md](./ai-sdk-integration.md) |
| AI error handling           | [ai-sdk-integration.md](./ai-sdk-integration.md) |

---

## Core Rules Summary

| Rule                                                             | Reference                                          |
| ---------------------------------------------------------------- | -------------------------------------------------- |
| **No `await` in loops** - use `inArray` for batch queries        | [database.md](./database.md)                       |
| **No `console.log`** - use structured logger                     | [logging.md](./logging.md)                         |
| **No non-null assertions `!`** - use type narrowing              | [type-safety.md](./type-safety.md)                 |
| **All API inputs/outputs have Zod schemas**                      | [type-safety.md](./type-safety.md)                 |
| **Import enums from utils** - not from database package          | [type-safety.md](./type-safety.md)                 |
| **Standard response format** - always include `success`/`reason` | [type-safety.md](./type-safety.md)                 |
| **Use `protectedProcedure`** for authenticated endpoints         | [authentication.md](./authentication.md)           |
| **One procedure per file** - keep procedures focused             | [orpc-usage.md](./orpc-usage.md)                   |
| **Service modules follow domain layout**                         | [directory-structure.md](./directory-structure.md) |
| **Use `Promise.all`** for independent parallel operations        | [performance.md](./performance.md)                 |
| **Use `p-limit`** for external API concurrency control           | [performance.md](./performance.md)                 |
| **Always enable AI telemetry** for token tracking                | [ai-sdk-integration.md](./ai-sdk-integration.md)   |
| **Cast `::jsonb`** for PostgreSQL JSON operations                | [database.md](./database.md)                       |
| **Double-quote camelCase** column names in raw SQL               | [database.md](./database.md)                       |
| **Use structured context** in logs - no string interpolation     | [logging.md](./logging.md)                         |
| **Run pre-commit checklist** before committing                   | [quality.md](./quality.md)                         |

---

## Reference Files

| Feature              | Typical Location                        |
| -------------------- | --------------------------------------- |
| Drizzle Client       | `packages/database/drizzle/client.ts`   |
| Schema Definition    | `packages/database/drizzle/schema/`     |
| Database Queries     | `packages/database/drizzle/queries/`    |
| oRPC Router          | `packages/api/orpc/router.ts`           |
| Base Procedures      | `packages/api/orpc/procedures.ts`       |
| Middleware           | `packages/api/orpc/middleware/`         |
| Service Module       | `packages/api/modules/{domain}/`        |
| Module Types (Zod)   | `packages/api/modules/{domain}/types.ts`|
| Auth Configuration   | `packages/auth/auth.ts`                 |
| Auth Client          | `packages/auth/client.ts`               |
| Shared Utils/Enums   | `packages/utils/`                       |

---

**Language**: All documentation must be written in **English**.

exec
/bin/bash -lc "sed -n '1,220p' .trellis/spec/shared/index.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
# Shared Development Guidelines

> These guidelines apply to all Next.js full-stack applications using this architecture.

---

## Documentation Files

| File                                   | Description                          | When to Read            |
| -------------------------------------- | ------------------------------------ | ----------------------- |
| [code-quality.md](./code-quality.md)   | Code quality mandatory rules         | Always                  |
| [typescript.md](./typescript.md)       | TypeScript best practices            | Type-related decisions  |
| [dependencies.md](./dependencies.md)   | Dependency versions and constraints  | Adding/updating deps    |

---

## Quick Navigation

| Task                        | File                                   |
| --------------------------- | -------------------------------------- |
| Code quality rules          | [code-quality.md](./code-quality.md)   |
| Type annotations            | [typescript.md](./typescript.md)       |
| Dependency management       | [dependencies.md](./dependencies.md)   |

---

## Core Rules (MANDATORY)

| Rule                                      | File                                   |
| ----------------------------------------- | -------------------------------------- |
| No non-null assertions (`!`)              | [code-quality.md](./code-quality.md)   |
| No `any` type                             | [code-quality.md](./code-quality.md)   |
| No `@ts-expect-error` / `@ts-ignore`      | [code-quality.md](./code-quality.md)   |
| No `console.log` (use structured logging) | [code-quality.md](./code-quality.md)   |
| Zod-first type definitions                | [typescript.md](./typescript.md)       |
| Import types from backend, never redefine | [typescript.md](./typescript.md)       |
| Standard response format (`success` + `reason`) | [typescript.md](./typescript.md) |

---

## Before Every Commit

- [ ] `pnpm lint` - 0 errors
- [ ] `pnpm type-check` - 0 errors
- [ ] `pnpm build` - production build succeeds
- [ ] No `any` types in new code
- [ ] No non-null assertions (`!`)
- [ ] No `@ts-expect-error` or `@ts-ignore` comments
- [ ] No `console.log` statements (use `logger`)
- [ ] Tests pass (if applicable)

---

## Code Review Checklist

- [ ] Types are explicit, not `any`
- [ ] API inputs/outputs have Zod schemas
- [ ] Error handling returns structured responses
- [ ] No duplicate type definitions (import from source of truth)
- [ ] Naming follows conventions (files: kebab-case, components: PascalCase)
- [ ] Unused imports and dead code removed
- [ ] No swallowed errors (silent `catch` blocks)

---

**Language**: All documentation must be written in **English**.

exec
/bin/bash -lc "sed -n '1,220p' .trellis/spec/guides/index.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
# Thinking Guides for Next.js Full-Stack Projects

> **Purpose**: Systematic thinking guides to catch issues before they become bugs.
>
> **Core Philosophy**: 30 minutes of thinking saves 3 hours of debugging.

---

## Why Thinking Guides?

**Most bugs and tech debt come from "didn't think of that"**, not from lack of skill:

- Didn't think about what happens at layer boundaries -> cross-layer bugs
- Didn't think about code patterns repeating -> duplicated code everywhere
- Didn't think about edge cases -> runtime errors
- Didn't think about future maintainers -> unreadable code

These guides help you **ask the right questions before coding**.

---

## Available Thinking Guides

| Guide                                                             | Purpose                                       | When to Use                                      |
| ----------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------ |
| [Cross-Layer Thinking](./cross-layer-thinking-guide.md)           | Think through data flow across layers         | Before implementing features that span 3+ layers |
| [Pre-Implementation Checklist](./pre-implementation-checklist.md) | Verify readiness before coding                | Before starting any feature implementation       |

---

## Quick Reference: When to Use Which Guide

### Cross-Layer Issues

Use [Cross-Layer Thinking Guide](./cross-layer-thinking-guide.md) when:

- [ ] Feature touches 3+ layers (Server Component, Client Component, oRPC, Database)
- [ ] Data format changes between layers
- [ ] Multiple consumers need the same data
- [ ] You're not sure where to put some logic
- [ ] Integrates with external services or third-party APIs

### Before Writing Code

Use [Pre-Implementation Checklist](./pre-implementation-checklist.md) when:

- [ ] About to add a constant or config value
- [ ] About to implement new logic
- [ ] About to define a type or Zod schema
- [ ] About to create a component or hook
- [ ] About to add an oRPC procedure
- [ ] Feels like you've seen similar code before

---

## The Pre-Modification Rule (CRITICAL)

> **Before changing ANY value, ALWAYS search first!**

```bash
# Search for the value you're about to change
rg "value_to_change" --type ts

# Check how many files define this value
rg "CONFIG_NAME" --type ts -c
```

This single habit prevents most "forgot to update X" bugs.

---

## Next.js-Specific Layers

In Next.js full-stack projects with oRPC and Drizzle, these are the typical layers:

```
Server Components (RSC - data fetching, static rendering)
        |
        v
Client Components ('use client' - interactivity, React Query)
        |
        v
API Routes / oRPC Router (type-safe RPC, middleware, validation)
        |
        v
Service / Business Logic (shared utilities, domain rules)
        |
        v
Database Layer (Drizzle ORM, PostgreSQL, migrations)
```

Each boundary is a potential source of bugs due to:

- **Serialization** - Only serializable data crosses the RSC/Client boundary (no functions, no Date objects, no Maps)
- **Type mismatches** - Zod schemas on oRPC may not match what the frontend expects
- **Auth context** - Session availability differs between Server Components, API routes, and middleware
- **Rendering mode** - Server Components vs Client Components have different capabilities and constraints
- **Async timing** - React Query caching, stale data, and race conditions

---

## Core Principles

1. **Search Before Write** - Always search for existing patterns before creating new ones
2. **Think Before Code** - 5 minutes of checklist saves 50 minutes of debugging
3. **Document Assumptions** - Make implicit assumptions explicit
4. **Verify All Layers** - Changes often need updates in multiple places
5. **Learn From Bugs** - Add lessons to these guides after fixing non-trivial bugs

---

## Contributing

Found a new "didn't think of that" moment? Add it:

1. If it's a **general thinking pattern** -> Add to existing guide or create new one
2. If it caused a bug -> Add to "Lessons Learned" section in the relevant guide
3. If it's **project-specific** -> Create a separate project-specific guide

---

**Language**: All documentation should be written in **English**.

exec
/bin/bash -lc 'git status --short --branch' in /mnt/x/project/sub2api
 succeeded in 7351ms:
## feat/proxy-realism
 M .dockerignore
 M .gitattributes
 M .gitignore
 M CLA.md
 M DEV_GUIDE.md
 M Dockerfile.goreleaser
 M LICENSE
 M Makefile
 M README.md
 M README_JA.md
 M deploy/.env.example
 M deploy/.gitignore
 M deploy/Caddyfile
 M deploy/DATAMANAGEMENTD_CN.md
 M deploy/DOCKER.md
 M deploy/Makefile
 M deploy/README.md
 M deploy/codex-instructions.md.tmpl
 M deploy/sub2api-datamanagementd.service
 M deploy/sub2api.service
 M docs/ADMIN_PAYMENT_INTEGRATION_API.md
 M docs/PAYMENT.md
 M docs/PAYMENT_CN.md
 M docs/legal/admin-compliance.en.md
 M docs/legal/admin-compliance.zh.md
 M frontend/.eslintignore
 M frontend/.eslintrc.cjs
 M frontend/.npmrc
 M frontend/audit.json
 M frontend/index.html
 M frontend/package.json
 M frontend/src/assets/icons/airwallex.svg
 M frontend/src/components/common/README.md
 M frontend/src/components/layout/EXAMPLES.md
 M frontend/src/components/layout/INTEGRATION.md
 M frontend/src/components/layout/README.md
 M frontend/src/router/README.md
 M frontend/src/stores/README.md
 M frontend/src/style.css
 M frontend/src/styles/onboarding.css
 M frontend/src/views/auth/README.md
 M frontend/src/views/auth/USAGE_EXAMPLES.md
 M frontend/src/views/auth/VISUAL_GUIDE.md
 M frontend/tsconfig.json
 M frontend/tsconfig.node.json
 M skills/sub2api-admin/SKILL.md
 M skills/sub2api-admin/references/admin-cli.md
?? .agents/
?? .cursor/
?? .trellis/

codex
当前工作树已有大量非本次改动，且主要在根目录和前端；我会只碰后端本任务文件，不回滚这些已有变更。下一步会先确认现有 `clientidentity`、config 和 wire 结构，再做最小改动。
exec
/bin/bash -lc "sed -n '1,220p' .trellis/spec/backend/directory-structure.md" in /mnt/x/project/sub2api
 succeeded in 0ms:
# Directory Structure

This document describes the module organization pattern for backend API development.

## Module Structure

Each API module follows a consistent directory structure:

```
packages/api/modules/[module]/
├── types.ts          # Zod schemas and TypeScript types
├── router.ts         # Hono router with route definitions
├── lib/              # Core business logic (shared across procedures)
│   ├── client.ts     # External service clients
│   └── helpers.ts    # Helper functions
├── procedures/       # HTTP endpoint handlers
│   ├── create.ts
│   ├── update.ts
│   ├── delete.ts
│   └── list.ts
└── api/              # API documentation (optional)
    ├── create.md
    └── list.md
```

## File Responsibilities

### `types.ts` - Schemas and Types

Define all Zod schemas and TypeScript types for the module.

```typescript
// types.ts
import { z } from "zod";

// Input Schemas
export const createOrderInputSchema = z.object({
  customerId: z.string(),
  items: z.array(z.object({
    productId: z.string(),
    quantity: z.number().min(1),
  })).min(1),
});

// Output Schemas
export const orderResponseSchema = z.object({
  success: z.boolean(),
  reason: z.string(),
  order: z.object({
    id: z.string(),
    status: z.string(),
    total: z.number(),
  }).optional(),
});

// Type exports
export type CreateOrderInput = z.infer<typeof createOrderInputSchema>;
export type OrderResponse = z.infer<typeof orderResponseSchema>;
```

### `router.ts` - Route Definitions

The router aggregates all procedures and defines the API routes.

```typescript
// router.ts
import { Hono } from "hono";
import { createOrder } from "./procedures/create";
import { listOrders } from "./procedures/list";
import { updateOrderStatus } from "./procedures/update";

export const ordersRouter = new Hono()
  .basePath("/orders")
  .post("/", createOrder)
  .get("/", listOrders)
  .patch("/:id/status", updateOrderStatus);
```

### `lib/` - Business Logic

Contains reusable business logic shared across procedures.

```
lib/
├── client.ts       # External API clients (payment gateway, etc.)
├── helpers.ts      # Pure helper functions
├── validators.ts   # Business rule validators
└── transformers.ts # Data transformation utilities
```

**Example: `lib/helpers.ts`**

```typescript
// lib/helpers.ts
import type { Order } from "../types";

/**
 * Calculate order total with tax
 */
export function calculateOrderTotal(
  items: Array<{ price: number; quantity: number }>,
  taxRate: number = 0.1,
): number {
  const subtotal = items.reduce((sum, item) => sum + item.price * item.quantity, 0);
  return subtotal * (1 + taxRate);
}

/**
 * Generate order reference number
 */
export function generateOrderReference(timestamp: Date): string {
  const year = timestamp.getFullYear();
  const month = String(timestamp.getMonth() + 1).padStart(2, "0");
  const random = Math.random().toString(36).substring(2, 8).toUpperCase();
  return `ORD-${year}${month}-${random}`;
}
```

### `procedures/` - Endpoint Handlers

Each procedure handles a single API endpoint with clear responsibilities.

```typescript
// procedures/create.ts
import { db } from "@your-app/database";
import { order as orderTable } from "@your-app/database/drizzle/schema/postgres";
import { logger } from "@your-app/logs";
import { protectedProcedure } from "../../../orpc/procedures";
import { calculateOrderTotal, generateOrderReference } from "../lib/helpers";
import { createOrderInputSchema, orderResponseSchema } from "../types";

export const createOrder = protectedProcedure
  .route({
    method: "POST",
    path: "/orders",
    tags: ["Orders"],
    summary: "Create a new order",
  })
  .input(createOrderInputSchema)
  .output(orderResponseSchema)
  .handler(async ({ input, context: { user } }) => {
    const { customerId, items } = input;

    // Business logic
    const total = calculateOrderTotal(items);
    const reference = generateOrderReference(new Date());

    // Database operation
    const [newOrder] = await db
      .insert(orderTable)
      .values({
        userId: user.id,
        customerId,
        reference,
        total,
        status: "PENDING",
      })
      .returning();

    logger.info("Order created", {
      orderId: newOrder.id,
      userId: user.id,
      total,
    });

    return {
      success: true,
      reason: "Order created successfully",
      order: {
        id: newOrder.id,
        status: newOrder.status,
        total: newOrder.total,
      },
    };
  });
```

### `api/` - Documentation (Optional)

Markdown documentation for each endpoint, useful for complex APIs.

```markdown
<!-- api/create.md -->
# Create Order

## Schema

### Input
- `customerId`: string - Customer identifier
- `items`: array - Order items
  - `productId`: string - Product identifier
  - `quantity`: number - Quantity (min: 1)

### Output
- `success`: boolean
- `reason`: string
- `order`: object (optional)

## Logic

1. Validate user has permission to create orders for the customer
2. Verify all products exist and are in stock
3. Calculate total with applicable discounts
4. Create order record
5. Reserve inventory
6. Return order details

## Usage Example

```typescript
const result = await api.orders.create({
  customerId: "cust_123",
  items: [
    { productId: "prod_456", quantity: 2 },
  ],
});
```
```

## Naming Conventions

exec
/bin/bash -lc "sed -n '1,220p' .trellis/spec/backend/quality.md" in /mnt/x/project/sub2api
exec
/bin/bash -lc "sed -n '1,220p' .trellis/spec/shared/code-quality.md" in /mnt/x/project/sub2api
exec
/bin/bash -lc "sed -n '1,220p' .trellis/spec/guides/pre-implementation-checklist.md" in /mnt/x/project/sub2api
 succeeded in 123ms:
# Pre-commit Checklist

Run through this checklist before committing backend code.

## Type Safety

- [ ] **No non-null assertions (`!`)** - Use local variables and conditionals for type narrowing
- [ ] **All API inputs have Zod schemas** - Defined in `types.ts`
- [ ] **All API outputs have Zod schemas** - Including `success` and `reason` fields
- [ ] **Enums imported from `@your-app/utils`** - Not from database package

## Database Operations

- [ ] **No `await` in loops** - Use `inArray` for batch queries
- [ ] **Batch inserts used** - Not individual inserts in loops
- [ ] **Conflict handling considered** - Use `onConflictDoUpdate` when appropriate
- [ ] **JSON columns cast properly** - `::jsonb` for jsonb functions
- [ ] **Raw SQL column names quoted** - Double quotes for camelCase columns

## Logging

- [ ] **No `console.log`** - Use `logger` from `@your-app/logs`
- [ ] **Structured logging used** - Pass objects, not string interpolation
- [ ] **Errors logged with context** - Include relevant IDs and stack traces
- [ ] **Sensitive data excluded** - No passwords, tokens, or PII in logs

## Performance

- [ ] **Parallel execution where possible** - Use `Promise.all` for independent operations
- [ ] **Concurrency control for external APIs** - Use `p-limit` for rate-limited APIs
- [ ] **Retry logic for rate limits** - Exponential backoff implemented
- [ ] **Caching considered** - For expensive or frequently accessed data

## Error Handling

- [ ] **Errors properly caught and logged** - With Sentry context when applicable
- [ ] **Appropriate error codes returned** - `NOT_FOUND`, `FORBIDDEN`, `BAD_REQUEST`, etc.
- [ ] **Batch operations handle partial failures** - Return detailed error information

## Code Organization

- [ ] **Code in correct location** - Procedures, lib, types in right directories
- [ ] **Reusable logic extracted** - Shared code in `lib/` directory
- [ ] **Naming conventions followed** - Schemas, types, functions named correctly

## Quick Reference

### Response Format
```typescript
return {
  success: true,
  reason: "Operation completed successfully",
  // additional fields
};
```

### Batch Query Pattern
```typescript
const items = await db
  .select()
  .from(itemTable)
  .where(inArray(itemTable.parentId, parentIds));

const itemsByParent = groupBy(items, "parentId");
```

### Logging Pattern
```typescript
logger.info("Operation completed", {
  operationId,
  userId,
  itemCount: items.length,
});
```

### Error Pattern
```typescript
if (!resource) {
  throw new ORPCError("NOT_FOUND", { message: "Resource not found" });
}
```

 succeeded in 124ms:
# Code Quality Guidelines

> Mandatory code quality rules for all Next.js full-stack applications.

---

## No Non-Null Assertions

**NEVER** use non-null assertions (`!`). They bypass TypeScript's null checking and lead to runtime errors.

```typescript
// FORBIDDEN
const name = user!.name;
const value = data!.items![0]!;

// REQUIRED - Use explicit checks
const user = getUser();
if (!user) {
  throw new Error('User not found');
}
const name = user.name;

// REQUIRED - Use optional chaining with fallback
const value = data?.items?.[0] ?? defaultValue;

// REQUIRED - Use local variable after null check
const project = getProject(id);
if (!project) {
  return { success: false, reason: 'Project not found' };
}
const projectName = project.name;
```

---

## No `any` Type

```typescript
// BAD
function process(data: any) { ... }

// GOOD - Use proper types
function process(data: ProcessInput) { ... }

// GOOD - Use unknown for truly unknown data
function parseJSON(input: string): unknown {
  return JSON.parse(input);
}

// BAD - any in cache updates
queryClient.setQueryData(['users'], (old: any) => ...);

// GOOD - Properly typed cache updates
queryClient.setQueryData<UserListData>(['users'], (old) => {
  if (!old) return old;
  return { ...old, items: old.items.filter((u) => u.id !== deletedId) };
});
```

---

## No `@ts-expect-error` / `@ts-ignore`

```typescript
// FORBIDDEN
// @ts-expect-error - field exists at runtime
const value = user.customField;

// @ts-ignore
doSomething(invalidArg);

// REQUIRED - Fix the type issue at the source
// If a field exists at runtime but not in types, update the type definition.
doSomething(validArg);
```

---

## No `console.log`

Use structured logging instead of `console.log`. This applies to both frontend and backend code.

```typescript
// BAD
console.log('User created:', userId);
console.log('Error:', error);

// GOOD - Backend: use structured logger
logger.info('user_created', { userId });
logger.error('operation_failed', { error, operationId });

// GOOD - Frontend: remove debug logs before commit
// Use browser dev tools for debugging, not console.log
```

**Exception**: `console.warn` and `console.error` are acceptable in frontend code for development-time warnings that will not appear in production.

---

## Import Ordering

Organize imports in this order, separated by blank lines:

```typescript
// 1. Node built-ins
import path from 'node:path';

// 2. External packages
import { z } from 'zod';
import { useQuery } from '@tanstack/react-query';

// 3. Internal workspace packages
import type { User } from '@your-app/api/modules/users/types';

// 4. Local imports (relative paths)
import { formatDate } from './utils';
import type { Props } from './types';
```

Always use `import type` for type-only imports:

```typescript
// GOOD
import type { User, Project } from './types';
import { createUser } from './procedures';

// BAD - Mixed imports without type annotation
import { User, createUser } from './types';
```

---

## Naming Conventions

### Files and Directories

| Type            | Convention                  | Example                     |
| --------------- | --------------------------- | --------------------------- |
| React Component | PascalCase                  | `UserProfile.tsx`           |
| Hook            | camelCase with `use` prefix | `useProject.ts`             |
| Utility         | kebab-case                  | `date-utils.ts`             |
| Type file       | kebab-case or `types.ts`    | `types.ts`, `user-types.ts` |
| Test file       | Same name + `.test`         | `date-utils.test.ts`        |
| Directory       | kebab-case                  | `user-profile/`             |

### Variables and Functions

| Type           | Convention                                  | Example                            |
| -------------- | ------------------------------------------- | ---------------------------------- |
| Variable       | camelCase                                   | `userName`, `isActive`             |
| Constant       | SCREAMING_SNAKE_CASE                        | `MAX_RETRY_COUNT`                  |
| Function       | camelCase                                   | `getUserById`                      |
| Class          | PascalCase                                  | `UserService`                      |
| Type/Interface | PascalCase                                  | `UserInput`, `ProjectOutput`       |
| Enum           | PascalCase (type), SCREAMING_SNAKE (values) | `enum Status { ACTIVE, INACTIVE }` |

### Boolean Variables

Use `is`, `has`, `should`, `can` prefixes:

```typescript
// GOOD
const isLoading = true;
const hasPermission = user.role === 'admin';
const shouldRefresh = Date.now() > expiresAt;
const canEdit = isOwner || hasPermission;

// BAD
const loading = true;
const permission = user.role === 'admin';
```

---

## Error Handling

### Never Swallow Errors

```typescript
// BAD - Silent failure
try {
  await dangerousOperation();
} catch (e) {
  // nothing
}

// GOOD - Log and handle
try {
  await dangerousOperation();
} catch (error) {
  logger.error('operation_failed', { error });
  throw new AppError('Operation failed', 'OPERATION_FAILED');
}
```

### Consistent Error Response Format

All API responses must use the standard `success` + `reason` format:

```typescript
// Success
return {
  success: true,
  reason: 'Operation completed successfully',
  data: result,
};

// Error
return {
  success: false,
  reason: 'Insufficient permissions to perform this action',
};
```

---

## Dead Code Elimination

- Remove unused imports (Biome enforces this automatically)
- Remove commented-out code blocks

 succeeded in 127ms:
# Pre-Implementation Checklist

> **Purpose**: Ask the right questions **before** writing code to avoid common architectural mistakes.

---

## Why This Checklist?

Most code quality issues aren't caught during implementation--they're **designed in** from the start:

| Problem                                | Root Cause                                    | Cost                       |
| -------------------------------------- | --------------------------------------------- | -------------------------- |
| Constants duplicated across 5 files    | Didn't ask "will this be used elsewhere?"     | Refactoring + testing      |
| Same logic repeated in multiple hooks  | Didn't ask "does this pattern exist?"         | Creating abstraction later |
| Cross-layer type mismatches            | Didn't ask "who else consumes this?"          | Debugging + fixing         |
| Zod schema redefined in frontend       | Didn't ask "is this type already exported?"   | Inconsistent validation    |
| oRPC procedure duplicates existing one | Didn't ask "does a similar endpoint exist?"   | API surface bloat          |

**This checklist catches these issues before they become code.**

---

## The Checklist

### 1. Constants & Configuration

Before adding any constant or config value:

- [ ] **Cross-package usage?** Will this value be used in both frontend app and API package?
  - If yes -> Put in a shared package (e.g., `@your-app/utils` or `@your-app/config`)
  - Example: `MAX_UPLOAD_SIZE` used by both file upload UI and oRPC validation

- [ ] **Multiple consumers?** Will this value be used in 2+ files within the same package?
  - If yes -> Put in a shared constants file for that package
  - Example: Don't define `DEBOUNCE_MS = 300` in each hook file

- [ ] **Magic number?** Is this a hardcoded value that could change?
  - If yes -> Extract to named constant with comment explaining why
  - Example: `PAGINATION_LIMIT: 50  // oRPC default page size`

- [ ] **Environment-dependent?** Does this differ between dev/staging/production?
  - If yes -> Use environment variables with proper validation
  - Example: API URLs, feature flags, third-party API keys

### 2. Logic & Patterns

Before implementing any logic:

- [ ] **Pattern exists?** Search for similar patterns in the codebase first

  ```bash
  # Example: Before implementing debounced search
  rg "debounce" src/ packages/
  rg "useDebounce" src/ packages/
  ```

- [ ] **Will repeat?** Will this exact logic be needed in 2+ places?
  - If yes -> Create a shared hook/utility **first**, then use it
  - Example: `useDebounce` instead of repeating debounce logic in 5 hooks

- [ ] **React Query pattern?** Is there an existing query/mutation hook for this data?
  - Search before creating: `rg "orpc.items" src/`
  - Check if you can extend an existing hook rather than creating a new one

- [ ] **Server or client?** Does this logic need interactivity?
  - If no -> Keep it in a Server Component (default)
  - If yes -> Extract only the interactive part into a Client Component

### 3. Types & Schemas

Before defining types:

- [ ] **Zod schema exists?** Is there already a Zod schema for this data shape?
  - Check the API module's `types.ts`: `rg "Schema = z.object" packages/api/`
  - Derive TypeScript types from Zod schemas with `z.infer<typeof schema>`
  - Never manually define a TypeScript interface that duplicates a Zod schema

- [ ] **Existing type?** Does a similar type already exist?
  - Search before creating: `rg "interface.*YourTypeName\|type.*YourTypeName" src/ packages/`

- [ ] **Cross-layer type?** Is this type used across the oRPC boundary?
  - If yes -> Define the Zod schema in the API module's `types.ts`, export the inferred type
  - Frontend should import types from the API package, not redefine them

- [ ] **Derived from client?** Can you infer the type from the oRPC client?
  ```typescript
  // Prefer this over manually defining types
  type ItemResult = Awaited<ReturnType<(typeof orpcClient)["items"]["get"]>>;
  ```

### 4. UI Components

Before creating UI components:

- [ ] **Server or Client Component?** Does this component need:
  - Event handlers (onClick, onChange)? -> `'use client'`
  - React hooks (useState, useEffect)? -> `'use client'`
  - Browser APIs (window, document)? -> `'use client'`
  - None of the above? -> Keep as Server Component (default)

- [ ] **Similar component exists?** Search before creating
  - `rg "function.*YourComponent\|export.*YourComponent" src/`

- [ ] **Visual-logic consistency?** If there's already a visual distinction (icon, color, label) for a concept, does your logic match?

- [ ] **State lifecycle?** Will this component unmount during normal user flow?
  - If yes -> Consider where state should persist (URL params with nuqs, parent, context)

### 5. API Routes & oRPC Procedures

Before writing an API route or oRPC procedure:

- [ ] **Existing procedure?** Does a similar oRPC procedure already exist?
  - Check the module's `router.ts`: `rg "Router = {" packages/api/`
  - Can you extend an existing procedure rather than creating a new one?

- [ ] **Correct HTTP method?**
  - GET for read operations (queries)
  - POST for create operations (mutations)
  - PUT/PATCH for update operations
  - DELETE for removal operations

- [ ] **Authentication level?** Which base procedure to use?
  - Public data -> `publicProcedure`
  - User-specific data -> `protectedProcedure`
  - Admin operations -> `adminProcedure`

- [ ] **Input/output schemas defined?** Both should be Zod schemas in `types.ts`

### 6. Dependencies

Before adding a dependency:

- [ ] **Already installed?** Check `package.json` across all packages
  ```bash
  rg "\"dependency-name\"" package.json packages/*/package.json
  ```

- [ ] **Built-in alternative?** Can you use a native API or existing utility instead?
  - Example: `structuredClone()` instead of `lodash.cloneDeep`

- [ ] **Bundle impact?** Will this significantly increase the client bundle?
  - If yes -> Consider dynamic import or server-only usage

---

## Quick Decision Tree

```
Adding a value/constant?
|-- Used in both app AND api package? -> shared package (@your-app/utils)
|-- Used in 2+ files within same package? -> shared constants file
+-- Single file only? -> Local constant is fine

Adding logic/behavior?
|-- Similar pattern exists? -> Extend or reuse existing
|-- Will be used in 2+ places? -> Create shared hook/utility first
+-- Single use only? -> Implement directly (but document pattern)

Adding a type?
|-- Zod schema exists? -> Use z.infer<typeof schema>
|-- Crosses oRPC boundary? -> Define in API types.ts, import elsewhere
|-- Can derive from client? -> Use Awaited<ReturnType<...>>
+-- Local only? -> Define locally

Adding a component?
|-- Needs interactivity? -> 'use client'
|-- Pure display? -> Server Component (default)
+-- Mix of both? -> Split into Server wrapper + Client interactive part
```

---

## What to Verify Across Layers

When implementing a feature that spans Server Component -> API -> Database, verify:

| Layer            | Check                                                              |
| ---------------- | ------------------------------------------------------------------ |
| Server Component | Data fetched correctly? Props serializable? No client-only APIs?   |
| Client Component | Loading/error states handled? React Query cache invalidated?       |
| oRPC Procedure   | Input validated? Auth checked? Output schema matches?              |
| Database Query   | No N+1 queries? Proper indexes? Transactions where needed?         |
| Zod Schemas      | Input and output schemas consistent? Date/null handling correct?   |

---

## Anti-Patterns to Avoid

### Redefining Backend Types

```typescript
// DON'T: Manually define types that mirror Zod schemas
interface Item {
  id: string;
  name: string;
  createdAt: Date;
}

// DO: Import or infer from the source of truth
import type { Item } from "@your-app/api/modules/items/types";
// or
type Item = Awaited<ReturnType<(typeof orpcClient)["items"]["get"]>>["item"];
```

### Manual Query Keys

```typescript
// DON'T: Manually construct query keys
queryClient.invalidateQueries({ queryKey: ["items", "list"] });

// DO: Use oRPC generated keys
queryClient.invalidateQueries({ queryKey: orpc.items.list.key() });
```

### Unnecessary Client Components

```typescript
// DON'T: Mark everything as 'use client'
'use client';

codex
我需要满足项目里的 GitNexus 规则：改函数或结构前先做影响分析。现在工具列表里没有直接暴露 GitNexus MCP，我先查可用工具；如果没有，只能记录该项无法执行，并用本地检索补足风险判断。
codex
GitNexus 工具未暴露，无法执行 `impact/detect_changes`。我会继续用本地代码搜索确认调用面；这不是等价替代，但能避免盲改。
exec
/bin/bash -lc "rg -n \"ProviderSet|wire.Build|initializeApplication|Start\\(\\)\" backend/internal/service/wire.go backend/cmd/server/wire.go backend/cmd/server -g'*.go'" in /mnt/x/project/sub2api
 succeeded in 0ms:
backend/cmd/server/wire.go:31:func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
backend/cmd/server/wire.go:32:	wire.Build(
backend/cmd/server/wire.go:33:		// Infrastructure layer ProviderSets
backend/cmd/server/wire.go:34:		config.ProviderSet,
backend/cmd/server/wire.go:36:		// Business layer ProviderSets
backend/cmd/server/wire.go:37:		repository.ProviderSet,
backend/cmd/server/wire.go:38:		service.ProviderSet,
backend/cmd/server/wire.go:39:		payment.ProviderSet,
backend/cmd/server/wire.go:40:		middleware.ProviderSet,
backend/cmd/server/wire.go:41:		handler.ProviderSet,
backend/cmd/server/wire.go:43:		// Server layer ProviderSet
backend/cmd/server/wire.go:44:		server.ProviderSet,
backend/internal/service/wire.go:83:	svc.Start()
backend/internal/service/wire.go:161:	svc.Start()
backend/internal/service/wire.go:168:	svc.Start()
backend/internal/service/wire.go:175:	svc.Start()
backend/internal/service/wire.go:182:	svc.Start()
backend/internal/service/wire.go:192:	svc.Start()
backend/internal/service/wire.go:202:	svc.Start()
backend/internal/service/wire.go:209:	svc.Start()
backend/internal/service/wire.go:244:	svc.Start()
backend/internal/service/wire.go:279:	collector.Start()
backend/internal/service/wire.go:292:	svc.Start()
backend/internal/service/wire.go:306:	svc.Start()
backend/internal/service/wire.go:325:	svc.Start()
backend/internal/service/wire.go:334:	sink.Start()
backend/internal/service/wire.go:374:	svc.Start()
backend/internal/service/wire.go:395:	svc.Start()
backend/internal/service/wire.go:408:	svc.Start()
backend/internal/service/wire.go:428:	svc.Start()
backend/internal/service/wire.go:514:// ProviderSet is the Wire provider set for all services
backend/internal/service/wire.go:515:var ProviderSet = wire.NewSet(
backend/internal/service/wire.go:615:	svc.Start()
backend/internal/service/wire.go:643:	svc.Start()
backend/internal/service/wire.go:663:	r.Start()
backend/cmd/server/main.go:151:	app, err := initializeApplication(buildInfo)
backend/cmd/server/wire_gen.go:34:func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
backend/cmd/server/wire.go:31:func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
backend/cmd/server/wire.go:32:	wire.Build(
backend/cmd/server/wire.go:33:		// Infrastructure layer ProviderSets
backend/cmd/server/wire.go:34:		config.ProviderSet,
backend/cmd/server/wire.go:36:		// Business layer ProviderSets
backend/cmd/server/wire.go:37:		repository.ProviderSet,
backend/cmd/server/wire.go:38:		service.ProviderSet,
backend/cmd/server/wire.go:39:		payment.ProviderSet,
backend/cmd/server/wire.go:40:		middleware.ProviderSet,
backend/cmd/server/wire.go:41:		handler.ProviderSet,
backend/cmd/server/wire.go:43:		// Server layer ProviderSet
backend/cmd/server/wire.go:44:		server.ProviderSet,

exec
/bin/bash -lc 'rg -n "type VersionFields|VersionFields|type Registry|NewRegistry|clientidentity" backend/internal/pkg backend/internal/service backend/cmd/server' in /mnt/x/project/sub2api
exec
/bin/bash -lc "rg -n \"type Gateway struct|Gateway struct|UAAutoFetch|ua_auto_fetch|mapstructure:\\\"gateway\\\"\" backend/internal/config backend -g'*.go' -g'*.yaml' -g'*.yml'" in /mnt/x/project/sub2api
exec
/bin/bash -lc "rg -n \"DefaultHeaders|CLICurrentVersion|codexCLIUserAgent|codexCLIVersion|originator\" backend/internal/pkg backend/internal/service -g'*.go'" in /mnt/x/project/sub2api
 succeeded in 438ms:
backend/internal/service/payment_order_lifecycle_test.go:231:	registry := payment.NewRegistry()
backend/internal/service/payment_order_lifecycle_test.go:332:	registry := payment.NewRegistry()
backend/internal/service/payment_order_lifecycle_test.go:423:	registry := payment.NewRegistry()
backend/internal/service/payment_order_lifecycle_test.go:486:	registry := payment.NewRegistry()
backend/internal/service/payment_order_lifecycle_test.go:543:	registry := payment.NewRegistry()
backend/internal/service/payment_order_lifecycle_test.go:636:	registry := payment.NewRegistry()
backend/internal/service/payment_order_lifecycle_test.go:738:	registry := payment.NewRegistry()
backend/internal/service/payment_fulfillment_test.go:195:	registry := payment.NewRegistry()
backend/internal/service/payment_fulfillment_test.go:210:	registry := payment.NewRegistry()
backend/internal/service/payment_fulfillment_test.go:234:	registry := payment.NewRegistry()
backend/internal/service/payment_fulfillment_test.go:249:	registry := payment.NewRegistry()
backend/internal/service/payment_resume_lookup_test.go:245:	registry := payment.NewRegistry()
backend/internal/service/payment_resume_lookup_test.go:291:	registry := payment.NewRegistry()
backend/internal/service/payment_webhook_provider_test.go:326:		registry:        payment.NewRegistry(),
backend/internal/service/payment_webhook_provider_test.go:357:		registry:        payment.NewRegistry(),
backend/internal/service/payment_webhook_provider_test.go:378:	registry := payment.NewRegistry()
backend/internal/service/payment_webhook_provider_test.go:428:	registry := payment.NewRegistry()
backend/internal/service/payment_webhook_provider_test.go:502:		registry:        payment.NewRegistry(),

 succeeded in 448ms:
backend/internal/pkg/claude/constants.go:65:// CLICurrentVersion 是 sub2api 当前对外伪装的 Claude Code CLI 版本号（三段 semver）。
backend/internal/pkg/claude/constants.go:67:// 必须与 DefaultHeaders["User-Agent"] 中的版本号严格一致；不一致会被 Anthropic 判第三方。
backend/internal/pkg/claude/constants.go:68:const CLICurrentVersion = "2.1.161"
backend/internal/pkg/claude/constants.go:91:// DefaultHeaders 是 Claude Code 客户端默认请求头。
backend/internal/pkg/claude/constants.go:92:var DefaultHeaders = map[string]string{
backend/internal/pkg/claude/constants.go:96:	"User-Agent":                                "claude-cli/" + CLICurrentVersion + " (external, cli)",
backend/internal/service/gateway_service.go:4322:	billingText, err := buildBillingAttributionText(body, claude.CLICurrentVersion)
backend/internal/service/gateway_service.go:4326:	fp := computeClaudeCodeFingerprint(body, claude.CLICurrentVersion)
backend/internal/service/gateway_service.go:4329:		"{cc_version}", claude.CLICurrentVersion,
backend/internal/service/gateway_service.go:6970:	for key, value := range claude.DefaultHeaders {
backend/internal/service/gateway_service.go:7419:	for key, value := range claude.DefaultHeaders {
backend/internal/service/gateway_prompt_test.go:501:	require.Contains(t, arr[0].Get("text").String(), "prefix "+claude.CLICurrentVersion+".")
backend/internal/service/identity_service.go:30:	UserAgent:               "claude-cli/" + claude.CLICurrentVersion + " (external, cli)",
backend/internal/service/identity_service.go:189:	// 设置x-stainless-*头（保持与 claude.DefaultHeaders 一致的大小写）
backend/internal/service/openai_client_restriction_detector_test.go:13:func newCodexDetectorTestContext(ua string, originator string) *gin.Context {
backend/internal/service/openai_client_restriction_detector_test.go:20:	if originator != "" {
backend/internal/service/openai_client_restriction_detector_test.go:21:		c.Request.Header.Set("originator", originator)
backend/internal/service/openai_client_restriction_detector_test.go:81:	t.Run("开启后 originator 命中", func(t *testing.T) {
backend/internal/service/openai_client_restriction_detector_test.go:151:	t.Run("配置白名单但伪造 originator 仍拒绝", func(t *testing.T) {
backend/internal/service/openai_client_restriction_detector.go:14:	// CodexClientRestrictionReasonMatchedOriginator 表示请求命中官方客户端 originator 白名单。
backend/internal/service/openai_client_restriction_detector.go:15:	CodexClientRestrictionReasonMatchedOriginator = "official_client_originator_matched"
backend/internal/service/openai_client_restriction_detector.go:65:	originator := ""
backend/internal/service/openai_client_restriction_detector.go:68:		originator = c.GetHeader("originator")
backend/internal/service/openai_client_restriction_detector.go:77:	if openai.IsCodexOfficialClientOriginator(originator) {
backend/internal/service/openai_client_restriction_detector.go:87:		openai.MatchAllowedClients(userAgent, originator, allowed) {
backend/internal/service/openai_client_restriction_detector.go:97:		openai.MatchAllowedClients(userAgent, originator, globalAllowedClients) {
backend/internal/service/upstream_models.go:200:	for key, value := range claude.DefaultHeaders {
backend/internal/service/upstream_models.go:240:	for key, value := range claude.DefaultHeaders {
backend/internal/pkg/openai/request_test.go:63:		originator string
backend/internal/pkg/openai/request_test.go:66:		{name: "codex_cli_rs", originator: "codex_cli_rs", want: true},
backend/internal/pkg/openai/request_test.go:67:		{name: "codex_vscode", originator: "codex_vscode", want: true},
backend/internal/pkg/openai/request_test.go:68:		{name: "codex_app", originator: "codex_app", want: true},
backend/internal/pkg/openai/request_test.go:69:		{name: "codex_chatgpt_desktop", originator: "codex_chatgpt_desktop", want: true},
backend/internal/pkg/openai/request_test.go:70:		{name: "codex_atlas", originator: "codex_atlas", want: true},
backend/internal/pkg/openai/request_test.go:71:		{name: "codex_exec", originator: "codex_exec", want: true},
backend/internal/pkg/openai/request_test.go:72:		{name: "codex_sdk_ts", originator: "codex_sdk_ts", want: true},
backend/internal/pkg/openai/request_test.go:73:		{name: "Codex 前缀", originator: "Codex Desktop", want: true},
backend/internal/pkg/openai/request_test.go:74:		{name: "空白包裹", originator: "  codex_vscode  ", want: true},
backend/internal/pkg/openai/request_test.go:75:		{name: "非 codex", originator: "my_client", want: false},
backend/internal/pkg/openai/request_test.go:76:		{name: "空字符串", originator: "", want: false},
backend/internal/pkg/openai/request_test.go:81:			got := IsCodexOfficialClientOriginator(tt.originator)
backend/internal/pkg/openai/request_test.go:83:				t.Fatalf("IsCodexOfficialClientOriginator(%q) = %v, want %v", tt.originator, got, tt.want)
backend/internal/pkg/openai/request_test.go:93:		originator string
backend/internal/pkg/openai/request_test.go:96:		{name: "仅 originator 命中 desktop", originator: "Codex Desktop", want: true},
backend/internal/pkg/openai/request_test.go:97:		{name: "仅 originator 命中 vscode", originator: "codex_vscode", want: true},
backend/internal/pkg/openai/request_test.go:99:		{name: "ua 与 originator 都未命中", ua: "curl/8.0.1", originator: "my_client", want: false},
backend/internal/pkg/openai/request_test.go:104:			got := IsCodexOfficialClientByHeaders(tt.ua, tt.originator)
backend/internal/pkg/openai/request_test.go:106:				t.Fatalf("IsCodexOfficialClientByHeaders(%q, %q) = %v, want %v", tt.ua, tt.originator, got, tt.want)
backend/internal/pkg/openai/request.go:25:// CodexOfficialClientOriginatorPrefixes matches Codex 官方客户端家族 originator 前缀。
backend/internal/pkg/openai/request.go:63:// IsCodexOfficialClientOriginator checks if originator indicates a Codex 官方客户端请求。
backend/internal/pkg/openai/request.go:64:func IsCodexOfficialClientOriginator(originator string) bool {
backend/internal/pkg/openai/request.go:65:	v := normalizeCodexClientHeader(originator)
backend/internal/pkg/openai/request.go:74:func IsCodexOfficialClientByHeaders(userAgent, originator string) bool {
backend/internal/pkg/openai/request.go:75:	return IsCodexOfficialClientRequest(userAgent) || IsCodexOfficialClientOriginator(originator)
backend/internal/service/openai_gateway_service_test.go:2094:	require.Equal(t, codexCLIVersion, req.Header.Get("Version"))
backend/internal/service/openai_gateway_service_test.go:2115:	require.Equal(t, codexCLIVersion, req.Header.Get("Version"))
backend/internal/service/openai_gateway_service_test.go:2127:	c.Request.Header.Set("originator", "codex_cli_rs")
backend/internal/service/openai_gateway_service_test.go:2140:	require.Empty(t, req.Header.Get("originator"))
backend/internal/service/openai_gateway_service_test.go:2171:		originator     string
backend/internal/service/openai_gateway_service_test.go:2174:		{name: "desktop originator preserved", originator: "Codex Desktop", wantOriginator: "Codex Desktop"},
backend/internal/service/openai_gateway_service_test.go:2175:		{name: "vscode originator preserved", originator: "codex_vscode", wantOriginator: "codex_vscode"},
backend/internal/service/openai_gateway_service_test.go:2187:			if tt.originator != "" {
backend/internal/service/openai_gateway_service_test.go:2188:				c.Request.Header.Set("originator", tt.originator)
backend/internal/service/openai_gateway_service_test.go:2197:			isCodexCLI := openai.IsCodexOfficialClientByHeaders(c.GetHeader("User-Agent"), c.GetHeader("originator"))
backend/internal/service/openai_gateway_service_test.go:2200:			require.Equal(t, tt.wantOriginator, req.Header.Get("originator"))
backend/internal/service/openai_gateway_messages.go:267:		// the Responses experimental beta header, and forcing originator can make
backend/internal/service/openai_gateway_messages.go:270:		upstreamReq.Header.Del("originator")
backend/internal/service/openai_ws_forwarder_success_test.go:676:		originator     string
backend/internal/service/openai_ws_forwarder_success_test.go:679:		{name: "desktop originator preserved", originator: "Codex Desktop", wantOriginator: "Codex Desktop"},
backend/internal/service/openai_ws_forwarder_success_test.go:680:		{name: "vscode originator preserved", originator: "codex_vscode", wantOriginator: "codex_vscode"},
backend/internal/service/openai_ws_forwarder_success_test.go:692:			if tt.originator != "" {
backend/internal/service/openai_ws_forwarder_success_test.go:693:				c.Request.Header.Set("originator", tt.originator)
backend/internal/service/openai_ws_forwarder_success_test.go:710:					[]byte(`{"type":"response.completed","response":{"id":"resp_oauth_originator","model":"gpt-5.1","usage":{"input_tokens":1,"output_tokens":1}}}`),
backend/internal/service/openai_ws_forwarder_success_test.go:745:			require.Equal(t, tt.wantOriginator, captureDialer.lastHeaders.Get("originator"))
backend/internal/service/openai_gateway_chat_completions_raw.go:27:// （originator / session_id / x-codex-turn-state / x-codex-turn-metadata / conversation_id），
backend/internal/service/openai_oauth_passthrough_test.go:197:	require.Empty(t, upstream.lastReq.Header.Get("originator"))
backend/internal/service/openai_oauth_passthrough_test.go:452:	require.Equal(t, codexCLIVersion, upstream.lastReq.Header.Get("Version"))
backend/internal/service/openai_oauth_passthrough_test.go:688:	require.Equal(t, "codex_cli_rs", upstream.lastReq.Header.Get("originator"))
backend/internal/service/openai_oauth_passthrough_test.go:689:	require.NotEqual(t, "opencode", upstream.lastReq.Header.Get("originator"))
backend/internal/service/openai_oauth_passthrough_test.go:978:	require.Equal(t, codexCLIUserAgent, upstream.lastReq.Header.Get("User-Agent"))
backend/internal/service/openai_oauth_passthrough_test.go:1020:		originator string
backend/internal/service/openai_oauth_passthrough_test.go:1022:		{name: "codex_cli_rs", ua: "codex_cli_rs/0.99.0", originator: ""},
backend/internal/service/openai_oauth_passthrough_test.go:1023:		{name: "codex_vscode", ua: "codex_vscode/1.0.0", originator: ""},
backend/internal/service/openai_oauth_passthrough_test.go:1024:		{name: "codex_app", ua: "codex_app/2.1.0", originator: ""},
backend/internal/service/openai_oauth_passthrough_test.go:1025:		{name: "originator_codex_chatgpt_desktop", ua: "curl/8.0", originator: "codex_chatgpt_desktop"},
backend/internal/service/openai_oauth_passthrough_test.go:1034:			if tt.originator != "" {
backend/internal/service/openai_oauth_passthrough_test.go:1035:				c.Request.Header.Set("originator", tt.originator)
backend/internal/pkg/openai/allowed_client.go:17:// 这确保双因子匹配不会因缺失 UA 声明而退化为仅凭可伪造的 originator 单因子放行。
backend/internal/pkg/openai/allowed_client.go:26:// initialize 握手，codex 据此把 originator 设为 "Claude Code"，User-Agent 前缀同样为
backend/internal/pkg/openai/allowed_client.go:36:// originator 必须精确等值（归一化后）；UAContains 中每一项都必须出现在 UA 中。
backend/internal/pkg/openai/allowed_client.go:38:func IsAllowedClientMatch(userAgent, originator string, entry AllowedClientEntry) bool {
backend/internal/pkg/openai/allowed_client.go:43:	if normalizeCodexClientHeader(originator) != wantOriginator {
backend/internal/pkg/openai/allowed_client.go:46:	// 预设必须声明 UA 特征：否则将退化为仅凭可伪造的 originator 单因子匹配。
backend/internal/pkg/openai/allowed_client.go:54:			// 空白 marker 让该项失去校验能力，会让双因子退化为仅 originator
backend/internal/pkg/openai/allowed_client.go:67:func MatchAllowedClients(userAgent, originator string, clientIDs []string) bool {
backend/internal/pkg/openai/allowed_client.go:73:		if IsAllowedClientMatch(userAgent, originator, entry) {
backend/internal/service/openai_gateway_service.go:46:	// {originator}/{version} ({OS} {OS_version}; {arch}) {terminal}
backend/internal/service/openai_gateway_service.go:48:	codexCLIUserAgent = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
backend/internal/service/openai_gateway_service.go:62:	codexCLIVersion                    = "0.125.0"
backend/internal/service/openai_gateway_service.go:77:	"originator":            true,
backend/internal/service/openai_gateway_service.go:92:	"originator":            true,
backend/internal/service/openai_gateway_service.go:1072:	originator := ""
backend/internal/service/openai_gateway_service.go:1075:		originator = strings.TrimSpace(c.GetHeader("originator"))
backend/internal/service/openai_gateway_service.go:1085:		zap.Bool("codex_official_client_match", openai.IsCodexOfficialClientByHeaders(userAgent, originator)),
backend/internal/service/openai_gateway_service.go:1270:		if originator := strings.TrimSpace(c.GetHeader("originator")); originator != "" {
backend/internal/service/openai_gateway_service.go:1271:			return originator
backend/internal/service/openai_gateway_service.go:2400:	isCodexCLI := openai.IsCodexOfficialClientByHeaders(c.GetHeader("User-Agent"), c.GetHeader("originator")) || (s.cfg != nil && s.cfg.Gateway.ForceCodexCLI)
backend/internal/service/openai_gateway_service.go:3449:				req.Header.Set("version", codexCLIVersion)
backend/internal/service/openai_gateway_service.go:3460:		if req.Header.Get("originator") == "" {
backend/internal/service/openai_gateway_service.go:3461:			req.Header.Set("originator", "codex_cli_rs")
backend/internal/service/openai_gateway_service.go:3484:		req.Header.Set("user-agent", codexCLIUserAgent)
backend/internal/service/openai_gateway_service.go:3488:		req.Header.Set("user-agent", codexCLIUserAgent)
backend/internal/service/openai_gateway_service.go:4210:			req.Header.Del("originator")
backend/internal/service/openai_gateway_service.go:4213:			req.Header.Set("originator", resolveOpenAIUpstreamOriginator(c, isCodexCLI))
backend/internal/service/openai_gateway_service.go:4219:				req.Header.Set("version", codexCLIVersion)
backend/internal/service/openai_gateway_service.go:4244:		req.Header.Set("user-agent", codexCLIUserAgent)
backend/internal/pkg/openai/allowed_client_test.go:5:// 真实的 Claude Code codex 插件请求头：originator 与 UA 前缀同源于 clientInfo.name="Claude Code"。
backend/internal/pkg/openai/allowed_client_test.go:17:		originator string
backend/internal/pkg/openai/allowed_client_test.go:20:		{name: "真实签名命中", ua: testClaudeCodeUserAgent, originator: testClaudeCodeOriginator, want: true},
backend/internal/pkg/openai/allowed_client_test.go:21:		{name: "大小写不敏感", ua: "claude code/0.5.0 (macos)", originator: "claude code", want: true},
backend/internal/pkg/openai/allowed_client_test.go:22:		{name: "originator 两侧空白被裁剪", ua: testClaudeCodeUserAgent, originator: "  Claude Code  ", want: true},
backend/internal/pkg/openai/allowed_client_test.go:23:		{name: "originator 非精确（带后缀）不命中", ua: testClaudeCodeUserAgent, originator: "Claude Code Extra", want: false},
backend/internal/pkg/openai/allowed_client_test.go:24:		{name: "originator 为空不命中", ua: testClaudeCodeUserAgent, originator: "", want: false},
backend/internal/pkg/openai/allowed_client_test.go:25:		{name: "originator 是官方 codex 不命中", ua: testClaudeCodeUserAgent, originator: "codex_cli_rs", want: false},
backend/internal/pkg/openai/allowed_client_test.go:26:		{name: "UA 缺少 Claude Code/ 标记不命中", ua: "curl/8.0", originator: testClaudeCodeOriginator, want: false},
backend/internal/pkg/openai/allowed_client_test.go:31:			if got := IsAllowedClientMatch(tt.ua, tt.originator, entry); got != tt.want {
backend/internal/pkg/openai/allowed_client_test.go:32:				t.Fatalf("IsAllowedClientMatch(%q, %q) = %v, want %v", tt.ua, tt.originator, got, tt.want)
backend/internal/pkg/openai/allowed_client_test.go:47:	// 预设必须声明 UA 特征，否则退化为仅凭可伪造的 originator 单因子匹配，绝不放行。
backend/internal/pkg/openai/allowed_client_test.go:50:		t.Fatal("未声明 UA 特征的预设不应匹配，避免退化为单因子 originator 匹配")
backend/internal/pkg/openai/allowed_client_test.go:55:	// 全空白 marker 归一化后为空，若被跳过则退化为仅 originator 单因子；
backend/internal/pkg/openai/allowed_client_test.go:59:		t.Fatal("UAContains 含全空白 marker 不应匹配，避免退化为单因子 originator 匹配")
backend/internal/pkg/openai/allowed_client_test.go:76:		originator string
backend/internal/pkg/openai/allowed_client_test.go:80:		{name: "claude_code 预设命中真实签名", ua: testClaudeCodeUserAgent, originator: testClaudeCodeOriginator, clientIDs: []string{AllowedClientClaudeCode}, want: true},
backend/internal/pkg/openai/allowed_client_test.go:81:		{name: "claude_code 预设 + 伪造 originator 不命中", ua: testClaudeCodeUserAgent, originator: "my_client", clientIDs: []string{AllowedClientClaudeCode}, want: false},
backend/internal/pkg/openai/allowed_client_test.go:82:		{name: "空列表不放行", ua: testClaudeCodeUserAgent, originator: testClaudeCodeOriginator, clientIDs: nil, want: false},
backend/internal/pkg/openai/allowed_client_test.go:83:		{name: "未知预设 ID 不放行", ua: testClaudeCodeUserAgent, originator: testClaudeCodeOriginator, clientIDs: []string{"unknown_client"}, want: false},
backend/internal/pkg/openai/allowed_client_test.go:84:		{name: "ID 大小写/空白容错", ua: testClaudeCodeUserAgent, originator: testClaudeCodeOriginator, clientIDs: []string{"  Claude_Code "}, want: true},
backend/internal/pkg/openai/allowed_client_test.go:85:		{name: "多预设任一命中即放行", ua: testClaudeCodeUserAgent, originator: testClaudeCodeOriginator, clientIDs: []string{"unknown_client", AllowedClientClaudeCode}, want: true},
backend/internal/pkg/openai/allowed_client_test.go:90:			if got := MatchAllowedClients(tt.ua, tt.originator, tt.clientIDs); got != tt.want {
backend/internal/pkg/openai/allowed_client_test.go:91:				t.Fatalf("MatchAllowedClients(%q, %q, %v) = %v, want %v", tt.ua, tt.originator, tt.clientIDs, got, tt.want)
backend/internal/service/openai_compat_model_test.go:841:	require.Empty(t, upstream.requests[0].Header.Get("originator"))
backend/internal/service/openai_compat_model_test.go:856:	require.Empty(t, upstream.requests[1].Header.Get("originator"))
backend/internal/service/openai_compat_model_test.go:1068:	require.Empty(t, upstream.requests[0].Header.Get("originator"))
backend/internal/service/openai_quota_service.go:273:		"originator":         openaiQuotaCodexOriginator,
backend/internal/service/openai_ws_forwarder.go:1158:		headers.Set("originator", resolveOpenAIUpstreamOriginator(c, isCodexCLI))
backend/internal/service/openai_ws_forwarder.go:1179:		headers.Set("user-agent", codexCLIUserAgent)
backend/internal/service/openai_ws_forwarder.go:1182:		headers.Set("user-agent", codexCLIUserAgent)
backend/internal/service/openai_ws_forwarder.go:1874:		"acquire_start account_id=%d account_type=%s transport=%s preferred_conn_id=%s has_previous_response_id=%v session_hash=%s has_turn_state=%v turn_state_len=%d has_turn_metadata=%v turn_metadata_len=%d store_disabled=%v store_disabled_conn_mode=%s retry_last_reason=%s force_new_conn=%v header_user_agent=%s header_openai_beta=%s header_originator=%s header_accept_language=%s header_session_id=%s header_conversation_id=%s session_id_source=%s conversation_id_source=%s has_prompt_cache_key=%v has_chatgpt_account_id=%v has_authorization=%v has_session_id=%v has_conversation_id=%v proxy_enabled=%v",
backend/internal/service/openai_ws_forwarder.go:1891:		openAIWSHeaderValueForLog(wsHeaders, "originator"),
backend/internal/service/openai_ws_forwarder.go:2525:	isCodexCLI := openai.IsCodexOfficialClientByHeaders(c.GetHeader("User-Agent"), c.GetHeader("originator")) || (s.cfg != nil && s.cfg.Gateway.ForceCodexCLI)
backend/internal/service/openai_ws_v2_passthrough_adapter.go:337:		isCodexCLI = openai.IsCodexOfficialClientByHeaders(c.GetHeader("User-Agent"), c.GetHeader("originator"))

 succeeded in 638ms:
backend/internal/service/gateway_multiplatform_test.go:245:type mockGroupRepoForGateway struct {

**Assessing implementation needs**

I need to inspect some files, and it seems like a previous R1 result could indicate that things might already be done. I notice that the R1_step1_result.txt has prior output, but I need to check everything beyond the initial capture. We should look at the current state and see if there’s anything for the wire generation as well. I want to ensure that we understand our tools and memory use correctly, especially if anything is relevant for citations.
codex
现有 `clientidentity` 已经有 R3 的 family/resolver 类型，R1 需要在同包内补 registry，不替换 resolver。接下来我会读取这些文件和 wire 生成文件，确认不会破坏已完成 R2/R3。
exec
/bin/bash -lc "sed -n '1,220p' backend/internal/pkg/clientidentity/snapshot.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
package clientidentity

// IdentitySnapshot keeps the client identity fields together so later header,
// billing, identity, and TLS injection can consume one consistent source.
type IdentitySnapshot struct {
	Headers        map[string]string
	VersionFields  VersionFields
	TLSProfileName string
}

type VersionFields struct {
	CLIVersion string
	SDKVersion string
	CCVersion  string
	CCFP       string
}

exec
/bin/bash -lc "sed -n '1,180p' backend/internal/pkg/clientidentity/resolver.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
package clientidentity

import (
	"strings"

	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
	"github.com/Wei-Shaw/sub2api/internal/pkg/tlsfingerprint"
)

const (
	TLSProfileClaudeCLIDefault     = tlsfingerprint.ProfileNameClaudeCLIDefault
	TLSProfileClaudeDesktopDefault = tlsfingerprint.ProfileNameClaudeDesktopDefault
	TLSProfileCodexCLIDefault      = tlsfingerprint.ProfileNameCodexCLIDefault
	TLSProfileCodexDesktopDefault  = tlsfingerprint.ProfileNameCodexDesktopDefault
)

type Account interface {
	GetExtraString(key string) string
}

type Resolver struct{}

func NewResolver() *Resolver {
	return &Resolver{}
}

// Resolve maps account.extra.client_family to a complete identity snapshot.
// Empty or unknown families return nil so callers can preserve existing defaults.
func (r *Resolver) Resolve(account Account) *IdentitySnapshot {
	if account == nil {
		return nil
	}
	familyStr := strings.TrimSpace(account.GetExtraString("client_family"))
	if familyStr == "" {
		return nil
	}

	switch ClientFamily(familyStr) {
	case FamilyClaudeCLI:
		return r.claudeCLISnapshot()
	case FamilyClaudeDesktop:
		return r.claudeDesktopSnapshot()
	case FamilyCodexCLI:
		return r.codexCLISnapshot()
	case FamilyCodexDesktop:
		return r.codexDesktopSnapshot()
	default:
		return nil
	}
}

func (r *Resolver) claudeCLISnapshot() *IdentitySnapshot {
	// TODO(R1): read dynamic versions from IdentityRegistry atomic.Pointer.
	return &IdentitySnapshot{
		Headers:        cloneHeaders(claude.DefaultHeaders),
		VersionFields:  VersionFields{CLIVersion: claude.CLICurrentVersion, SDKVersion: claude.DefaultHeaders["X-Stainless-Package-Version"]},
		TLSProfileName: TLSProfileClaudeCLIDefault,
	}
}

func (r *Resolver) claudeDesktopSnapshot() *IdentitySnapshot {
	// TODO(R1): replace placeholder headers/version fields with captured Claude Desktop data.
	return &IdentitySnapshot{
		Headers: map[string]string{
			"User-Agent":       "claude-desktop/0.0.0",
			"anthropic-beta":   claude.DefaultBetaHeader,
			"X-App":            "desktop",
			"X-Stainless-Lang": "js",
		},
		VersionFields:  VersionFields{CLIVersion: "0.0.0"},
		TLSProfileName: TLSProfileClaudeDesktopDefault,
	}
}

func (r *Resolver) codexCLISnapshot() *IdentitySnapshot {
	// TODO(R1): replace placeholder headers/version fields with captured codex_cli_rs data.
	return &IdentitySnapshot{
		Headers: map[string]string{
			"User-Agent": "codex_cli_rs/0.0.0",
			"X-App":      "cli",
		},
		VersionFields:  VersionFields{CLIVersion: "0.0.0"},
		TLSProfileName: TLSProfileCodexCLIDefault,
	}
}

func (r *Resolver) codexDesktopSnapshot() *IdentitySnapshot {
	// TODO(R1): replace placeholder headers/version fields with captured Codex Desktop data.
	return &IdentitySnapshot{
		Headers: map[string]string{
			"User-Agent": "codex-desktop/0.0.0",
			"X-App":      "desktop",
		},
		VersionFields:  VersionFields{CLIVersion: "0.0.0"},
		TLSProfileName: TLSProfileCodexDesktopDefault,
	}
}

func cloneHeaders(headers map[string]string) map[string]string {
	cloned := make(map[string]string, len(headers))
	for key, value := range headers {
		cloned[key] = value
	}
	return cloned
}

exec
/bin/bash -lc "sed -n '1,160p' backend/internal/pkg/clientidentity/family.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
package clientidentity

type ClientFamily string

const (
	FamilyClaudeCLI     ClientFamily = "claude-cli"
	FamilyClaudeDesktop ClientFamily = "claude-desktop"
	FamilyCodexCLI      ClientFamily = "codex-cli"
	FamilyCodexDesktop  ClientFamily = "codex-desktop"
)

exec
/bin/bash -lc "sed -n '1,190p' backend/internal/config/config.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
// Package config provides configuration loading, defaults, and validation.
package config

import (
	"crypto/rand"
	"encoding/hex"
	"fmt"
	"log/slog"
	"net/url"
	"os"
	"strings"
	"sync/atomic"
	"time"

	"github.com/spf13/viper"
)

const (
	RunModeStandard = "standard"
	RunModeSimple   = "simple"
)

// 使用量记录队列溢出策略
const (
	UsageRecordOverflowPolicyDrop   = "drop"
	UsageRecordOverflowPolicySample = "sample"
	UsageRecordOverflowPolicySync   = "sync"
)

// DefaultCSPPolicy is the default Content-Security-Policy with nonce support
// __CSP_NONCE__ will be replaced with actual nonce at request time by the SecurityHeaders middleware
const DefaultCSPPolicy = "default-src 'self'; script-src 'self' __CSP_NONCE__ https://challenges.cloudflare.com https://static.cloudflareinsights.com https://*.stripe.com https://static.airwallex.com https://checkout.airwallex.com https://static-demo.airwallex.com https://checkout-demo.airwallex.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://static.airwallex.com https://checkout.airwallex.com https://static-demo.airwallex.com https://checkout-demo.airwallex.com; img-src 'self' data: https:; font-src 'self' data: https://fonts.gstatic.com; connect-src 'self' https:; frame-src https://challenges.cloudflare.com https://*.stripe.com https://checkout.airwallex.com https://checkout-demo.airwallex.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self'"

// UMQ（用户消息队列）模式常量
const (
	// UMQModeSerialize: 账号级串行锁 + RPM 自适应延迟
	UMQModeSerialize = "serialize"
	// UMQModeThrottle: 仅 RPM 自适应前置延迟，不阻塞并发
	UMQModeThrottle = "throttle"
)

// 连接池隔离策略常量
// 用于控制上游 HTTP 连接池的隔离粒度，影响连接复用和资源消耗
const (
	// ConnectionPoolIsolationProxy: 按代理隔离
	// 同一代理地址共享连接池，适合代理数量少、账户数量多的场景
	ConnectionPoolIsolationProxy = "proxy"
	// ConnectionPoolIsolationAccount: 按账户隔离
	// 每个账户独立连接池，适合账户数量少、需要严格隔离的场景
	ConnectionPoolIsolationAccount = "account"
	// ConnectionPoolIsolationAccountProxy: 按账户+代理组合隔离（默认）
	// 同一账户+代理组合共享连接池，提供最细粒度的隔离
	ConnectionPoolIsolationAccountProxy = "account_proxy"
)

// DefaultUpstreamResponseReadMaxBytes 上游非流式响应体的默认读取上限。
// 128 MB 可容纳 2-3 张 4K PNG（base64 膨胀 33%，单张 4K PNG 最坏约 67MB base64）。
// 可通过 gateway.upstream_response_read_max_bytes 配置项覆盖。
const DefaultUpstreamResponseReadMaxBytes int64 = 128 * 1024 * 1024

type Config struct {
	Server                  ServerConfig                  `mapstructure:"server"`
	Log                     LogConfig                     `mapstructure:"log"`
	CORS                    CORSConfig                    `mapstructure:"cors"`
	Security                SecurityConfig                `mapstructure:"security"`
	Billing                 BillingConfig                 `mapstructure:"billing"`
	Turnstile               TurnstileConfig               `mapstructure:"turnstile"`
	Database                DatabaseConfig                `mapstructure:"database"`
	Redis                   RedisConfig                   `mapstructure:"redis"`
	Ops                     OpsConfig                     `mapstructure:"ops"`
	JWT                     JWTConfig                     `mapstructure:"jwt"`
	Totp                    TotpConfig                    `mapstructure:"totp"`
	LinuxDo                 LinuxDoConnectConfig          `mapstructure:"linuxdo_connect"`
	WeChat                  WeChatConnectConfig           `mapstructure:"wechat_connect"`
	OIDC                    OIDCConnectConfig             `mapstructure:"oidc_connect"`
	DingTalk                DingTalkConnectConfig         `mapstructure:"dingtalk_connect"`
	GitHubOAuth             EmailOAuthProviderConfig      `mapstructure:"github_oauth"`
	GoogleOAuth             EmailOAuthProviderConfig      `mapstructure:"google_oauth"`
	Default                 DefaultConfig                 `mapstructure:"default"`
	RateLimit               RateLimitConfig               `mapstructure:"rate_limit"`
	Pricing                 PricingConfig                 `mapstructure:"pricing"`
	Gateway                 GatewayConfig                 `mapstructure:"gateway"`
	APIKeyAuth              APIKeyAuthCacheConfig         `mapstructure:"api_key_auth_cache"`
	SubscriptionCache       SubscriptionCacheConfig       `mapstructure:"subscription_cache"`
	SubscriptionMaintenance SubscriptionMaintenanceConfig `mapstructure:"subscription_maintenance"`
	Dashboard               DashboardCacheConfig          `mapstructure:"dashboard_cache"`
	DashboardAgg            DashboardAggregationConfig    `mapstructure:"dashboard_aggregation"`
	UsageCleanup            UsageCleanupConfig            `mapstructure:"usage_cleanup"`
	Concurrency             ConcurrencyConfig             `mapstructure:"concurrency"`
	TokenRefresh            TokenRefreshConfig            `mapstructure:"token_refresh"`
	RunMode                 string                        `mapstructure:"run_mode" yaml:"run_mode"`
	Timezone                string                        `mapstructure:"timezone"` // e.g. "Asia/Shanghai", "UTC"
	Gemini                  GeminiConfig                  `mapstructure:"gemini"`
	Update                  UpdateConfig                  `mapstructure:"update"`
	Idempotency             IdempotencyConfig             `mapstructure:"idempotency"`
}

type LogConfig struct {
	Level           string            `mapstructure:"level"`
	Format          string            `mapstructure:"format"`
	ServiceName     string            `mapstructure:"service_name"`
	Environment     string            `mapstructure:"env"`
	Caller          bool              `mapstructure:"caller"`
	StacktraceLevel string            `mapstructure:"stacktrace_level"`
	Output          LogOutputConfig   `mapstructure:"output"`
	Rotation        LogRotationConfig `mapstructure:"rotation"`
	Sampling        LogSamplingConfig `mapstructure:"sampling"`
}

type LogOutputConfig struct {
	ToStdout bool   `mapstructure:"to_stdout"`
	ToFile   bool   `mapstructure:"to_file"`
	FilePath string `mapstructure:"file_path"`
}

type LogRotationConfig struct {
	MaxSizeMB  int  `mapstructure:"max_size_mb"`
	MaxBackups int  `mapstructure:"max_backups"`
	MaxAgeDays int  `mapstructure:"max_age_days"`
	Compress   bool `mapstructure:"compress"`
	LocalTime  bool `mapstructure:"local_time"`
}

type LogSamplingConfig struct {
	Enabled    bool `mapstructure:"enabled"`
	Initial    int  `mapstructure:"initial"`
	Thereafter int  `mapstructure:"thereafter"`
}

type GeminiConfig struct {
	OAuth GeminiOAuthConfig `mapstructure:"oauth"`
	Quota GeminiQuotaConfig `mapstructure:"quota"`
}

type GeminiOAuthConfig struct {
	ClientID     string `mapstructure:"client_id"`
	ClientSecret string `mapstructure:"client_secret"`
	Scopes       string `mapstructure:"scopes"`
}

type GeminiQuotaConfig struct {
	Tiers  map[string]GeminiTierQuotaConfig `mapstructure:"tiers"`
	Policy string                           `mapstructure:"policy"`
}

type GeminiTierQuotaConfig struct {
	ProRPD          *int64 `mapstructure:"pro_rpd" json:"pro_rpd"`
	FlashRPD        *int64 `mapstructure:"flash_rpd" json:"flash_rpd"`
	CooldownMinutes *int   `mapstructure:"cooldown_minutes" json:"cooldown_minutes"`
}

type UpdateConfig struct {
	// ProxyURL 用于访问 GitHub 的代理地址
	// 支持 http/https/socks5/socks5h 协议
	// 例如: "http://127.0.0.1:7890", "socks5://127.0.0.1:1080"
	ProxyURL string `mapstructure:"proxy_url"`
}

type IdempotencyConfig struct {
	// ObserveOnly 为 true 时处于观察期：未携带 Idempotency-Key 的请求继续放行。
	ObserveOnly bool `mapstructure:"observe_only"`
	// DefaultTTLSeconds 关键写接口的幂等记录默认 TTL（秒）。
	DefaultTTLSeconds int `mapstructure:"default_ttl_seconds"`
	// SystemOperationTTLSeconds 系统操作接口的幂等记录 TTL（秒）。
	SystemOperationTTLSeconds int `mapstructure:"system_operation_ttl_seconds"`
	// ProcessingTimeoutSeconds processing 状态锁超时（秒）。
	ProcessingTimeoutSeconds int `mapstructure:"processing_timeout_seconds"`
	// FailedRetryBackoffSeconds 失败退避窗口（秒）。
	FailedRetryBackoffSeconds int `mapstructure:"failed_retry_backoff_seconds"`
	// MaxStoredResponseLen 持久化响应体最大长度（字节）。
	MaxStoredResponseLen int `mapstructure:"max_stored_response_len"`
	// CleanupIntervalSeconds 过期记录清理周期（秒）。
	CleanupIntervalSeconds int `mapstructure:"cleanup_interval_seconds"`
	// CleanupBatchSize 每次清理的最大记录数。
	CleanupBatchSize int `mapstructure:"cleanup_batch_size"`
}

type LinuxDoConnectConfig struct {
	Enabled             bool   `mapstructure:"enabled"`
	ClientID            string `mapstructure:"client_id"`
	ClientSecret        string `mapstructure:"client_secret"`
	AuthorizeURL        string `mapstructure:"authorize_url"`
	TokenURL            string `mapstructure:"token_url"`
	UserInfoURL         string `mapstructure:"userinfo_url"`
	Scopes              string `mapstructure:"scopes"`
	RedirectURL         string `mapstructure:"redirect_url"`          // 后端回调地址（需在提供方后台登记）
	FrontendRedirectURL string `mapstructure:"frontend_redirect_url"` // 前端接收 token 的路由（默认：/auth/linuxdo/callback）
	TokenAuthMethod     string `mapstructure:"token_auth_method"`     // client_secret_post / client_secret_basic / none
	UsePKCE             bool   `mapstructure:"use_pkce"`


codex
配置结构名是 `GatewayConfig`，不是提示里的 `Gateway`。我会按现有命名扩展 `GatewayConfig`，避免新增平行结构导致配置不生效。
exec
/bin/bash -lc "rg -n \"type GatewayConfig struct|gateway:\" backend/internal/config/config.go backend/config*.yml backend/config*.yaml backend -g'*.yaml' -g'*.yml'" in /mnt/x/project/sub2api
 exited 2 in 0ms:
rg: backend/config*.yml: No such file or directory (os error 2)
rg: backend/config*.yaml: No such file or directory (os error 2)
backend/internal/config/config.go:688:type GatewayConfig struct {

exec
/bin/bash -lc "sed -n '500,720p' backend/internal/service/wire.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
	apiKeyRepo APIKeyRepository,
	userRepo UserRepository,
	groupRepo GroupRepository,
	userSubRepo UserSubscriptionRepository,
	userGroupRateRepo UserGroupRateRepository,
	cache APIKeyCache,
	cfg *config.Config,
	billingCacheService *BillingCacheService,
) *APIKeyService {
	svc := NewAPIKeyService(apiKeyRepo, userRepo, groupRepo, userSubRepo, userGroupRateRepo, cache, cfg)
	svc.SetRateLimitCacheInvalidator(billingCacheService)
	return svc
}

// ProviderSet is the Wire provider set for all services
var ProviderSet = wire.NewSet(
	// Core services
	NewAuthService,
	NewUserService,
	ProvideAPIKeyService,
	ProvideAPIKeyAuthCacheInvalidator,
	NewGroupService,
	NewAccountService,
	NewProxyService,
	NewRedeemService,
	NewPromoService,
	NewUsageService,
	NewDashboardService,
	ProvidePricingService,
	NewBillingService,
	ProvideBillingCacheService,
	NewAnnouncementService,
	NewAdminService,
	NewGatewayService,
	NewOpenAIGatewayService,
	wire.Bind(new(AccountRuntimeBlocker), new(*OpenAIGatewayService)),
	NewOAuthService,
	ProvideOpenAIOAuthService,
	NewGeminiOAuthService,
	NewGeminiQuotaService,
	NewCompositeTokenCacheInvalidator,
	wire.Bind(new(TokenCacheInvalidator), new(*CompositeTokenCacheInvalidator)),
	NewAntigravityOAuthService,
	ProvideOAuthRefreshAPI,
	ProvideGeminiTokenProvider,
	NewGeminiMessagesCompatService,
	ProvideAntigravityTokenProvider,
	ProvideOpenAITokenProvider,
	ProvideOpenAIQuotaService,
	ProvideClaudeTokenProvider,
	NewAntigravityGatewayService,
	ProvideRateLimitService,
	NewAccountUsageService,
	NewAccountTestService,
	ProvideSettingService,
	NewDataManagementService,
	ProvideBackupService,
	ProvideOpsSystemLogSink,
	ProvideOpsService,
	ProvideOpsMetricsCollector,
	ProvideOpsAggregationService,
	ProvideOpsAlertEvaluatorService,
	ProvideOpsCleanupService,
	ProvideOpsScheduledReportService,
	NewEmailService,
	NewNotificationEmailService,
	ProvideEmailQueueService,
	NewTurnstileService,
	NewSubscriptionService,
	wire.Bind(new(DefaultSubscriptionAssigner), new(*SubscriptionService)),
	ProvideConcurrencyService,
	ProvideUserMessageQueueService,
	NewUsageRecordWorkerPool,
	ProvideSchedulerSnapshotService,
	NewIdentityService,
	NewCRSSyncService,
	ProvideUpdateService,
	ProvideTokenRefreshService,
	ProvideAccountExpiryService,
	ProvideProxyExpiryService,
	ProvideSubscriptionExpiryService,
	ProvideTimingWheelService,
	ProvideDashboardAggregationService,
	ProvideUsageCleanupService,
	ProvideDeferredService,
	NewAntigravityQuotaFetcher,
	NewUserAttributeService,
	NewUsageCache,
	NewTotpService,
	NewErrorPassthroughService,
	NewTLSFingerprintProfileService,
	NewDigestSessionStore,
	ProvideIdempotencyCoordinator,
	ProvideSystemOperationLockService,
	ProvideIdempotencyCleanupService,
	ProvideScheduledTestService,
	ProvideScheduledTestRunnerService,
	NewGroupCapacityService,
	NewChannelService,
	NewModelPricingResolver,
	NewContentModerationService,
	NewAffiliateService,
	ProvidePaymentConfigService,
	ProvidePaymentService,
	ProvidePaymentOrderExpiryService,
	ProvideBalanceNotifyService,
	ProvideChannelMonitorService,
	ProvideChannelMonitorRunner,
	NewChannelMonitorRequestTemplateService,
	ProvideUserPlatformQuotaUsageFlusher,
)

// ProvideUserPlatformQuotaUsageFlusher 创建并启动 UserPlatformQuotaUsageFlusher。
func ProvideUserPlatformQuotaUsageFlusher(cfg *config.Config, cache BillingCache, quotaRepo UserPlatformQuotaRepository, tw *TimingWheelService) *UserPlatformQuotaUsageFlusher {
	svc := NewUserPlatformQuotaUsageFlusher(cfg, cache, quotaRepo, tw)
	svc.Start()
	return svc
}

// ProvidePaymentConfigService wraps NewPaymentConfigService to accept the named
// payment.EncryptionKey type instead of raw []byte, avoiding Wire ambiguity.
func ProvidePaymentConfigService(entClient *dbent.Client, settingRepo SettingRepository, key payment.EncryptionKey) *PaymentConfigService {
	return NewPaymentConfigService(entClient, settingRepo, []byte(key))
}

// ProvideBalanceNotifyService creates BalanceNotifyService
func ProvideBalanceNotifyService(emailService *EmailService, settingRepo SettingRepository, accountRepo AccountRepository, notificationEmailService *NotificationEmailService) *BalanceNotifyService {
	svc := NewBalanceNotifyService(emailService, settingRepo, accountRepo)
	svc.SetNotificationEmailService(notificationEmailService)
	return svc
}

// ProvidePaymentService creates PaymentService and attaches notification email delivery.
func ProvidePaymentService(entClient *dbent.Client, registry *payment.Registry, loadBalancer payment.LoadBalancer, redeemService *RedeemService, subscriptionSvc *SubscriptionService, configService *PaymentConfigService, userRepo UserRepository, groupRepo GroupRepository, affiliateService *AffiliateService, notificationEmailService *NotificationEmailService) *PaymentService {
	svc := NewPaymentService(entClient, registry, loadBalancer, redeemService, subscriptionSvc, configService, userRepo, groupRepo, affiliateService)
	svc.SetNotificationEmailService(notificationEmailService)
	return svc
}

// ProvidePaymentOrderExpiryService creates and starts PaymentOrderExpiryService.
func ProvidePaymentOrderExpiryService(paymentSvc *PaymentService, lockCache LeaderLockCache, db *sql.DB) *PaymentOrderExpiryService {
	svc := NewPaymentOrderExpiryService(paymentSvc, 60*time.Second)
	svc.SetLeaderLock(lockCache, db)
	svc.Start()
	return svc
}

// ProvideChannelMonitorService 创建渠道监控服务（CRUD + RunCheck + 用户视图聚合）。
// 加密器复用 wire 中已注入的 SecretEncryptor（AES-256-GCM）。
func ProvideChannelMonitorService(
	repo ChannelMonitorRepository,
	encryptor SecretEncryptor,
) *ChannelMonitorService {
	return NewChannelMonitorService(repo, encryptor)
}

// ProvideChannelMonitorRunner 创建并启动渠道监控调度器。
// 通过 SetScheduler 注入回 service 后再 Start，确保启动时加载所有 enabled monitor，
// 后续 CRUD 也能即时同步任务表。Runner.Stop 由 cleanup function 调用。
// settingService 用于 runner 每次 fire 读取功能开关。
func ProvideChannelMonitorRunner(svc *ChannelMonitorService, settingService *SettingService) *ChannelMonitorRunner {
	r := NewChannelMonitorRunner(svc, settingService)
	svc.SetScheduler(r)
	r.Start()
	return r
}

exec
/bin/bash -lc "sed -n '1,170p' backend/cmd/server/wire.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
//go:build wireinject
// +build wireinject

package main

import (
	"context"
	"log"
	"net/http"
	"sync"
	"time"

	"github.com/Wei-Shaw/sub2api/ent"
	"github.com/Wei-Shaw/sub2api/internal/config"
	"github.com/Wei-Shaw/sub2api/internal/handler"
	"github.com/Wei-Shaw/sub2api/internal/payment"
	"github.com/Wei-Shaw/sub2api/internal/repository"
	"github.com/Wei-Shaw/sub2api/internal/server"
	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
	"github.com/Wei-Shaw/sub2api/internal/service"

	"github.com/google/wire"
	"github.com/redis/go-redis/v9"
)

type Application struct {
	Server  *http.Server
	Cleanup func()
}

func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
	wire.Build(
		// Infrastructure layer ProviderSets
		config.ProviderSet,

		// Business layer ProviderSets
		repository.ProviderSet,
		service.ProviderSet,
		payment.ProviderSet,
		middleware.ProviderSet,
		handler.ProviderSet,

		// Server layer ProviderSet
		server.ProviderSet,

		// Privacy client factory for OpenAI training opt-out
		providePrivacyClientFactory,

		// BuildInfo provider
		provideServiceBuildInfo,

		// Cleanup function provider
		provideCleanup,

		// Application struct
		wire.Struct(new(Application), "Server", "Cleanup"),
	)
	return nil, nil
}

func providePrivacyClientFactory() service.PrivacyClientFactory {
	return repository.CreatePrivacyReqClient
}

func provideServiceBuildInfo(buildInfo handler.BuildInfo) service.BuildInfo {
	return service.BuildInfo{
		Version:   buildInfo.Version,
		BuildType: buildInfo.BuildType,
	}
}

func provideCleanup(
	entClient *ent.Client,
	rdb *redis.Client,
	opsMetricsCollector *service.OpsMetricsCollector,
	opsAggregation *service.OpsAggregationService,
	opsAlertEvaluator *service.OpsAlertEvaluatorService,
	opsCleanup *service.OpsCleanupService,
	opsScheduledReport *service.OpsScheduledReportService,
	opsSystemLogSink *service.OpsSystemLogSink,
	schedulerSnapshot *service.SchedulerSnapshotService,
	tokenRefresh *service.TokenRefreshService,
	accountExpiry *service.AccountExpiryService,
	proxyExpiry *service.ProxyExpiryService,
	subscriptionExpiry *service.SubscriptionExpiryService,
	usageCleanup *service.UsageCleanupService,
	idempotencyCleanup *service.IdempotencyCleanupService,
	pricing *service.PricingService,
	emailQueue *service.EmailQueueService,
	billingCache *service.BillingCacheService,
	usageRecordWorkerPool *service.UsageRecordWorkerPool,
	subscriptionService *service.SubscriptionService,
	oauth *service.OAuthService,
	openaiOAuth *service.OpenAIOAuthService,
	geminiOAuth *service.GeminiOAuthService,
	antigravityOAuth *service.AntigravityOAuthService,
	openAIGateway *service.OpenAIGatewayService,
	scheduledTestRunner *service.ScheduledTestRunnerService,
	backupSvc *service.BackupService,
	paymentOrderExpiry *service.PaymentOrderExpiryService,
	channelMonitorRunner *service.ChannelMonitorRunner,
	quotaFlusher *service.UserPlatformQuotaUsageFlusher,
) func() {
	return func() {
		ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
		defer cancel()

		type cleanupStep struct {
			name string
			fn   func() error
		}

		// 应用层清理步骤可并行执行，基础设施资源（Redis/Ent）最后按顺序关闭。
		parallelSteps := []cleanupStep{
			{"OpsScheduledReportService", func() error {
				if opsScheduledReport != nil {
					opsScheduledReport.Stop()
				}
				return nil
			}},
			{"OpsCleanupService", func() error {
				if opsCleanup != nil {
					opsCleanup.Stop()
				}
				return nil
			}},
			{"OpsSystemLogSink", func() error {
				if opsSystemLogSink != nil {
					opsSystemLogSink.Stop()
				}
				return nil
			}},
			{"OpsAlertEvaluatorService", func() error {
				if opsAlertEvaluator != nil {
					opsAlertEvaluator.Stop()
				}
				return nil
			}},
			{"OpsAggregationService", func() error {
				if opsAggregation != nil {
					opsAggregation.Stop()
				}
				return nil
			}},
			{"OpsMetricsCollector", func() error {
				if opsMetricsCollector != nil {
					opsMetricsCollector.Stop()
				}
				return nil
			}},
			{"SchedulerSnapshotService", func() error {
				if schedulerSnapshot != nil {
					schedulerSnapshot.Stop()
				}
				return nil
			}},
			{"UsageCleanupService", func() error {
				if usageCleanup != nil {
					usageCleanup.Stop()
				}
				return nil
			}},
			{"IdempotencyCleanupService", func() error {
				if idempotencyCleanup != nil {
					idempotencyCleanup.Stop()
				}
				return nil
			}},
			{"TokenRefreshService", func() error {
				tokenRefresh.Stop()

exec
/bin/bash -lc "sed -n '1,220p' backend/cmd/server/wire_gen.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"context"
	"github.com/Wei-Shaw/sub2api/ent"
	"github.com/Wei-Shaw/sub2api/internal/config"
	"github.com/Wei-Shaw/sub2api/internal/handler"
	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
	"github.com/Wei-Shaw/sub2api/internal/payment"
	"github.com/Wei-Shaw/sub2api/internal/repository"
	"github.com/Wei-Shaw/sub2api/internal/server"
	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
	"github.com/Wei-Shaw/sub2api/internal/service"
	"github.com/redis/go-redis/v9"
	"log"
	"net/http"
	"sync"
	"time"
)

import (
	_ "embed"
	_ "github.com/Wei-Shaw/sub2api/ent/runtime"
)

// Injectors from wire.go:

func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
	configConfig, err := config.ProvideConfig()
	if err != nil {
		return nil, err
	}
	client, err := repository.ProvideEnt(configConfig)
	if err != nil {
		return nil, err
	}
	db, err := repository.ProvideSQLDB(client)
	if err != nil {
		return nil, err
	}
	userRepository := repository.NewUserRepository(client, db)
	redeemCodeRepository := repository.NewRedeemCodeRepository(client)
	redisClient := repository.ProvideRedis(configConfig)
	refreshTokenCache := repository.NewRefreshTokenCache(redisClient)
	settingRepository := repository.NewSettingRepository(client)
	groupRepository := repository.NewGroupRepository(client, db)
	proxyRepository := repository.NewProxyRepository(client, db)
	settingService := service.ProvideSettingService(settingRepository, groupRepository, proxyRepository, configConfig)
	emailCache := repository.NewEmailCache(redisClient)
	emailService := service.NewEmailService(settingRepository, emailCache)
	turnstileVerifier := repository.NewTurnstileVerifier()
	turnstileService := service.NewTurnstileService(settingService, turnstileVerifier)
	emailQueueService := service.ProvideEmailQueueService(emailService)
	promoCodeRepository := repository.NewPromoCodeRepository(client)
	billingCache := repository.NewBillingCache(redisClient)
	userSubscriptionRepository := repository.NewUserSubscriptionRepository(client)
	apiKeyRepository := repository.NewAPIKeyRepository(client, db)
	userRPMCache := repository.NewUserRPMCache(redisClient)
	userGroupRateRepository := repository.NewUserGroupRateRepository(db)
	userPlatformQuotaRepository := repository.NewUserPlatformQuotaRepository(client)
	serviceUserPlatformQuotaRepository := repository.NewUserPlatformQuotaServiceAdapter(userPlatformQuotaRepository)
	billingCacheService := service.ProvideBillingCacheService(billingCache, userRepository, userSubscriptionRepository, apiKeyRepository, userRPMCache, userGroupRateRepository, configConfig, serviceUserPlatformQuotaRepository)
	apiKeyCache := repository.NewAPIKeyCache(redisClient)
	apiKeyService := service.ProvideAPIKeyService(apiKeyRepository, userRepository, groupRepository, userSubscriptionRepository, userGroupRateRepository, apiKeyCache, configConfig, billingCacheService)
	apiKeyAuthCacheInvalidator := service.ProvideAPIKeyAuthCacheInvalidator(apiKeyService)
	promoService := service.NewPromoService(promoCodeRepository, userRepository, billingCacheService, client, apiKeyAuthCacheInvalidator)
	subscriptionService := service.NewSubscriptionService(groupRepository, userSubscriptionRepository, billingCacheService, client, configConfig)
	affiliateRepository := repository.NewAffiliateRepository(client, db)
	affiliateService := service.NewAffiliateService(affiliateRepository, settingService, apiKeyAuthCacheInvalidator, billingCacheService)
	authService := service.NewAuthService(client, userRepository, redeemCodeRepository, refreshTokenCache, configConfig, settingService, emailService, turnstileService, emailQueueService, promoService, subscriptionService, affiliateService, serviceUserPlatformQuotaRepository)
	userService := service.NewUserService(userRepository, settingRepository, apiKeyAuthCacheInvalidator, billingCache)
	redeemCache := repository.NewRedeemCache(redisClient)
	redeemService := service.NewRedeemService(redeemCodeRepository, userRepository, subscriptionService, redeemCache, billingCacheService, client, apiKeyAuthCacheInvalidator, affiliateService)
	secretEncryptor, err := repository.NewAESEncryptor(configConfig)
	if err != nil {
		return nil, err
	}
	totpCache := repository.NewTotpCache(redisClient)
	totpService := service.NewTotpService(userRepository, secretEncryptor, totpCache, settingService, emailService, emailQueueService)
	userAttributeDefinitionRepository := repository.NewUserAttributeDefinitionRepository(client)
	userAttributeValueRepository := repository.NewUserAttributeValueRepository(client)
	userAttributeService := service.NewUserAttributeService(userAttributeDefinitionRepository, userAttributeValueRepository)
	authHandler := handler.NewAuthHandler(configConfig, authService, userService, settingService, promoService, redeemService, totpService, userAttributeService)
	userHandler := handler.NewUserHandler(userService, authService, emailService, emailCache, affiliateService, serviceUserPlatformQuotaRepository)
	apiKeyHandler := handler.NewAPIKeyHandler(apiKeyService)
	usageLogRepository := repository.NewUsageLogRepository(client, db)
	usageService := service.NewUsageService(usageLogRepository, userRepository, client, apiKeyAuthCacheInvalidator)
	opsRepository := repository.NewOpsRepository(db)
	schedulerCache := repository.ProvideSchedulerCache(redisClient, configConfig)
	accountRepository := repository.NewAccountRepository(client, db, schedulerCache)
	concurrencyCache := repository.ProvideConcurrencyCache(redisClient, configConfig)
	concurrencyService := service.ProvideConcurrencyService(concurrencyCache, accountRepository, configConfig)
	usageBillingRepository := repository.NewUsageBillingRepository(client, db)
	gatewayCache := repository.NewGatewayCache(redisClient)
	schedulerOutboxRepository := repository.NewSchedulerOutboxRepository(db)
	schedulerSnapshotService := service.ProvideSchedulerSnapshotService(schedulerCache, schedulerOutboxRepository, accountRepository, groupRepository, configConfig)
	pricingRemoteClient := repository.ProvidePricingRemoteClient(configConfig)
	pricingService, err := service.ProvidePricingService(configConfig, pricingRemoteClient)
	if err != nil {
		return nil, err
	}
	billingService := service.NewBillingService(configConfig, pricingService)
	geminiQuotaService := service.NewGeminiQuotaService(configConfig, settingRepository)
	tempUnschedCache := repository.NewTempUnschedCache(redisClient)
	timeoutCounterCache := repository.NewTimeoutCounterCache(redisClient)
	openAI403CounterCache := repository.NewOpenAI403CounterCache(redisClient)
	geminiTokenCache := repository.NewGeminiTokenCache(redisClient)
	compositeTokenCacheInvalidator := service.NewCompositeTokenCacheInvalidator(geminiTokenCache)
	rateLimitService := service.ProvideRateLimitService(accountRepository, usageLogRepository, configConfig, geminiQuotaService, tempUnschedCache, timeoutCounterCache, openAI403CounterCache, settingService, compositeTokenCacheInvalidator)
	identityCache := repository.NewIdentityCache(redisClient)
	identityService := service.NewIdentityService(identityCache)
	httpUpstream := repository.NewHTTPUpstream(configConfig)
	timingWheelService, err := service.ProvideTimingWheelService()
	if err != nil {
		return nil, err
	}
	deferredService := service.ProvideDeferredService(accountRepository, timingWheelService)
	claudeOAuthClient := repository.NewClaudeOAuthClient()
	oAuthService := service.NewOAuthService(proxyRepository, claudeOAuthClient)
	oAuthRefreshAPI := service.ProvideOAuthRefreshAPI(accountRepository, geminiTokenCache)
	claudeTokenProvider := service.ProvideClaudeTokenProvider(accountRepository, geminiTokenCache, oAuthService, oAuthRefreshAPI)
	sessionLimitCache := repository.ProvideSessionLimitCache(redisClient, configConfig)
	rpmCache := repository.NewRPMCache(redisClient)
	digestSessionStore := service.NewDigestSessionStore()
	tlsFingerprintProfileRepository := repository.NewTLSFingerprintProfileRepository(client)
	tlsFingerprintProfileCache := repository.NewTLSFingerprintProfileCache(redisClient)
	tlsFingerprintProfileService := service.NewTLSFingerprintProfileService(tlsFingerprintProfileRepository, tlsFingerprintProfileCache)
	channelRepository := repository.NewChannelRepository(db)
	channelService := service.NewChannelService(channelRepository, groupRepository, apiKeyAuthCacheInvalidator, pricingService)
	modelPricingResolver := service.NewModelPricingResolver(channelService, billingService)
	notificationEmailService := service.NewNotificationEmailService(settingRepository, emailService)
	balanceNotifyService := service.ProvideBalanceNotifyService(emailService, settingRepository, accountRepository, notificationEmailService)
	gatewayService := service.NewGatewayService(accountRepository, groupRepository, usageLogRepository, usageBillingRepository, userRepository, userSubscriptionRepository, userGroupRateRepository, gatewayCache, configConfig, schedulerSnapshotService, concurrencyService, billingService, rateLimitService, billingCacheService, identityService, httpUpstream, deferredService, claudeTokenProvider, sessionLimitCache, rpmCache, digestSessionStore, settingService, tlsFingerprintProfileService, channelService, modelPricingResolver, balanceNotifyService, serviceUserPlatformQuotaRepository)
	openAIOAuthClient := repository.NewOpenAIOAuthClient()
	privacyClientFactory := providePrivacyClientFactory()
	openAIOAuthService := service.ProvideOpenAIOAuthService(proxyRepository, openAIOAuthClient, privacyClientFactory)
	openAITokenProvider := service.ProvideOpenAITokenProvider(accountRepository, geminiTokenCache, openAIOAuthService, oAuthRefreshAPI)
	openAIGatewayService := service.NewOpenAIGatewayService(accountRepository, usageLogRepository, usageBillingRepository, userRepository, userSubscriptionRepository, userGroupRateRepository, gatewayCache, configConfig, schedulerSnapshotService, concurrencyService, billingService, rateLimitService, billingCacheService, httpUpstream, deferredService, openAITokenProvider, modelPricingResolver, channelService, balanceNotifyService, settingService, serviceUserPlatformQuotaRepository)
	geminiOAuthClient := repository.NewGeminiOAuthClient(configConfig)
	geminiCliCodeAssistClient := repository.NewGeminiCliCodeAssistClient()
	driveClient := repository.NewGeminiDriveClient()
	geminiOAuthService := service.NewGeminiOAuthService(proxyRepository, geminiOAuthClient, geminiCliCodeAssistClient, driveClient, configConfig)
	geminiTokenProvider := service.ProvideGeminiTokenProvider(accountRepository, geminiTokenCache, geminiOAuthService, oAuthRefreshAPI)
	antigravityOAuthService := service.NewAntigravityOAuthService(proxyRepository)
	antigravityTokenProvider := service.ProvideAntigravityTokenProvider(accountRepository, geminiTokenCache, antigravityOAuthService, oAuthRefreshAPI, tempUnschedCache)
	internal500CounterCache := repository.NewInternal500CounterCache(redisClient)
	antigravityGatewayService := service.NewAntigravityGatewayService(accountRepository, gatewayCache, schedulerSnapshotService, antigravityTokenProvider, rateLimitService, httpUpstream, settingService, internal500CounterCache)
	geminiMessagesCompatService := service.NewGeminiMessagesCompatService(accountRepository, groupRepository, gatewayCache, schedulerSnapshotService, geminiTokenProvider, rateLimitService, httpUpstream, antigravityGatewayService, configConfig)
	opsSystemLogSink := service.ProvideOpsSystemLogSink(opsRepository)
	opsService := service.ProvideOpsService(opsRepository, settingRepository, configConfig, accountRepository, userRepository, concurrencyService, gatewayService, openAIGatewayService, geminiMessagesCompatService, antigravityGatewayService, opsSystemLogSink, settingService)
	usageHandler := handler.NewUsageHandler(usageService, apiKeyService, opsService, settingService)
	redeemHandler := handler.NewRedeemHandler(redeemService)
	subscriptionHandler := handler.NewSubscriptionHandler(subscriptionService)
	announcementRepository := repository.NewAnnouncementRepository(client)
	announcementReadRepository := repository.NewAnnouncementReadRepository(client)
	announcementService := service.NewAnnouncementService(announcementRepository, announcementReadRepository, userRepository, userSubscriptionRepository)
	announcementHandler := handler.NewAnnouncementHandler(announcementService)
	channelMonitorRepository := repository.NewChannelMonitorRepository(client, db)
	channelMonitorService := service.ProvideChannelMonitorService(channelMonitorRepository, secretEncryptor)
	channelMonitorUserHandler := handler.NewChannelMonitorUserHandler(channelMonitorService, settingService)
	dashboardAggregationRepository := repository.NewDashboardAggregationRepository(db)
	dashboardStatsCache := repository.NewDashboardCache(redisClient, configConfig)
	dashboardService := service.NewDashboardService(usageLogRepository, dashboardAggregationRepository, dashboardStatsCache, configConfig)
	leaderLockCache := repository.NewLeaderLockCache(redisClient)
	dashboardAggregationService := service.ProvideDashboardAggregationService(dashboardAggregationRepository, timingWheelService, leaderLockCache, db, configConfig)
	dashboardHandler := admin.NewDashboardHandler(dashboardService, dashboardAggregationService)
	proxyExitInfoProber := repository.NewProxyExitInfoProber(configConfig)
	proxyLatencyCache := repository.NewProxyLatencyCache(redisClient)
	adminService := service.NewAdminService(userRepository, groupRepository, accountRepository, proxyRepository, apiKeyRepository, redeemCodeRepository, userGroupRateRepository, userRPMCache, billingCacheService, proxyExitInfoProber, proxyLatencyCache, apiKeyAuthCacheInvalidator, client, settingService, configConfig, subscriptionService, userSubscriptionRepository, privacyClientFactory, openAIGatewayService)
	adminUserHandler := admin.NewUserHandler(adminService, concurrencyService, serviceUserPlatformQuotaRepository, billingCache)
	groupCapacityService := service.NewGroupCapacityService(accountRepository, groupRepository, concurrencyService, sessionLimitCache, rpmCache)
	groupHandler := admin.NewGroupHandler(adminService, dashboardService, groupCapacityService)
	claudeUsageFetcher := repository.NewClaudeUsageFetcher(httpUpstream)
	antigravityQuotaFetcher := service.NewAntigravityQuotaFetcher(proxyRepository)
	usageCache := service.NewUsageCache()
	accountUsageService := service.NewAccountUsageService(accountRepository, usageLogRepository, claudeUsageFetcher, geminiQuotaService, antigravityQuotaFetcher, usageCache, identityCache, tlsFingerprintProfileService)
	accountTestService := service.NewAccountTestService(accountRepository, geminiTokenProvider, claudeTokenProvider, antigravityGatewayService, httpUpstream, configConfig, tlsFingerprintProfileService)
	crsSyncService := service.NewCRSSyncService(accountRepository, proxyRepository, oAuthService, openAIOAuthService, geminiOAuthService, configConfig)
	accountHandler := admin.NewAccountHandler(adminService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, rateLimitService, accountUsageService, accountTestService, concurrencyService, crsSyncService, sessionLimitCache, rpmCache, compositeTokenCacheInvalidator)
	adminAnnouncementHandler := admin.NewAnnouncementHandler(announcementService)
	dataManagementService := service.NewDataManagementService()
	dataManagementHandler := admin.NewDataManagementHandler(dataManagementService)
	backupObjectStoreFactory := repository.NewS3BackupStoreFactory()
	dbDumper := repository.NewPgDumper(configConfig)
	backupService := service.ProvideBackupService(settingRepository, configConfig, secretEncryptor, backupObjectStoreFactory, dbDumper)
	backupHandler := admin.NewBackupHandler(backupService, userService)
	oAuthHandler := admin.NewOAuthHandler(oAuthService)
	openAIQuotaService := service.ProvideOpenAIQuotaService(accountRepository, proxyRepository, openAITokenProvider, privacyClientFactory)
	openAIOAuthHandler := admin.NewOpenAIOAuthHandler(openAIOAuthService, adminService, openAIQuotaService)
	geminiOAuthHandler := admin.NewGeminiOAuthHandler(geminiOAuthService)
	antigravityOAuthHandler := admin.NewAntigravityOAuthHandler(antigravityOAuthService)
	proxyHandler := admin.NewProxyHandler(adminService)
	adminRedeemHandler := admin.NewRedeemHandler(adminService, redeemService)
	promoHandler := admin.NewPromoHandler(promoService)
	encryptionKey, err := payment.ProvideEncryptionKey(configConfig)
	if err != nil {
		return nil, err
	}
	paymentConfigService := service.ProvidePaymentConfigService(client, settingRepository, encryptionKey)
	registry := payment.ProvideRegistry()
	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
	opsHandler := admin.NewOpsHandler(opsService)
	updateCache := repository.NewUpdateCache(redisClient)
	gitHubReleaseClient := repository.ProvideGitHubReleaseClient(configConfig)
	serviceBuildInfo := provideServiceBuildInfo(buildInfo)
	updateService := service.ProvideUpdateService(updateCache, gitHubReleaseClient, serviceBuildInfo)
	idempotencyRepository := repository.NewIdempotencyRepository(client, db)
	systemOperationLockService := service.ProvideSystemOperationLockService(idempotencyRepository, configConfig)
	systemHandler := handler.ProvideSystemHandler(updateService, systemOperationLockService)
	adminSubscriptionHandler := admin.NewSubscriptionHandler(subscriptionService)
	usageCleanupRepository := repository.NewUsageCleanupRepository(client, db)
	usageCleanupService := service.ProvideUsageCleanupService(usageCleanupRepository, timingWheelService, dashboardAggregationService, configConfig)

codex
Wire 这里需要注意：项目提交了生成后的 `wire_gen.go`，本环境不一定有 wire 命令。若无法生成，我会手工同步生成文件，保证 `go build` 可用。
exec
/bin/bash -lc "sed -n '688,850p' backend/internal/config/config.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
type GatewayConfig struct {
	// 等待上游响应头的超时时间（秒），0表示无超时
	// 注意：这不影响流式数据传输，只控制等待响应头的时间
	ResponseHeaderTimeout int `mapstructure:"response_header_timeout"`
	// OpenAIResponseHeaderTimeout: OpenAI/Codex 上游等待响应头的超时时间（秒），0表示无超时
	// OpenAI/Codex 请求可能在上游排队较久；默认不使用通用响应头超时截断。
	OpenAIResponseHeaderTimeout int `mapstructure:"openai_response_header_timeout"`
	// 请求体最大字节数，用于网关请求体大小限制
	MaxBodySize int64 `mapstructure:"max_body_size"`
	// 非流式上游响应体读取上限（字节），用于防止无界读取导致内存放大
	UpstreamResponseReadMaxBytes int64 `mapstructure:"upstream_response_read_max_bytes"`
	// 代理探测响应体读取上限（字节）
	ProxyProbeResponseReadMaxBytes int64 `mapstructure:"proxy_probe_response_read_max_bytes"`
	// Gemini 上游响应头调试日志开关（默认关闭，避免高频日志开销）
	GeminiDebugResponseHeaders bool `mapstructure:"gemini_debug_response_headers"`
	// ConnectionPoolIsolation: 上游连接池隔离策略（proxy/account/account_proxy）
	ConnectionPoolIsolation string `mapstructure:"connection_pool_isolation"`
	// ForceCodexCLI: 强制将 OpenAI `/v1/responses` 请求按 Codex CLI 处理。
	// 用于网关未透传/改写 User-Agent 时的兼容兜底（默认关闭，避免影响其他客户端）。
	ForceCodexCLI bool `mapstructure:"force_codex_cli"`
	// CodexImageGenerationBridgeEnabled: 是否为 Codex `/v1/responses` 自动注入 image_generation 工具和桥接指令。
	// 默认关闭，避免纯文本 Codex 请求被意外改写；显式携带 image_generation 工具的请求仍按分组能力转发。
	CodexImageGenerationBridgeEnabled bool `mapstructure:"codex_image_generation_bridge_enabled"`
	// ForcedCodexInstructionsTemplateFile: 服务端强制附加到 Codex 顶层 instructions 的模板文件路径。
	// 模板渲染后会直接覆盖最终 instructions；若需要保留客户端 system 转换结果，请在模板中显式引用 {{ .ExistingInstructions }}。
	ForcedCodexInstructionsTemplateFile string `mapstructure:"forced_codex_instructions_template_file"`
	// ForcedCodexInstructionsTemplate: 启动时从模板文件读取并缓存的模板内容。
	// 该字段不直接参与配置反序列化，仅用于请求热路径避免重复读盘。
	ForcedCodexInstructionsTemplate string `mapstructure:"-"`
	// OpenAIPassthroughAllowTimeoutHeaders: OpenAI 透传模式是否放行客户端超时头
	// 关闭（默认）可避免 x-stainless-timeout 等头导致上游提前断流。
	OpenAIPassthroughAllowTimeoutHeaders bool `mapstructure:"openai_passthrough_allow_timeout_headers"`
	// OpenAIWS: OpenAI Responses WebSocket 配置（默认开启，可按需回滚到 HTTP）
	OpenAIWS GatewayOpenAIWSConfig `mapstructure:"openai_ws"`
	// OpenAIScheduler: OpenAI 高级调度器粘性逃逸配置
	OpenAIScheduler GatewayOpenAISchedulerConfig `mapstructure:"openai_scheduler"`
	// OpenAIHTTP2: OpenAI HTTP 上游协议策略（默认启用 HTTP/2，可按代理能力回退 HTTP/1.1）
	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`

	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
	// MaxIdleConns: 所有主机的最大空闲连接总数
	MaxIdleConns int `mapstructure:"max_idle_conns"`
	// MaxIdleConnsPerHost: 每个主机的最大空闲连接数（关键参数，影响连接复用率）
	MaxIdleConnsPerHost int `mapstructure:"max_idle_conns_per_host"`
	// MaxConnsPerHost: 每个主机的最大连接数（包括活跃+空闲），0表示无限制
	MaxConnsPerHost int `mapstructure:"max_conns_per_host"`
	// IdleConnTimeoutSeconds: 空闲连接超时时间（秒）
	IdleConnTimeoutSeconds int `mapstructure:"idle_conn_timeout_seconds"`
	// MaxUpstreamClients: 上游连接池客户端最大缓存数量
	// 当使用连接池隔离策略时，系统会为不同的账户/代理组合创建独立的 HTTP 客户端
	// 此参数限制缓存的客户端数量，超出后会淘汰最久未使用的客户端
	// 建议值：预估的活跃账户数 * 1.2（留有余量）
	MaxUpstreamClients int `mapstructure:"max_upstream_clients"`
	// ClientIdleTTLSeconds: 上游连接池客户端空闲回收阈值（秒）
	// 超过此时间未使用的客户端会被标记为可回收
	// 建议值：根据用户访问频率设置，一般 10-30 分钟
	ClientIdleTTLSeconds int `mapstructure:"client_idle_ttl_seconds"`
	// ConcurrencySlotTTLMinutes: 并发槽位过期时间（分钟）
	// 应大于最长 LLM 请求时间，防止请求完成前槽位过期
	ConcurrencySlotTTLMinutes int `mapstructure:"concurrency_slot_ttl_minutes"`
	// SessionIdleTimeoutMinutes: 会话空闲超时时间（分钟），默认 5 分钟
	// 用于 Anthropic OAuth/SetupToken 账号的会话数量限制功能
	// 空闲超过此时间的会话将被自动释放
	SessionIdleTimeoutMinutes int `mapstructure:"session_idle_timeout_minutes"`

	// StreamDataIntervalTimeout: 流数据间隔超时（秒），0表示禁用
	StreamDataIntervalTimeout int `mapstructure:"stream_data_interval_timeout"`
	// StreamKeepaliveInterval: 流式 keepalive 间隔（秒），0表示禁用
	StreamKeepaliveInterval int `mapstructure:"stream_keepalive_interval"`
	// ImageStreamDataIntervalTimeout: 图片流数据间隔超时（秒），0表示禁用
	ImageStreamDataIntervalTimeout int `mapstructure:"image_stream_data_interval_timeout"`
	// ImageStreamKeepaliveInterval: 图片流式 keepalive 间隔（秒），0表示禁用
	ImageStreamKeepaliveInterval int `mapstructure:"image_stream_keepalive_interval"`
	// MaxLineSize: 上游 SSE 单行最大字节数（0使用默认值）
	MaxLineSize int `mapstructure:"max_line_size"`

	// 是否记录上游错误响应体摘要（避免输出请求内容）
	LogUpstreamErrorBody bool `mapstructure:"log_upstream_error_body"`
	// 上游错误响应体记录最大字节数（超过会截断）
	LogUpstreamErrorBodyMaxBytes int `mapstructure:"log_upstream_error_body_max_bytes"`

	// API-key 账号在客户端未提供 anthropic-beta 时，是否按需自动补齐（默认关闭以保持兼容）
	InjectBetaForAPIKey bool `mapstructure:"inject_beta_for_apikey"`

	// 是否允许对部分 400 错误触发 failover（默认关闭以避免改变语义）
	FailoverOn400 bool `mapstructure:"failover_on_400"`

	// 账户切换最大次数（遇到上游错误时切换到其他账户的次数上限）
	MaxAccountSwitches int `mapstructure:"max_account_switches"`
	// Gemini 账户切换最大次数（Gemini 平台单独配置，因 API 限制更严格）
	MaxAccountSwitchesGemini int `mapstructure:"max_account_switches_gemini"`

	// Antigravity 429 fallback 限流时间（分钟），解析重置时间失败时使用
	AntigravityFallbackCooldownMinutes int `mapstructure:"antigravity_fallback_cooldown_minutes"`

	// Scheduling: 账号调度相关配置
	Scheduling GatewaySchedulingConfig `mapstructure:"scheduling"`

	// TLSFingerprint: TLS指纹伪装配置
	TLSFingerprint TLSFingerprintConfig `mapstructure:"tls_fingerprint"`

	// UsageRecord: 使用量记录异步队列配置（有界队列 + 固定 worker）
	UsageRecord GatewayUsageRecordConfig `mapstructure:"usage_record"`

	// UserGroupRateCacheTTLSeconds: 用户分组倍率热路径缓存 TTL（秒）
	UserGroupRateCacheTTLSeconds int `mapstructure:"user_group_rate_cache_ttl_seconds"`
	// ModelsListCacheTTLSeconds: /v1/models 模型列表短缓存 TTL（秒）
	ModelsListCacheTTLSeconds int `mapstructure:"models_list_cache_ttl_seconds"`

	// UserMessageQueue: 用户消息串行队列配置
	// 对 role:"user" 的真实用户消息实施账号级串行化 + RPM 自适应延迟
	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
}

// GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
// 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
type GatewayOpenAIHTTP2Config struct {
	// Enabled: 是否启用 OpenAI HTTP/2 优先策略
	Enabled bool `mapstructure:"enabled"`
	// AllowProxyFallbackToHTTP1: HTTP/HTTPS 代理出现明确 H2 兼容错误时，临时回退 HTTP/1.1
	AllowProxyFallbackToHTTP1 bool `mapstructure:"allow_proxy_fallback_to_http1"`
	// FallbackErrorThreshold: 回退窗口内累计多少次兼容错误后触发回退
	FallbackErrorThreshold int `mapstructure:"fallback_error_threshold"`
	// FallbackWindowSeconds: 统计兼容错误的时间窗口（秒）
	FallbackWindowSeconds int `mapstructure:"fallback_window_seconds"`
	// FallbackTTLSeconds: 触发后回退 HTTP/1.1 的持续时间（秒）
	FallbackTTLSeconds int `mapstructure:"fallback_ttl_seconds"`
}

// UserMessageQueueConfig 用户消息串行队列配置
// 用于 Anthropic OAuth/SetupToken 账号的用户消息串行化发送
type UserMessageQueueConfig struct {
	// Mode: 模式选择
	// "serialize" = 账号级串行锁 + RPM 自适应延迟
	// "throttle" = 仅 RPM 自适应前置延迟，不阻塞并发
	// "" = 禁用（默认）
	Mode string `mapstructure:"mode"`
	// Enabled: 已废弃，仅向后兼容（等同于 mode: "serialize"）
	Enabled bool `mapstructure:"enabled"`
	// LockTTLMs: 串行锁 TTL（毫秒），应大于最长请求时间
	LockTTLMs int `mapstructure:"lock_ttl_ms"`
	// WaitTimeoutMs: 等待获取锁的超时时间（毫秒）
	WaitTimeoutMs int `mapstructure:"wait_timeout_ms"`
	// MinDelayMs: RPM 自适应延迟下限（毫秒）
	MinDelayMs int `mapstructure:"min_delay_ms"`
	// MaxDelayMs: RPM 自适应延迟上限（毫秒）
	MaxDelayMs int `mapstructure:"max_delay_ms"`
	// CleanupIntervalSeconds: 孤儿锁清理间隔（秒），0 表示禁用
	CleanupIntervalSeconds int `mapstructure:"cleanup_interval_seconds"`
}

// WaitTimeout 返回等待超时的 time.Duration
func (c *UserMessageQueueConfig) WaitTimeout() time.Duration {
	if c.WaitTimeoutMs <= 0 {
		return 30 * time.Second
	}
	return time.Duration(c.WaitTimeoutMs) * time.Millisecond
}

// GetEffectiveMode 返回生效的模式
// 注意：Mode 字段已在 load() 中做过白名单校验和规范化，此处无需重复验证

exec
/bin/bash -lc "sed -n '220,520p' backend/cmd/server/wire_gen.go" in /mnt/x/project/sub2api
exec
/bin/bash -lc "sed -n '220,520p' backend/cmd/server/wire.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
			}},
			{"AntigravityOAuthService", func() error {
				antigravityOAuth.Stop()
				return nil
			}},
			{"OpenAIWSPool", func() error {
				if openAIGateway != nil {
					openAIGateway.CloseOpenAIWSPool()
				}
				return nil
			}},
			{"ScheduledTestRunnerService", func() error {
				if scheduledTestRunner != nil {
					scheduledTestRunner.Stop()
				}
				return nil
			}},
			{"BackupService", func() error {
				if backupSvc != nil {
					backupSvc.Stop()
				}
				return nil
			}},
			{"PaymentOrderExpiryService", func() error {
				if paymentOrderExpiry != nil {
					paymentOrderExpiry.Stop()
				}
				return nil
			}},
			{"ChannelMonitorRunner", func() error {
				if channelMonitorRunner != nil {
					channelMonitorRunner.Stop()
				}
				return nil
			}},
			{"UserPlatformQuotaUsageFlusher", func() error {
				if quotaFlusher != nil {
					quotaFlusher.Stop()
				}
				return nil
			}},
		}

		infraSteps := []cleanupStep{
			{"Redis", func() error {
				if rdb == nil {
					return nil
				}
				return rdb.Close()
			}},
			{"Ent", func() error {
				if entClient == nil {
					return nil
				}
				return entClient.Close()
			}},
		}

		runParallel := func(steps []cleanupStep) {
			var wg sync.WaitGroup
			for i := range steps {
				step := steps[i]
				wg.Add(1)
				go func() {
					defer wg.Done()
					if err := step.fn(); err != nil {
						log.Printf("[Cleanup] %s failed: %v", step.name, err)
						return
					}
					log.Printf("[Cleanup] %s succeeded", step.name)
				}()
			}
			wg.Wait()
		}

		runSequential := func(steps []cleanupStep) {
			for i := range steps {
				step := steps[i]
				if err := step.fn(); err != nil {
					log.Printf("[Cleanup] %s failed: %v", step.name, err)
					continue
				}
				log.Printf("[Cleanup] %s succeeded", step.name)
			}
		}

		runParallel(parallelSteps)
		runSequential(infraSteps)

		// Check if context timed out
		select {
		case <-ctx.Done():
			log.Printf("[Cleanup] Warning: cleanup timed out after 10 seconds")
		default:
			log.Printf("[Cleanup] All cleanup steps completed")
		}
	}
}

 succeeded in 0ms:
	usageCleanupService := service.ProvideUsageCleanupService(usageCleanupRepository, timingWheelService, dashboardAggregationService, configConfig)
	adminUsageHandler := admin.NewUsageHandler(usageService, apiKeyService, adminService, usageCleanupService)
	userAttributeHandler := admin.NewUserAttributeHandler(userAttributeService)
	errorPassthroughRepository := repository.NewErrorPassthroughRepository(client)
	errorPassthroughCache := repository.NewErrorPassthroughCache(redisClient)
	errorPassthroughService := service.NewErrorPassthroughService(errorPassthroughRepository, errorPassthroughCache)
	errorPassthroughHandler := admin.NewErrorPassthroughHandler(errorPassthroughService)
	tlsFingerprintProfileHandler := admin.NewTLSFingerprintProfileHandler(tlsFingerprintProfileService)
	adminAPIKeyHandler := admin.NewAdminAPIKeyHandler(adminService)
	scheduledTestPlanRepository := repository.NewScheduledTestPlanRepository(db)
	scheduledTestResultRepository := repository.NewScheduledTestResultRepository(db)
	scheduledTestService := service.ProvideScheduledTestService(scheduledTestPlanRepository, scheduledTestResultRepository)
	scheduledTestHandler := admin.NewScheduledTestHandler(scheduledTestService)
	channelHandler := admin.NewChannelHandler(channelService, billingService, pricingService)
	channelMonitorHandler := admin.NewChannelMonitorHandler(channelMonitorService)
	channelMonitorRequestTemplateRepository := repository.NewChannelMonitorRequestTemplateRepository(client, db)
	channelMonitorRequestTemplateService := service.NewChannelMonitorRequestTemplateService(channelMonitorRequestTemplateRepository)
	channelMonitorRequestTemplateHandler := admin.NewChannelMonitorRequestTemplateHandler(channelMonitorRequestTemplateService)
	contentModerationRepository := repository.NewContentModerationRepository(db)
	contentModerationHashCache := repository.NewContentModerationHashCache(redisClient)
	contentModerationService := service.NewContentModerationService(settingRepository, contentModerationRepository, contentModerationHashCache, groupRepository, userRepository, apiKeyAuthCacheInvalidator, emailService)
	contentModerationHandler := admin.NewContentModerationHandler(contentModerationService)
	paymentHandler := admin.NewPaymentHandler(paymentService, paymentConfigService)
	affiliateHandler := admin.NewAffiliateHandler(affiliateService, adminService)
	complianceHandler := admin.NewComplianceHandler(settingService)
	adminHandlers := handler.ProvideAdminHandlers(dashboardHandler, adminUserHandler, groupHandler, accountHandler, adminAnnouncementHandler, dataManagementHandler, backupHandler, oAuthHandler, openAIOAuthHandler, geminiOAuthHandler, antigravityOAuthHandler, proxyHandler, adminRedeemHandler, promoHandler, settingHandler, opsHandler, systemHandler, adminSubscriptionHandler, adminUsageHandler, userAttributeHandler, errorPassthroughHandler, tlsFingerprintProfileHandler, adminAPIKeyHandler, scheduledTestHandler, channelHandler, channelMonitorHandler, channelMonitorRequestTemplateHandler, contentModerationHandler, paymentHandler, affiliateHandler, complianceHandler)
	usageRecordWorkerPool := service.NewUsageRecordWorkerPool(configConfig)
	userMsgQueueCache := repository.NewUserMsgQueueCache(redisClient)
	userMessageQueueService := service.ProvideUserMessageQueueService(userMsgQueueCache, rpmCache, configConfig)
	gatewayHandler := handler.NewGatewayHandler(gatewayService, geminiMessagesCompatService, antigravityGatewayService, userService, concurrencyService, billingCacheService, usageService, apiKeyService, usageRecordWorkerPool, errorPassthroughService, contentModerationService, userMessageQueueService, configConfig, settingService)
	openAIGatewayHandler := handler.NewOpenAIGatewayHandler(openAIGatewayService, concurrencyService, billingCacheService, apiKeyService, usageRecordWorkerPool, errorPassthroughService, contentModerationService, opsService, configConfig)
	handlerSettingHandler := handler.ProvideSettingHandler(settingService, buildInfo, notificationEmailService)
	totpHandler := handler.NewTotpHandler(totpService)
	handlerPaymentHandler := handler.NewPaymentHandler(paymentService, paymentConfigService, channelService)
	paymentWebhookHandler := handler.NewPaymentWebhookHandler(paymentService, registry)
	availableChannelHandler := handler.NewAvailableChannelHandler(channelService, apiKeyService, settingService)
	idempotencyCoordinator := service.ProvideIdempotencyCoordinator(idempotencyRepository, configConfig)
	idempotencyCleanupService := service.ProvideIdempotencyCleanupService(idempotencyRepository, configConfig)
	handlers := handler.ProvideHandlers(authHandler, userHandler, apiKeyHandler, usageHandler, redeemHandler, subscriptionHandler, announcementHandler, channelMonitorUserHandler, adminHandlers, gatewayHandler, openAIGatewayHandler, handlerSettingHandler, totpHandler, handlerPaymentHandler, paymentWebhookHandler, availableChannelHandler, idempotencyCoordinator, idempotencyCleanupService)
	jwtAuthMiddleware := middleware.NewJWTAuthMiddleware(authService, userService)
	adminAuthMiddleware := middleware.NewAdminAuthMiddleware(authService, userService, settingService)
	apiKeyAuthMiddleware := middleware.NewAPIKeyAuthMiddleware(apiKeyService, subscriptionService, configConfig)
	engine := server.ProvideRouter(configConfig, handlers, jwtAuthMiddleware, adminAuthMiddleware, apiKeyAuthMiddleware, apiKeyService, subscriptionService, opsService, settingService, redisClient)
	httpServer := server.ProvideHTTPServer(configConfig, engine)
	opsMetricsCollector := service.ProvideOpsMetricsCollector(opsRepository, settingRepository, accountRepository, concurrencyService, db, redisClient, configConfig)
	opsAggregationService := service.ProvideOpsAggregationService(opsRepository, settingRepository, db, redisClient, configConfig)
	opsAlertEvaluatorService := service.ProvideOpsAlertEvaluatorService(opsService, opsRepository, emailService, redisClient, configConfig, proxyRepository)
	opsCleanupService := service.ProvideOpsCleanupService(opsRepository, db, redisClient, configConfig, channelMonitorService, settingRepository, opsService)
	opsScheduledReportService := service.ProvideOpsScheduledReportService(opsService, userService, emailService, redisClient, configConfig)
	tokenRefreshService := service.ProvideTokenRefreshService(accountRepository, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, compositeTokenCacheInvalidator, schedulerCache, configConfig, tempUnschedCache, privacyClientFactory, proxyRepository, oAuthRefreshAPI, openAIGatewayService)
	accountExpiryService := service.ProvideAccountExpiryService(accountRepository)
	proxyExpiryService := service.ProvideProxyExpiryService(proxyRepository)
	subscriptionExpiryService := service.ProvideSubscriptionExpiryService(userSubscriptionRepository, settingRepository, notificationEmailService, leaderLockCache, db)
	scheduledTestRunnerService := service.ProvideScheduledTestRunnerService(scheduledTestPlanRepository, scheduledTestService, accountTestService, rateLimitService, configConfig)
	paymentOrderExpiryService := service.ProvidePaymentOrderExpiryService(paymentService, leaderLockCache, db)
	channelMonitorRunner := service.ProvideChannelMonitorRunner(channelMonitorService, settingService)
	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
	application := &Application{
		Server:  httpServer,
		Cleanup: v,
	}
	return application, nil
}

// wire.go:

type Application struct {
	Server  *http.Server
	Cleanup func()
}

func providePrivacyClientFactory() service.PrivacyClientFactory {
	return repository.CreatePrivacyReqClient
}

func provideServiceBuildInfo(buildInfo handler.BuildInfo) service.BuildInfo {
	return service.BuildInfo{
		Version:   buildInfo.Version,
		BuildType: buildInfo.BuildType,
	}
}

func provideCleanup(
	entClient *ent.Client,
	rdb *redis.Client,
	opsMetricsCollector *service.OpsMetricsCollector,
	opsAggregation *service.OpsAggregationService,
	opsAlertEvaluator *service.OpsAlertEvaluatorService,
	opsCleanup *service.OpsCleanupService,
	opsScheduledReport *service.OpsScheduledReportService,
	opsSystemLogSink *service.OpsSystemLogSink,
	schedulerSnapshot *service.SchedulerSnapshotService,
	tokenRefresh *service.TokenRefreshService,
	accountExpiry *service.AccountExpiryService,
	proxyExpiry *service.ProxyExpiryService,
	subscriptionExpiry *service.SubscriptionExpiryService,
	usageCleanup *service.UsageCleanupService,
	idempotencyCleanup *service.IdempotencyCleanupService,
	pricing *service.PricingService,
	emailQueue *service.EmailQueueService,
	billingCache *service.BillingCacheService,
	usageRecordWorkerPool *service.UsageRecordWorkerPool,
	subscriptionService *service.SubscriptionService,
	oauth *service.OAuthService,
	openaiOAuth *service.OpenAIOAuthService,
	geminiOAuth *service.GeminiOAuthService,
	antigravityOAuth *service.AntigravityOAuthService,
	openAIGateway *service.OpenAIGatewayService,
	scheduledTestRunner *service.ScheduledTestRunnerService,
	backupSvc *service.BackupService,
	paymentOrderExpiry *service.PaymentOrderExpiryService,
	channelMonitorRunner *service.ChannelMonitorRunner,
	quotaFlusher *service.UserPlatformQuotaUsageFlusher,
) func() {
	return func() {
		ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
		defer cancel()

		type cleanupStep struct {
			name string
			fn   func() error
		}

		parallelSteps := []cleanupStep{
			{"OpsScheduledReportService", func() error {
				if opsScheduledReport != nil {
					opsScheduledReport.Stop()
				}
				return nil
			}},
			{"OpsCleanupService", func() error {
				if opsCleanup != nil {
					opsCleanup.Stop()
				}
				return nil
			}},
			{"OpsSystemLogSink", func() error {
				if opsSystemLogSink != nil {
					opsSystemLogSink.Stop()
				}
				return nil
			}},
			{"OpsAlertEvaluatorService", func() error {
				if opsAlertEvaluator != nil {
					opsAlertEvaluator.Stop()
				}
				return nil
			}},
			{"OpsAggregationService", func() error {
				if opsAggregation != nil {
					opsAggregation.Stop()
				}
				return nil
			}},
			{"OpsMetricsCollector", func() error {
				if opsMetricsCollector != nil {
					opsMetricsCollector.Stop()
				}
				return nil
			}},
			{"SchedulerSnapshotService", func() error {
				if schedulerSnapshot != nil {
					schedulerSnapshot.Stop()
				}
				return nil
			}},
			{"UsageCleanupService", func() error {
				if usageCleanup != nil {
					usageCleanup.Stop()
				}
				return nil
			}},
			{"IdempotencyCleanupService", func() error {
				if idempotencyCleanup != nil {
					idempotencyCleanup.Stop()
				}
				return nil
			}},
			{"TokenRefreshService", func() error {
				tokenRefresh.Stop()
				return nil
			}},
			{"AccountExpiryService", func() error {
				accountExpiry.Stop()
				return nil
			}},
			{"ProxyExpiryService", func() error {
				proxyExpiry.Stop()
				return nil
			}},
			{"SubscriptionExpiryService", func() error {
				subscriptionExpiry.Stop()
				return nil
			}},
			{"SubscriptionService", func() error {
				if subscriptionService != nil {
					subscriptionService.Stop()
				}
				return nil
			}},
			{"PricingService", func() error {
				pricing.Stop()
				return nil
			}},
			{"EmailQueueService", func() error {
				emailQueue.Stop()
				return nil
			}},
			{"BillingCacheService", func() error {
				billingCache.Stop()
				return nil
			}},
			{"UsageRecordWorkerPool", func() error {
				if usageRecordWorkerPool != nil {
					usageRecordWorkerPool.Stop()
				}
				return nil
			}},
			{"OAuthService", func() error {
				oauth.Stop()
				return nil
			}},
			{"OpenAIOAuthService", func() error {
				openaiOAuth.Stop()
				return nil
			}},
			{"GeminiOAuthService", func() error {
				geminiOAuth.Stop()
				return nil
			}},
			{"AntigravityOAuthService", func() error {
				antigravityOAuth.Stop()
				return nil
			}},
			{"OpenAIWSPool", func() error {
				if openAIGateway != nil {
					openAIGateway.CloseOpenAIWSPool()
				}
				return nil
			}},
			{"ScheduledTestRunnerService", func() error {
				if scheduledTestRunner != nil {
					scheduledTestRunner.Stop()
				}
				return nil
			}},
			{"BackupService", func() error {
				if backupSvc != nil {
					backupSvc.Stop()
				}
				return nil
			}},
			{"PaymentOrderExpiryService", func() error {
				if paymentOrderExpiry != nil {
					paymentOrderExpiry.Stop()
				}
				return nil
			}},
			{"ChannelMonitorRunner", func() error {
				if channelMonitorRunner != nil {
					channelMonitorRunner.Stop()
				}
				return nil
			}},
			{"UserPlatformQuotaUsageFlusher", func() error {
				if quotaFlusher != nil {
					quotaFlusher.Stop()
				}
				return nil
			}},
		}

		infraSteps := []cleanupStep{
			{"Redis", func() error {
				if rdb == nil {
					return nil
				}
				return rdb.Close()
			}},
			{"Ent", func() error {
				if entClient == nil {
					return nil
				}
				return entClient.Close()
			}},
		}

		runParallel := func(steps []cleanupStep) {
			var wg sync.WaitGroup
			for i := range steps {
				step := steps[i]
				wg.Add(1)
				go func() {
					defer wg.Done()
					if err := step.fn(); err != nil {
						log.Printf("[Cleanup] %s failed: %v", step.name, err)
						return
					}
					log.Printf("[Cleanup] %s succeeded", step.name)
				}()

exec
/bin/bash -lc "find backend -maxdepth 3 -type f \\( -name '*.yaml' -o -name '*.yml' \\) -print | sort" in /mnt/x/project/sub2api
 succeeded in 0ms:
backend/.golangci.yml

**Searching for defaults**

I need to implement something, and it looks like I'll have to inspect the default configuration load. It’s important to search for the defaults related to the gateway as well. It feels like there’s a lot to consider, but checking the defaults seems essential to get everything running smoothly. I wonder if I’m missing anything about how all the configurations relate to each other. Let’s make sure to be thorough with this!
codex
影响面判断：新增 registry 目前不会被请求路径消费；fetcher 注入后默认开关 false，不启动 goroutine。主要风险在 Wire 依赖图和 Stop 幂等，我会加互斥保护避免重复 Stop panic。
exec
/bin/bash -lc "rg -n \"SetDefault|gateway\\.|openai_ws|force_codex|response_header_timeout|interval\" backend/internal/config/config.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
58:// 可通过 gateway.upstream_response_read_max_bytes 配置项覆盖。
173:	CleanupIntervalSeconds int `mapstructure:"cleanup_interval_seconds"`
525:	CheckIntervalMinutes int `mapstructure:"check_interval_minutes"`
544:	UpdateIntervalHours int `mapstructure:"update_interval_hours"`
546:	HashCheckIntervalMinutes int `mapstructure:"hash_check_interval_minutes"`
666:	PingInterval int `mapstructure:"ping_interval"`
691:	ResponseHeaderTimeout int `mapstructure:"response_header_timeout"`
694:	OpenAIResponseHeaderTimeout int `mapstructure:"openai_response_header_timeout"`
707:	ForceCodexCLI bool `mapstructure:"force_codex_cli"`
721:	OpenAIWS GatewayOpenAIWSConfig `mapstructure:"openai_ws"`
756:	StreamDataIntervalTimeout int `mapstructure:"stream_data_interval_timeout"`
758:	StreamKeepaliveInterval int `mapstructure:"stream_keepalive_interval"`
760:	ImageStreamDataIntervalTimeout int `mapstructure:"image_stream_data_interval_timeout"`
762:	ImageStreamKeepaliveInterval int `mapstructure:"image_stream_keepalive_interval"`
838:	CleanupIntervalSeconds int `mapstructure:"cleanup_interval_seconds"`
919:	EventFlushIntervalMS int `mapstructure:"event_flush_interval_ms"`
1000:	AutoScaleCheckIntervalSeconds int `mapstructure:"auto_scale_check_interval_seconds"`
1067:	SlotCleanupInterval time.Duration `mapstructure:"slot_cleanup_interval"`
1078:	OutboxPollIntervalSeconds int `mapstructure:"outbox_poll_interval_seconds"`
1090:	FullRebuildIntervalSeconds int `mapstructure:"full_rebuild_interval_seconds"`
1118:	UserPlatformQuotaFlushIntervalMs int `mapstructure:"user_platform_quota_flush_interval_ms"`
1307:	IntervalSeconds int `mapstructure:"interval_seconds"`
1337:	WorkerIntervalSeconds int `mapstructure:"worker_interval_seconds"`
1406:	if !cfg.Gateway.OpenAIScheduler.StickyEscapeEnabled && !viper.IsSet("gateway.openai_scheduler.sticky_escape_enabled") {
1471:	// 兼容旧键 gateway.openai_ws.sticky_previous_response_ttl_seconds。
1534:	viper.SetDefault("run_mode", RunModeStandard)
1537:	viper.SetDefault("server.host", "0.0.0.0")
1538:	viper.SetDefault("server.port", 8080)
1539:	viper.SetDefault("server.mode", "release")
1540:	viper.SetDefault("server.frontend_url", "")
1541:	viper.SetDefault("server.read_header_timeout", 30) // 30秒读取请求头
1542:	viper.SetDefault("server.idle_timeout", 120)       // 120秒空闲超时
1543:	viper.SetDefault("server.trusted_proxies", []string{})
1544:	viper.SetDefault("server.max_request_body_size", int64(256*1024*1024))
1546:	viper.SetDefault("server.h2c.enabled", false)
1547:	viper.SetDefault("server.h2c.max_concurrent_streams", uint32(50))      // 50 个并发流
1548:	viper.SetDefault("server.h2c.idle_timeout", 75)                        // 75 秒
1549:	viper.SetDefault("server.h2c.max_read_frame_size", 1<<20)              // 1MB（够用）
1550:	viper.SetDefault("server.h2c.max_upload_buffer_per_connection", 2<<20) // 2MB
1551:	viper.SetDefault("server.h2c.max_upload_buffer_per_stream", 512<<10)   // 512KB
1554:	viper.SetDefault("log.level", "info")
1555:	viper.SetDefault("log.format", "console")
1556:	viper.SetDefault("log.service_name", "sub2api")
1557:	viper.SetDefault("log.env", "production")
1558:	viper.SetDefault("log.caller", true)
1559:	viper.SetDefault("log.stacktrace_level", "error")
1560:	viper.SetDefault("log.output.to_stdout", true)
1561:	viper.SetDefault("log.output.to_file", true)
1562:	viper.SetDefault("log.output.file_path", "")
1563:	viper.SetDefault("log.rotation.max_size_mb", 100)
1564:	viper.SetDefault("log.rotation.max_backups", 10)
1565:	viper.SetDefault("log.rotation.max_age_days", 7)
1566:	viper.SetDefault("log.rotation.compress", true)
1567:	viper.SetDefault("log.rotation.local_time", true)
1568:	viper.SetDefault("log.sampling.enabled", false)
1569:	viper.SetDefault("log.sampling.initial", 100)
1570:	viper.SetDefault("log.sampling.thereafter", 100)
1573:	viper.SetDefault("cors.allowed_origins", []string{})
1574:	viper.SetDefault("cors.allow_credentials", true)
1577:	viper.SetDefault("security.url_allowlist.enabled", false)
1578:	viper.SetDefault("security.url_allowlist.upstream_hosts", []string{
1588:	viper.SetDefault("security.url_allowlist.pricing_hosts", []string{
1591:	viper.SetDefault("security.url_allowlist.crs_hosts", []string{})
1592:	viper.SetDefault("security.url_allowlist.allow_private_hosts", true)
1593:	viper.SetDefault("security.url_allowlist.allow_insecure_http", true)
1594:	viper.SetDefault("security.response_headers.enabled", true)
1595:	viper.SetDefault("security.response_headers.additional_allowed", []string{})
1596:	viper.SetDefault("security.response_headers.force_remove", []string{})
1597:	viper.SetDefault("security.csp.enabled", true)
1598:	viper.SetDefault("security.csp.policy", DefaultCSPPolicy)
1599:	viper.SetDefault("security.proxy_probe.insecure_skip_verify", false)
1600:	viper.SetDefault("security.trust_forwarded_ip_for_api_key_acl", false)
1603:	viper.SetDefault("security.proxy_fallback.allow_direct_on_error", false)
1606:	viper.SetDefault("billing.circuit_breaker.enabled", true)
1607:	viper.SetDefault("billing.circuit_breaker.failure_threshold", 5)
1608:	viper.SetDefault("billing.circuit_breaker.reset_timeout_seconds", 30)
1609:	viper.SetDefault("billing.circuit_breaker.half_open_requests", 3)
1610:	viper.SetDefault("billing.user_platform_quota_cache_ttl_seconds", 86400)
1611:	viper.SetDefault("billing.user_platform_quota_sentinel_ttl_seconds", 3600)
1614:	viper.SetDefault("turnstile.required", false)
1617:	viper.SetDefault("linuxdo_connect.enabled", false)
1618:	viper.SetDefault("linuxdo_connect.client_id", "")
1619:	viper.SetDefault("linuxdo_connect.client_secret", "")
1620:	viper.SetDefault("linuxdo_connect.authorize_url", "https://connect.linux.do/oauth2/authorize")
1621:	viper.SetDefault("linuxdo_connect.token_url", "https://connect.linux.do/oauth2/token")
1622:	viper.SetDefault("linuxdo_connect.userinfo_url", "https://connect.linux.do/api/user")
1623:	viper.SetDefault("linuxdo_connect.scopes", "user")
1624:	viper.SetDefault("linuxdo_connect.redirect_url", "")
1625:	viper.SetDefault("linuxdo_connect.frontend_redirect_url", "/auth/linuxdo/callback")
1626:	viper.SetDefault("linuxdo_connect.token_auth_method", "client_secret_post")
1627:	viper.SetDefault("linuxdo_connect.use_pkce", false)
1628:	viper.SetDefault("linuxdo_connect.userinfo_email_path", "")
1629:	viper.SetDefault("linuxdo_connect.userinfo_id_path", "")
1630:	viper.SetDefault("linuxdo_connect.userinfo_username_path", "")
1633:	viper.SetDefault("wechat_connect.enabled", false)
1634:	viper.SetDefault("wechat_connect.app_id", "")
1635:	viper.SetDefault("wechat_connect.app_secret", "")
1636:	viper.SetDefault("wechat_connect.open_app_id", "")
1637:	viper.SetDefault("wechat_connect.open_app_secret", "")
1638:	viper.SetDefault("wechat_connect.mp_app_id", "")
1639:	viper.SetDefault("wechat_connect.mp_app_secret", "")
1640:	viper.SetDefault("wechat_connect.mobile_app_id", "")
1641:	viper.SetDefault("wechat_connect.mobile_app_secret", "")
1642:	viper.SetDefault("wechat_connect.open_enabled", false)
1643:	viper.SetDefault("wechat_connect.mp_enabled", false)
1644:	viper.SetDefault("wechat_connect.mobile_enabled", false)
1645:	viper.SetDefault("wechat_connect.mode", defaultWeChatConnectMode)
1646:	viper.SetDefault("wechat_connect.scopes", defaultWeChatConnectScopes)
1647:	viper.SetDefault("wechat_connect.redirect_url", "")
1648:	viper.SetDefault("wechat_connect.frontend_redirect_url", defaultWeChatConnectFrontendRedirect)
1651:	viper.SetDefault("oidc_connect.enabled", false)
1652:	viper.SetDefault("oidc_connect.provider_name", "OIDC")
1653:	viper.SetDefault("oidc_connect.client_id", "")
1654:	viper.SetDefault("oidc_connect.client_secret", "")
1655:	viper.SetDefault("oidc_connect.issuer_url", "")
1656:	viper.SetDefault("oidc_connect.discovery_url", "")
1657:	viper.SetDefault("oidc_connect.authorize_url", "")
1658:	viper.SetDefault("oidc_connect.token_url", "")
1659:	viper.SetDefault("oidc_connect.userinfo_url", "")
1660:	viper.SetDefault("oidc_connect.jwks_url", "")
1661:	viper.SetDefault("oidc_connect.scopes", "openid email profile")
1662:	viper.SetDefault("oidc_connect.redirect_url", "")
1663:	viper.SetDefault("oidc_connect.frontend_redirect_url", "/auth/oidc/callback")
1664:	viper.SetDefault("oidc_connect.token_auth_method", "client_secret_post")
1665:	viper.SetDefault("oidc_connect.use_pkce", true)
1666:	viper.SetDefault("oidc_connect.validate_id_token", true)
1667:	viper.SetDefault("oidc_connect.allowed_signing_algs", "RS256,ES256,PS256")
1668:	viper.SetDefault("oidc_connect.clock_skew_seconds", 120)
1669:	viper.SetDefault("oidc_connect.require_email_verified", false)
1670:	viper.SetDefault("oidc_connect.userinfo_email_path", "")
1671:	viper.SetDefault("oidc_connect.userinfo_id_path", "")
1672:	viper.SetDefault("oidc_connect.userinfo_username_path", "")
1675:	viper.SetDefault("dingtalk_connect.enabled", false)
1676:	viper.SetDefault("dingtalk_connect.authorize_url", "https://login.dingtalk.com/oauth2/auth")
1677:	viper.SetDefault("dingtalk_connect.token_url", "https://api.dingtalk.com/v1.0/oauth2/userAccessToken")
1678:	viper.SetDefault("dingtalk_connect.userinfo_url", "https://api.dingtalk.com/v1.0/contact/users/me")
1679:	viper.SetDefault("dingtalk_connect.scopes", "openid")
1680:	viper.SetDefault("dingtalk_connect.frontend_redirect_url", "/auth/dingtalk/callback")
1681:	viper.SetDefault("dingtalk_connect.dingtalk_app_kind", "internal_app")
1682:	viper.SetDefault("dingtalk_connect.app_type", "public")
1683:	viper.SetDefault("dingtalk_connect.corp_restriction_policy", "none")
1684:	viper.SetDefault("dingtalk_connect.require_email", true)
1685:	viper.SetDefault("dingtalk_connect.username_overwrite_policy", "if_empty")
1688:	viper.SetDefault("database.host", "localhost")
1689:	viper.SetDefault("database.port", 5432)
1690:	viper.SetDefault("database.user", "postgres")
1691:	viper.SetDefault("database.password", "postgres")
1692:	viper.SetDefault("database.dbname", "sub2api")
1693:	viper.SetDefault("database.sslmode", "prefer")
1694:	viper.SetDefault("database.max_open_conns", 256)
1695:	viper.SetDefault("database.max_idle_conns", 128)
1696:	viper.SetDefault("database.conn_max_lifetime_minutes", 30)
1697:	viper.SetDefault("database.conn_max_idle_time_minutes", 5)
1698:	viper.SetDefault("database.user_platform_quota_flusher_enabled", false)
1699:	viper.SetDefault("database.user_platform_quota_flush_interval_ms", 2000)
1700:	viper.SetDefault("database.user_platform_quota_flush_batch_size", 1000)
1703:	viper.SetDefault("redis.host", "localhost")
1704:	viper.SetDefault("redis.port", 6379)
1705:	viper.SetDefault("redis.password", "")
1706:	viper.SetDefault("redis.db", 0)
1707:	viper.SetDefault("redis.dial_timeout_seconds", 5)
1708:	viper.SetDefault("redis.read_timeout_seconds", 3)
1709:	viper.SetDefault("redis.write_timeout_seconds", 3)
1710:	viper.SetDefault("redis.pool_size", 1024)
1711:	viper.SetDefault("redis.min_idle_conns", 128)
1712:	viper.SetDefault("redis.enable_tls", false)
1715:	viper.SetDefault("ops.enabled", true)
1716:	viper.SetDefault("ops.use_preaggregated_tables", true)
1717:	viper.SetDefault("ops.cleanup.enabled", true)
1718:	viper.SetDefault("ops.cleanup.schedule", "0 2 * * *")
1720:	viper.SetDefault("ops.cleanup.error_log_retention_days", 30)
1721:	viper.SetDefault("ops.cleanup.minute_metrics_retention_days", 30)
1722:	viper.SetDefault("ops.cleanup.hourly_metrics_retention_days", 30)
1723:	viper.SetDefault("ops.aggregation.enabled", true)
1724:	viper.SetDefault("ops.metrics_collector_cache.enabled", true)
1725:	// TTL should be slightly larger than collection interval (1m) to maximize cross-replica cache hits.
1726:	viper.SetDefault("ops.metrics_collector_cache.ttl", 65*time.Second)
1729:	viper.SetDefault("jwt.secret", "")
1730:	viper.SetDefault("jwt.expire_hour", 24)
1731:	viper.SetDefault("jwt.access_token_expire_minutes", 0) // 0 表示回退到 expire_hour
1732:	viper.SetDefault("jwt.refresh_token_expire_days", 30)  // 30天Refresh Token有效期
1733:	viper.SetDefault("jwt.refresh_window_minutes", 2)      // 过期前2分钟开始允许刷新
1736:	viper.SetDefault("totp.encryption_key", "")
1741:	viper.SetDefault("default.admin_email", "")
1742:	viper.SetDefault("default.admin_password", "")
1743:	viper.SetDefault("default.user_concurrency", 5)
1744:	viper.SetDefault("default.user_balance", 0)
1745:	viper.SetDefault("default.api_key_prefix", "sk-")
1746:	viper.SetDefault("default.rate_multiplier", 1.0)
1749:	viper.SetDefault("rate_limit.overload_cooldown_minutes", 10)
1750:	viper.SetDefault("rate_limit.oauth_401_cooldown_minutes", 10)
1753:	viper.SetDefault("pricing.remote_url", "https://raw.githubusercontent.com/Wei-Shaw/model-price-repo/main/model_prices_and_context_window.json")
1754:	viper.SetDefault("pricing.hash_url", "https://raw.githubusercontent.com/Wei-Shaw/model-price-repo/main/model_prices_and_context_window.sha256")
1755:	viper.SetDefault("pricing.data_dir", "./data")
1756:	viper.SetDefault("pricing.fallback_file", "./resources/model-pricing/model_prices_and_context_window.json")
1757:	viper.SetDefault("pricing.update_interval_hours", 24)
1758:	viper.SetDefault("pricing.hash_check_interval_minutes", 10)
1761:	viper.SetDefault("timezone", "Asia/Shanghai")
1764:	viper.SetDefault("api_key_auth_cache.l1_size", 65535)
1765:	viper.SetDefault("api_key_auth_cache.l1_ttl_seconds", 15)
1766:	viper.SetDefault("api_key_auth_cache.l2_ttl_seconds", 300)
1767:	viper.SetDefault("api_key_auth_cache.negative_ttl_seconds", 30)
1768:	viper.SetDefault("api_key_auth_cache.jitter_percent", 10)
1769:	viper.SetDefault("api_key_auth_cache.singleflight", true)
1772:	viper.SetDefault("subscription_cache.l1_size", 16384)
1773:	viper.SetDefault("subscription_cache.l1_ttl_seconds", 10)
1774:	viper.SetDefault("subscription_cache.jitter_percent", 10)
1777:	viper.SetDefault("dashboard_cache.enabled", true)
1778:	viper.SetDefault("dashboard_cache.key_prefix", "sub2api:")
1779:	viper.SetDefault("dashboard_cache.stats_fresh_ttl_seconds", 15)
1780:	viper.SetDefault("dashboard_cache.stats_ttl_seconds", 30)
1781:	viper.SetDefault("dashboard_cache.stats_refresh_timeout_seconds", 30)
1784:	viper.SetDefault("dashboard_aggregation.enabled", true)
1785:	viper.SetDefault("dashboard_aggregation.interval_seconds", 60)
1786:	viper.SetDefault("dashboard_aggregation.lookback_seconds", 120)
1787:	viper.SetDefault("dashboard_aggregation.backfill_enabled", false)
1788:	viper.SetDefault("dashboard_aggregation.backfill_max_days", 31)
1789:	viper.SetDefault("dashboard_aggregation.retention.usage_logs_days", 90)
1790:	viper.SetDefault("dashboard_aggregation.retention.usage_billing_dedup_days", 365)
1791:	viper.SetDefault("dashboard_aggregation.retention.hourly_days", 180)
1792:	viper.SetDefault("dashboard_aggregation.retention.daily_days", 730)
1793:	viper.SetDefault("dashboard_aggregation.recompute_days", 2)
1796:	viper.SetDefault("usage_cleanup.enabled", true)
1797:	viper.SetDefault("usage_cleanup.max_range_days", 31)
1798:	viper.SetDefault("usage_cleanup.batch_size", 5000)
1799:	viper.SetDefault("usage_cleanup.worker_interval_seconds", 10)
1800:	viper.SetDefault("usage_cleanup.task_timeout_seconds", 1800)
1803:	viper.SetDefault("idempotency.observe_only", true)
1804:	viper.SetDefault("idempotency.default_ttl_seconds", 86400)
1805:	viper.SetDefault("idempotency.system_operation_ttl_seconds", 3600)
1806:	viper.SetDefault("idempotency.processing_timeout_seconds", 30)
1807:	viper.SetDefault("idempotency.failed_retry_backoff_seconds", 5)
1808:	viper.SetDefault("idempotency.max_stored_response_len", 64*1024)
1809:	viper.SetDefault("idempotency.cleanup_interval_seconds", 60)
1810:	viper.SetDefault("idempotency.cleanup_batch_size", 500)
1813:	viper.SetDefault("gateway.response_header_timeout", 600) // 600秒(10分钟)等待上游响应头，LLM高负载时可能排队较久
1814:	viper.SetDefault("gateway.openai_response_header_timeout", 0)
1815:	viper.SetDefault("gateway.log_upstream_error_body", true)
1816:	viper.SetDefault("gateway.log_upstream_error_body_max_bytes", 2048)
1817:	viper.SetDefault("gateway.inject_beta_for_apikey", false)
1818:	viper.SetDefault("gateway.failover_on_400", false)
1819:	viper.SetDefault("gateway.max_account_switches", 10)
1820:	viper.SetDefault("gateway.max_account_switches_gemini", 3)
1821:	viper.SetDefault("gateway.force_codex_cli", false)
1822:	viper.SetDefault("gateway.codex_image_generation_bridge_enabled", false)
1823:	viper.SetDefault("gateway.openai_passthrough_allow_timeout_headers", false)
1825:	viper.SetDefault("gateway.openai_ws.enabled", true)
1826:	viper.SetDefault("gateway.openai_ws.mode_router_v2_enabled", false)
1827:	viper.SetDefault("gateway.openai_ws.ingress_mode_default", "ctx_pool")
1828:	viper.SetDefault("gateway.openai_ws.oauth_enabled", true)
1829:	viper.SetDefault("gateway.openai_ws.apikey_enabled", true)
1830:	viper.SetDefault("gateway.openai_ws.force_http", false)
1831:	viper.SetDefault("gateway.openai_ws.allow_store_recovery", false)
1832:	viper.SetDefault("gateway.openai_ws.ingress_previous_response_recovery_enabled", true)
1833:	viper.SetDefault("gateway.openai_ws.store_disabled_conn_mode", "strict")
1834:	viper.SetDefault("gateway.openai_ws.store_disabled_force_new_conn", true)
1835:	viper.SetDefault("gateway.openai_ws.prewarm_generate_enabled", false)
1836:	viper.SetDefault("gateway.openai_ws.client_read_limit_bytes", 64*1024*1024)
1837:	viper.SetDefault("gateway.openai_ws.http_bridge_enabled", true)
1838:	viper.SetDefault("gateway.openai_ws.http_bridge_threshold_bytes", 15*1024*1024)
1839:	viper.SetDefault("gateway.openai_ws.responses_websockets", false)
1840:	viper.SetDefault("gateway.openai_ws.responses_websockets_v2", true)
1841:	viper.SetDefault("gateway.openai_ws.max_conns_per_account", 128)
1842:	viper.SetDefault("gateway.openai_ws.min_idle_per_account", 4)
1843:	viper.SetDefault("gateway.openai_ws.max_idle_per_account", 12)
1844:	viper.SetDefault("gateway.openai_ws.dynamic_max_conns_by_account_concurrency_enabled", true)
1845:	viper.SetDefault("gateway.openai_ws.oauth_max_conns_factor", 1.0)
1846:	viper.SetDefault("gateway.openai_ws.apikey_max_conns_factor", 1.0)
1847:	viper.SetDefault("gateway.openai_ws.dial_timeout_seconds", 10)
1848:	viper.SetDefault("gateway.openai_ws.read_timeout_seconds", 900)
1849:	viper.SetDefault("gateway.openai_ws.write_timeout_seconds", 120)
1850:	viper.SetDefault("gateway.openai_ws.pool_target_utilization", 0.7)
1851:	viper.SetDefault("gateway.openai_ws.queue_limit_per_conn", 64)
1852:	viper.SetDefault("gateway.openai_ws.event_flush_batch_size", 1)
1853:	viper.SetDefault("gateway.openai_ws.event_flush_interval_ms", 10)
1854:	viper.SetDefault("gateway.openai_ws.prewarm_cooldown_ms", 300)
1855:	viper.SetDefault("gateway.openai_ws.fallback_cooldown_seconds", 30)
1856:	viper.SetDefault("gateway.openai_ws.retry_backoff_initial_ms", 120)
1857:	viper.SetDefault("gateway.openai_ws.retry_backoff_max_ms", 2000)
1858:	viper.SetDefault("gateway.openai_ws.retry_jitter_ratio", 0.2)
1859:	viper.SetDefault("gateway.openai_ws.retry_total_budget_ms", 5000)
1860:	viper.SetDefault("gateway.openai_ws.payload_log_sample_rate", 0.2)
1861:	viper.SetDefault("gateway.openai_ws.lb_top_k", 7)
1862:	viper.SetDefault("gateway.openai_ws.sticky_session_ttl_seconds", 3600)
1863:	viper.SetDefault("gateway.openai_ws.session_hash_read_old_fallback", true)
1864:	viper.SetDefault("gateway.openai_ws.session_hash_dual_write_old", true)
1865:	viper.SetDefault("gateway.openai_ws.metadata_bridge_enabled", true)
1866:	viper.SetDefault("gateway.openai_ws.sticky_response_id_ttl_seconds", 3600)
1867:	viper.SetDefault("gateway.openai_ws.sticky_previous_response_ttl_seconds", 3600)
1868:	viper.SetDefault("gateway.openai_ws.scheduler_score_weights.priority", 1.0)
1869:	viper.SetDefault("gateway.openai_ws.scheduler_score_weights.load", 1.0)
1870:	viper.SetDefault("gateway.openai_ws.scheduler_score_weights.queue", 0.7)
1871:	viper.SetDefault("gateway.openai_ws.scheduler_score_weights.error_rate", 0.8)
1872:	viper.SetDefault("gateway.openai_ws.scheduler_score_weights.ttft", 0.5)
1874:	viper.SetDefault("gateway.openai_http2.enabled", true)
1875:	viper.SetDefault("gateway.openai_http2.allow_proxy_fallback_to_http1", true)
1876:	viper.SetDefault("gateway.openai_http2.fallback_error_threshold", 2)
1877:	viper.SetDefault("gateway.openai_http2.fallback_window_seconds", 60)
1878:	viper.SetDefault("gateway.openai_http2.fallback_ttl_seconds", 600)
1879:	viper.SetDefault("gateway.image_concurrency.enabled", false)
1880:	viper.SetDefault("gateway.image_concurrency.max_concurrent_requests", 0)
1881:	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
1882:	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
1883:	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
1884:	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
1885:	viper.SetDefault("gateway.antigravity_extra_retries", 10)
1886:	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
1887:	viper.SetDefault("gateway.upstream_response_read_max_bytes", DefaultUpstreamResponseReadMaxBytes)
1888:	viper.SetDefault("gateway.proxy_probe_response_read_max_bytes", int64(1024*1024))
1889:	viper.SetDefault("gateway.gemini_debug_response_headers", false)
1890:	viper.SetDefault("gateway.connection_pool_isolation", ConnectionPoolIsolationAccountProxy)
1892:	viper.SetDefault("gateway.max_idle_conns", 2560)          // 最大空闲连接总数（高并发场景可调大）
1893:	viper.SetDefault("gateway.max_idle_conns_per_host", 120)  // 每主机最大空闲连接（HTTP/2 场景默认）
1894:	viper.SetDefault("gateway.max_conns_per_host", 1024)      // 每主机最大连接数（含活跃；流式/HTTP1.1 场景可调大，如 2400+）
1895:	viper.SetDefault("gateway.idle_conn_timeout_seconds", 90) // 空闲连接超时（秒）
1896:	viper.SetDefault("gateway.max_upstream_clients", 5000)
1897:	viper.SetDefault("gateway.client_idle_ttl_seconds", 900)
1898:	viper.SetDefault("gateway.concurrency_slot_ttl_minutes", 30) // 并发槽位过期时间（支持超长请求）
1899:	viper.SetDefault("gateway.stream_data_interval_timeout", 180)
1900:	viper.SetDefault("gateway.stream_keepalive_interval", 10)
1901:	viper.SetDefault("gateway.image_stream_data_interval_timeout", 900)
1902:	viper.SetDefault("gateway.image_stream_keepalive_interval", 10)
1903:	viper.SetDefault("gateway.max_line_size", 500*1024*1024)
1904:	viper.SetDefault("gateway.scheduling.sticky_session_max_waiting", 3)
1905:	viper.SetDefault("gateway.scheduling.sticky_session_wait_timeout", 120*time.Second)
1906:	viper.SetDefault("gateway.scheduling.fallback_wait_timeout", 30*time.Second)
1907:	viper.SetDefault("gateway.scheduling.fallback_max_waiting", 100)
1908:	viper.SetDefault("gateway.scheduling.fallback_selection_mode", "last_used")
1909:	viper.SetDefault("gateway.scheduling.load_batch_enabled", true)
1910:	viper.SetDefault("gateway.scheduling.load_batch_cache_ttl_ms", 200)
1911:	viper.SetDefault("gateway.scheduling.snapshot_mget_chunk_size", 128)
1912:	viper.SetDefault("gateway.scheduling.snapshot_write_chunk_size", 256)
1913:	viper.SetDefault("gateway.scheduling.slot_cleanup_interval", 30*time.Second)
1914:	viper.SetDefault("gateway.scheduling.db_fallback_enabled", true)
1915:	viper.SetDefault("gateway.scheduling.db_fallback_timeout_seconds", 0)
1916:	viper.SetDefault("gateway.scheduling.db_fallback_max_qps", 0)
1917:	viper.SetDefault("gateway.scheduling.outbox_poll_interval_seconds", 1)
1918:	viper.SetDefault("gateway.scheduling.outbox_lag_warn_seconds", 5)
1919:	viper.SetDefault("gateway.scheduling.outbox_lag_rebuild_seconds", 10)
1920:	viper.SetDefault("gateway.scheduling.outbox_lag_rebuild_failures", 3)
1921:	viper.SetDefault("gateway.scheduling.outbox_backlog_rebuild_rows", 10000)
1922:	viper.SetDefault("gateway.scheduling.full_rebuild_interval_seconds", 300)
1923:	viper.SetDefault("gateway.usage_record.worker_count", 128)
1924:	viper.SetDefault("gateway.usage_record.queue_size", 16384)
1925:	viper.SetDefault("gateway.usage_record.task_timeout_seconds", 5)
1926:	viper.SetDefault("gateway.usage_record.overflow_policy", UsageRecordOverflowPolicySample)
1927:	viper.SetDefault("gateway.usage_record.overflow_sample_percent", 10)
1928:	viper.SetDefault("gateway.usage_record.auto_scale_enabled", true)
1929:	viper.SetDefault("gateway.usage_record.auto_scale_min_workers", 128)
1930:	viper.SetDefault("gateway.usage_record.auto_scale_max_workers", 512)
1931:	viper.SetDefault("gateway.usage_record.auto_scale_up_queue_percent", 70)
1932:	viper.SetDefault("gateway.usage_record.auto_scale_down_queue_percent", 15)
1933:	viper.SetDefault("gateway.usage_record.auto_scale_up_step", 32)
1934:	viper.SetDefault("gateway.usage_record.auto_scale_down_step", 16)
1935:	viper.SetDefault("gateway.usage_record.auto_scale_check_interval_seconds", 3)
1936:	viper.SetDefault("gateway.usage_record.auto_scale_cooldown_seconds", 10)
1937:	viper.SetDefault("gateway.user_group_rate_cache_ttl_seconds", 30)
1938:	viper.SetDefault("gateway.models_list_cache_ttl_seconds", 15)
1941:	viper.SetDefault("gateway.user_message_queue.enabled", false)
1942:	viper.SetDefault("gateway.user_message_queue.lock_ttl_ms", 120000)
1943:	viper.SetDefault("gateway.user_message_queue.wait_timeout_ms", 30000)
1944:	viper.SetDefault("gateway.user_message_queue.min_delay_ms", 200)
1945:	viper.SetDefault("gateway.user_message_queue.max_delay_ms", 2000)
1946:	viper.SetDefault("gateway.user_message_queue.cleanup_interval_seconds", 60)
1948:	viper.SetDefault("gateway.tls_fingerprint.enabled", true)
1949:	viper.SetDefault("concurrency.ping_interval", 10)
1952:	viper.SetDefault("token_refresh.enabled", true)
1953:	viper.SetDefault("token_refresh.check_interval_minutes", 5)        // 每5分钟检查一次
1954:	viper.SetDefault("token_refresh.refresh_before_expiry_hours", 0.5) // 提前30分钟刷新（适配Google 1小时token）
1955:	viper.SetDefault("token_refresh.max_retries", 3)                   // 最多重试3次
1956:	viper.SetDefault("token_refresh.retry_backoff_seconds", 2)         // 重试退避基础2秒
1961:	viper.SetDefault("gemini.oauth.client_id", "")
1962:	viper.SetDefault("gemini.oauth.client_secret", "")
1963:	viper.SetDefault("gemini.oauth.scopes", "")
1964:	viper.SetDefault("gemini.quota.policy", "")
1967:	viper.SetDefault("subscription_maintenance.worker_count", 2)
1968:	viper.SetDefault("subscription_maintenance.queue_size", 1024)
2329:			return fmt.Errorf("dashboard_aggregation.interval_seconds must be positive")
2360:			return fmt.Errorf("dashboard_aggregation.interval_seconds must be non-negative")
2397:			return fmt.Errorf("usage_cleanup.worker_interval_seconds must be positive")
2410:			return fmt.Errorf("usage_cleanup.worker_interval_seconds must be non-negative")
2432:		return fmt.Errorf("idempotency.cleanup_interval_seconds must be positive")
2438:		return fmt.Errorf("gateway.max_body_size must be positive")
2441:		return fmt.Errorf("gateway.upstream_response_read_max_bytes must be positive")
2444:		return fmt.Errorf("gateway.proxy_probe_response_read_max_bytes must be positive")
2447:		return fmt.Errorf("gateway.response_header_timeout must be non-negative")
2450:		return fmt.Errorf("gateway.openai_response_header_timeout must be non-negative")
2456:			return fmt.Errorf("gateway.connection_pool_isolation must be one of: %s/%s/%s",
2461:		return fmt.Errorf("gateway.image_concurrency.max_concurrent_requests must be non-negative")
2466:		return fmt.Errorf("gateway.image_concurrency.overflow_mode must be one of: %s/%s",
2470:		return fmt.Errorf("gateway.image_concurrency.wait_timeout_seconds must be non-negative")
2473:		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
2476:		return fmt.Errorf("gateway.max_idle_conns must be positive")
2479:		return fmt.Errorf("gateway.max_idle_conns_per_host must be positive")
2482:		return fmt.Errorf("gateway.max_conns_per_host must be non-negative")
2485:		return fmt.Errorf("gateway.idle_conn_timeout_seconds must be positive")
2488:		slog.Warn("gateway.idle_conn_timeout_seconds is high; consider 60-120 seconds for better connection reuse", "idle_conn_timeout_seconds", c.Gateway.IdleConnTimeoutSeconds)
2491:		return fmt.Errorf("gateway.max_upstream_clients must be positive")
2494:		return fmt.Errorf("gateway.client_idle_ttl_seconds must be positive")
2497:		return fmt.Errorf("gateway.concurrency_slot_ttl_minutes must be positive")
2500:		return fmt.Errorf("gateway.stream_data_interval_timeout must be non-negative")
2504:		return fmt.Errorf("gateway.stream_data_interval_timeout must be 0 or between 30-300 seconds")
2507:		return fmt.Errorf("gateway.stream_keepalive_interval must be non-negative")
2511:		return fmt.Errorf("gateway.stream_keepalive_interval must be 0 or between 5-30 seconds")
2514:		return fmt.Errorf("gateway.image_stream_data_interval_timeout must be non-negative")
2518:		return fmt.Errorf("gateway.image_stream_data_interval_timeout must be 0 or between 60-1800 seconds")
2521:		return fmt.Errorf("gateway.image_stream_keepalive_interval must be non-negative")
2525:		return fmt.Errorf("gateway.image_stream_keepalive_interval must be 0 or between 5-60 seconds")
2532:		return fmt.Errorf("gateway.openai_ws.max_conns_per_account must be positive")
2535:		return fmt.Errorf("gateway.openai_ws.min_idle_per_account must be non-negative")
2538:		return fmt.Errorf("gateway.openai_ws.max_idle_per_account must be non-negative")
2541:		return fmt.Errorf("gateway.openai_ws.min_idle_per_account must be <= max_idle_per_account")
2544:		return fmt.Errorf("gateway.openai_ws.max_idle_per_account must be <= max_conns_per_account")
2547:		return fmt.Errorf("gateway.openai_ws.oauth_max_conns_factor must be positive")
2550:		return fmt.Errorf("gateway.openai_ws.apikey_max_conns_factor must be positive")
2553:		return fmt.Errorf("gateway.openai_ws.dial_timeout_seconds must be positive")
2556:		return fmt.Errorf("gateway.openai_ws.read_timeout_seconds must be positive")
2559:		return fmt.Errorf("gateway.openai_ws.write_timeout_seconds must be positive")
2562:		return fmt.Errorf("gateway.openai_ws.pool_target_utilization must be within (0,1]")
2565:		return fmt.Errorf("gateway.openai_ws.queue_limit_per_conn must be positive")
2568:		return fmt.Errorf("gateway.openai_ws.event_flush_batch_size must be positive")
2571:		return fmt.Errorf("gateway.openai_ws.event_flush_interval_ms must be non-negative")
2574:		return fmt.Errorf("gateway.openai_ws.prewarm_cooldown_ms must be non-negative")
2577:		return fmt.Errorf("gateway.openai_ws.client_read_limit_bytes must be positive")
2580:		return fmt.Errorf("gateway.openai_ws.http_bridge_threshold_bytes must be non-negative")
2583:		return fmt.Errorf("gateway.openai_ws.http_bridge_threshold_bytes must be positive when http_bridge_enabled is true")
2586:		return fmt.Errorf("gateway.openai_ws.fallback_cooldown_seconds must be non-negative")
2589:		return fmt.Errorf("gateway.openai_ws.retry_backoff_initial_ms must be non-negative")
2592:		return fmt.Errorf("gateway.openai_ws.retry_backoff_max_ms must be non-negative")
2596:		return fmt.Errorf("gateway.openai_ws.retry_backoff_max_ms must be >= retry_backoff_initial_ms")
2599:		return fmt.Errorf("gateway.openai_ws.retry_jitter_ratio must be within [0,1]")
2602:		return fmt.Errorf("gateway.openai_ws.retry_total_budget_ms must be non-negative")
2608:			slog.Warn("gateway.openai_ws.ingress_mode_default is deprecated, treating as ctx_pool; please update to off|ctx_pool|passthrough", "value", mode)
2610:			return fmt.Errorf("gateway.openai_ws.ingress_mode_default must be one of off|ctx_pool|passthrough")
2617:			return fmt.Errorf("gateway.openai_ws.store_disabled_conn_mode must be one of strict|adaptive|off")
2621:		return fmt.Errorf("gateway.openai_ws.payload_log_sample_rate must be within [0,1]")
2624:		return fmt.Errorf("gateway.openai_ws.lb_top_k must be positive")
2627:		return fmt.Errorf("gateway.openai_ws.sticky_session_ttl_seconds must be positive")
2630:		return fmt.Errorf("gateway.openai_ws.sticky_response_id_ttl_seconds must be positive")
2633:		return fmt.Errorf("gateway.openai_ws.sticky_previous_response_ttl_seconds must be non-negative")
2636:		return fmt.Errorf("gateway.openai_http2.fallback_error_threshold must be non-negative")
2639:		return fmt.Errorf("gateway.openai_http2.fallback_window_seconds must be non-negative")
2642:		return fmt.Errorf("gateway.openai_http2.fallback_ttl_seconds must be non-negative")
2649:		return fmt.Errorf("gateway.openai_ws.scheduler_score_weights.* must be non-negative")
2657:		return fmt.Errorf("gateway.openai_ws.scheduler_score_weights must not all be zero")
2660:		return fmt.Errorf("gateway.openai_scheduler.sticky_escape_ttft_ms must be positive")
2663:		return fmt.Errorf("gateway.openai_scheduler.sticky_escape_error_rate must be between 0 and 1")
2666:		return fmt.Errorf("gateway.max_line_size must be non-negative")
2669:		return fmt.Errorf("gateway.max_line_size must be at least 1MB")
2672:		return fmt.Errorf("gateway.usage_record.worker_count must be positive")
2675:		return fmt.Errorf("gateway.usage_record.queue_size must be positive")
2678:		return fmt.Errorf("gateway.usage_record.task_timeout_seconds must be positive")
2683:		return fmt.Errorf("gateway.usage_record.overflow_policy must be one of: %s/%s/%s",
2687:		return fmt.Errorf("gateway.usage_record.overflow_sample_percent must be between 0-100")
2691:		return fmt.Errorf("gateway.usage_record.overflow_sample_percent must be positive when overflow_policy=sample")
2695:			return fmt.Errorf("gateway.usage_record.auto_scale_min_workers must be positive")
2698:			return fmt.Errorf("gateway.usage_record.auto_scale_max_workers must be positive")
2701:			return fmt.Errorf("gateway.usage_record.auto_scale_max_workers must be >= auto_scale_min_workers")
2705:			return fmt.Errorf("gateway.usage_record.worker_count must be between auto_scale_min_workers and auto_scale_max_workers")
2708:			return fmt.Errorf("gateway.usage_record.auto_scale_up_queue_percent must be between 1-100")
2711:			return fmt.Errorf("gateway.usage_record.auto_scale_down_queue_percent must be between 0-99")
2714:			return fmt.Errorf("gateway.usage_record.auto_scale_down_queue_percent must be less than auto_scale_up_queue_percent")
2717:			return fmt.Errorf("gateway.usage_record.auto_scale_up_step must be positive")
2720:			return fmt.Errorf("gateway.usage_record.auto_scale_down_step must be positive")
2723:			return fmt.Errorf("gateway.usage_record.auto_scale_check_interval_seconds must be positive")
2726:			return fmt.Errorf("gateway.usage_record.auto_scale_cooldown_seconds must be non-negative")
2730:		return fmt.Errorf("gateway.user_group_rate_cache_ttl_seconds must be positive")
2733:		return fmt.Errorf("gateway.models_list_cache_ttl_seconds must be between 10-30")
2736:		return fmt.Errorf("gateway.scheduling.sticky_session_max_waiting must be positive")
2739:		return fmt.Errorf("gateway.scheduling.sticky_session_wait_timeout must be positive")
2742:		return fmt.Errorf("gateway.scheduling.fallback_wait_timeout must be positive")
2745:		return fmt.Errorf("gateway.scheduling.fallback_max_waiting must be positive")
2748:		return fmt.Errorf("gateway.scheduling.load_batch_cache_ttl_ms must be non-negative")
2751:		return fmt.Errorf("gateway.scheduling.snapshot_mget_chunk_size must be positive")
2754:		return fmt.Errorf("gateway.scheduling.snapshot_write_chunk_size must be positive")
2757:		return fmt.Errorf("gateway.scheduling.slot_cleanup_interval must be non-negative")
2760:		return fmt.Errorf("gateway.scheduling.db_fallback_timeout_seconds must be non-negative")
2763:		return fmt.Errorf("gateway.scheduling.db_fallback_max_qps must be non-negative")
2766:		return fmt.Errorf("gateway.scheduling.outbox_poll_interval_seconds must be positive")
2769:		return fmt.Errorf("gateway.scheduling.outbox_lag_warn_seconds must be non-negative")
2772:		return fmt.Errorf("gateway.scheduling.outbox_lag_rebuild_seconds must be non-negative")
2775:		return fmt.Errorf("gateway.scheduling.outbox_lag_rebuild_failures must be positive")
2778:		return fmt.Errorf("gateway.scheduling.outbox_backlog_rebuild_rows must be non-negative")
2781:		return fmt.Errorf("gateway.scheduling.full_rebuild_interval_seconds must be non-negative")
2786:		return fmt.Errorf("gateway.scheduling.outbox_lag_rebuild_seconds must be >= outbox_lag_warn_seconds")
2804:		return fmt.Errorf("concurrency.ping_interval must be between 5-30 seconds")
2872:	v.SetDefault("server.host", "0.0.0.0")
2873:	v.SetDefault("server.port", 8080)

exec
/bin/bash -lc "sed -n '65,115p' backend/internal/pkg/claude/constants.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
// CLICurrentVersion 是 sub2api 当前对外伪装的 Claude Code CLI 版本号（三段 semver）。
// 用于 billing attribution block 中的 cc_version=X.Y.Z.{fp} 前缀以及 fingerprint 计算。
// 必须与 DefaultHeaders["User-Agent"] 中的版本号严格一致；不一致会被 Anthropic 判第三方。
const CLICurrentVersion = "2.1.161"

// FullClaudeCodeMimicryBetas 返回最"像"真实 Claude Code CLI 的完整 beta 列表，
// 用于 OAuth 账号伪装成 Claude Code 时使用。
// 顺序与真实 CLI 抓包一致。
//
// 使用建议：
//   - OAuth 账号 + 非 haiku：追加这整份列表，再按需保留 client 带来的 beta。
//   - OAuth 账号 + haiku：Anthropic 对 haiku 不做 third-party 判定，使用 HaikuBetaHeader 即可。
//   - API-key 账号：不要使用本函数，参见 APIKeyBetaHeader。
//   - 不默认加入 redact-thinking，避免上游抹除 thinking 内容；客户端显式传入时由合并逻辑保留。
func FullClaudeCodeMimicryBetas() []string {
	return []string{
		BetaClaudeCode,
		BetaOAuth,
		BetaInterleavedThinking,
		BetaPromptCachingScope,
		BetaEffort,
		BetaContextManagement,
		BetaExtendedCacheTTL,
	}
}

// DefaultHeaders 是 Claude Code 客户端默认请求头。
var DefaultHeaders = map[string]string{
	// Keep these in sync with recent Claude CLI traffic to reduce the chance
	// that Claude Code-scoped OAuth credentials are rejected as "non-CLI" usage.
	// 版本参考：对齐 Parrot (src/transform/cc_mimicry.py:49) 的 CLI_USER_AGENT。
	"User-Agent":                                "claude-cli/" + CLICurrentVersion + " (external, cli)",
	"X-Stainless-Lang":                          "js",
	"X-Stainless-Package-Version":               "0.94.0",
	"X-Stainless-OS":                            "Linux",
	"X-Stainless-Arch":                          "arm64",
	"X-Stainless-Runtime":                       "node",
	"X-Stainless-Runtime-Version":               "v24.3.0",
	"X-Stainless-Retry-Count":                   "0",
	"X-Stainless-Timeout":                       "600",
	"X-App":                                     "cli",
	"Anthropic-Dangerous-Direct-Browser-Access": "true",
}

// Model 表示一个 Claude 模型
type Model struct {
	ID          string `json:"id"`
	Type        string `json:"type"`
	DisplayName string `json:"display_name"`
	CreatedAt   string `json:"created_at"`
}

exec
/bin/bash -lc "sed -n '1,140p' backend/internal/service/openai_gateway_service.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
package service

import (
	"bufio"
	"bytes"
	"context"
	"crypto/sha256"
	"encoding/hex"
	"encoding/json"
	"errors"
	"fmt"
	"io"
	"log/slog"
	"math/rand"
	"net/http"
	"sort"
	"strconv"
	"strings"
	"sync"
	"sync/atomic"
	"time"

	"github.com/Wei-Shaw/sub2api/internal/config"
	"github.com/Wei-Shaw/sub2api/internal/pkg/apicompat"
	"github.com/Wei-Shaw/sub2api/internal/pkg/ip"
	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
	"github.com/Wei-Shaw/sub2api/internal/pkg/openai"
	"github.com/Wei-Shaw/sub2api/internal/pkg/openai_compat"
	"github.com/Wei-Shaw/sub2api/internal/util/responseheaders"
	"github.com/Wei-Shaw/sub2api/internal/util/urlvalidator"
	"github.com/cespare/xxhash/v2"
	"github.com/gin-gonic/gin"
	"github.com/google/uuid"
	"github.com/tidwall/gjson"
	"github.com/tidwall/sjson"
	"go.uber.org/zap"
)

const (
	// ChatGPT internal API for OAuth accounts
	chatgptCodexURL = "https://chatgpt.com/backend-api/codex/responses"
	// OpenAI Platform API for API Key accounts (fallback)
	openaiPlatformAPIURL   = "https://api.openai.com/v1/responses"
	openaiStickySessionTTL = time.Hour // 粘性会话TTL
	// 与真实 Codex CLI 的 User-Agent 结构对齐：
	// {originator}/{version} ({OS} {OS_version}; {arch}) {terminal}
	// 旧值 "codex_cli_rs/0.125.0" 缺少 OS/架构/终端后缀，易被上游指纹识别为非官方客户端。
	codexCLIUserAgent = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
	// codex_cli_only 拒绝时单个请求头日志长度上限（字符）
	codexCLIOnlyHeaderValueMaxBytes = 256

	// OpenAI WS Mode 失败后的重连次数上限（不含首次尝试）。
	// 与 Codex 客户端保持一致：失败后最多重连 5 次。
	openAIWSReconnectRetryLimit = 5
	// 上游错误体只需要提取错误 JSON/日志摘要，默认 512KiB 避免错误风暴叠加大请求体。
	openAIUpstreamErrorBodyReadLimit int64 = 512 << 10
	// OpenAI WS Mode 重连退避默认值（可由配置覆盖）。
	openAIWSRetryBackoffInitialDefault = 120 * time.Millisecond
	openAIWSRetryBackoffMaxDefault     = 2 * time.Second
	openAIWSRetryJitterRatioDefault    = 0.2
	openAICompactSessionSeedKey        = "openai_compact_session_seed"
	codexCLIVersion                    = "0.125.0"
	// Codex 限额快照仅用于后台展示/诊断，不需要每个成功请求都立即落库。
	openAICodexSnapshotPersistMinInterval = 30 * time.Second
	// 配额自动暂停时，超过该时长仍未刷新的 used% 快照视为陈旧，不再据此暂停账号。
	// 被暂停的账号收不到流量，其快照永远不会从上游响应头刷新；该兜底让账号在快照
	// 陈旧时放行一次请求，从而通过正常响应头自愈，而无需等待整个窗口（5h/7d）重置。
	openAICodexAutoPauseStaleAfter = 2 * time.Hour
)

// OpenAI allowed headers whitelist (for non-passthrough).
var openaiAllowedHeaders = map[string]bool{
	"accept-language":       true,
	"content-type":          true,
	"conversation_id":       true,
	"user-agent":            true,
	"originator":            true,
	"session_id":            true,
	"x-codex-turn-state":    true,
	"x-codex-turn-metadata": true,
}

// OpenAI passthrough allowed headers whitelist.
// 透传模式下仅放行这些低风险请求头，避免将非标准/环境噪声头传给上游触发风控。
var openaiPassthroughAllowedHeaders = map[string]bool{
	"accept":                true,
	"accept-language":       true,
	"content-type":          true,
	"conversation_id":       true,
	"openai-beta":           true,
	"user-agent":            true,
	"originator":            true,
	"session_id":            true,
	"x-codex-turn-state":    true,
	"x-codex-turn-metadata": true,
}

// codex_cli_only 拒绝时记录的请求头白名单（仅用于诊断日志，不参与上游透传）
var codexCLIOnlyDebugHeaderWhitelist = []string{
	"User-Agent",
	"Content-Type",
	"Accept",
	"Accept-Language",
	"OpenAI-Beta",
	"Originator",
	"Session_ID",
	"Conversation_ID",
	"X-Request-ID",
	"X-Client-Request-ID",
	"X-Forwarded-For",
	"X-Real-IP",
}

// OpenAICodexUsageSnapshot represents Codex API usage limits from response headers
type OpenAICodexUsageSnapshot struct {
	PrimaryUsedPercent          *float64 `json:"primary_used_percent,omitempty"`
	PrimaryResetAfterSeconds    *int     `json:"primary_reset_after_seconds,omitempty"`
	PrimaryWindowMinutes        *int     `json:"primary_window_minutes,omitempty"`
	SecondaryUsedPercent        *float64 `json:"secondary_used_percent,omitempty"`
	SecondaryResetAfterSeconds  *int     `json:"secondary_reset_after_seconds,omitempty"`
	SecondaryWindowMinutes      *int     `json:"secondary_window_minutes,omitempty"`
	PrimaryOverSecondaryPercent *float64 `json:"primary_over_secondary_percent,omitempty"`
	UpdatedAt                   string   `json:"updated_at,omitempty"`
}

// NormalizedCodexLimits contains normalized 5h/7d rate limit data
type NormalizedCodexLimits struct {
	Used5hPercent   *float64
	Reset5hSeconds  *int
	Window5hMinutes *int
	Used7dPercent   *float64
	Reset7dSeconds  *int
	Window7dMinutes *int
}

// Normalize converts primary/secondary fields to canonical 5h/7d fields.
// Strategy: Compare window_minutes to determine which is 5h vs 7d.
// Returns nil if snapshot is nil or has no useful data.
func (s *OpenAICodexUsageSnapshot) Normalize() *NormalizedCodexLimits {
	if s == nil {

exec
/bin/bash -lc "sed -n '1,120p' backend/internal/pkg/clientidentity/resolver_test.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
package clientidentity_test

import (
	"testing"

	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
	"github.com/Wei-Shaw/sub2api/internal/service"
	"github.com/stretchr/testify/assert"
)

func TestResolve(t *testing.T) {
	resolver := clientidentity.NewResolver()

	account := &service.Account{Extra: map[string]any{"client_family": "claude-cli"}}
	snapshot := resolver.Resolve(account)
	assert.NotNil(t, snapshot)
	assert.Equal(t, "claude-cli-default", snapshot.TLSProfileName)

	account2 := &service.Account{Extra: map[string]any{}}
	snapshot2 := resolver.Resolve(account2)
	assert.Nil(t, snapshot2)

	account3 := &service.Account{Extra: map[string]any{"client_family": "unknown"}}
	snapshot3 := resolver.Resolve(account3)
	assert.Nil(t, snapshot3)
}

codex
我准备开始改动：新增 `registry.go` 和 fetcher service，扩展 `GatewayConfig`，再把服务加入 Wire 依赖图。不会接入实际请求头落点，也不会加入真实网络拉取。
exec
/bin/bash -lc "sed -n '1360,1435p' backend/internal/config/config.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
func LoadForBootstrap() (*Config, error) {
	return load(true)
}

func load(allowMissingJWTSecret bool) (*Config, error) {
	viper.SetConfigName("config")
	viper.SetConfigType("yaml")

	// Add config paths in priority order
	// 1. DATA_DIR environment variable (highest priority)
	if dataDir := os.Getenv("DATA_DIR"); dataDir != "" {
		viper.AddConfigPath(dataDir)
	}
	// 2. Docker data directory
	viper.AddConfigPath("/app/data")
	// 3. Current directory
	viper.AddConfigPath(".")
	// 4. Config subdirectory
	viper.AddConfigPath("./config")
	// 5. System config directory
	viper.AddConfigPath("/etc/sub2api")

	// 环境变量支持
	viper.AutomaticEnv()
	viper.SetEnvKeyReplacer(strings.NewReplacer(".", "_"))

	// 默认值
	setDefaults()

	if err := viper.ReadInConfig(); err != nil {
		if _, ok := err.(viper.ConfigFileNotFoundError); !ok {
			return nil, fmt.Errorf("read config error: %w", err)
		}
		// 配置文件不存在时使用默认值
	}

	var cfg Config
	if err := viper.Unmarshal(&cfg); err != nil {
		return nil, fmt.Errorf("unmarshal config error: %w", err)
	}
	if cfg.Gateway.OpenAIScheduler.StickyEscapeTTFTMs == 0 {
		cfg.Gateway.OpenAIScheduler.StickyEscapeTTFTMs = 15000
	}
	if cfg.Gateway.OpenAIScheduler.StickyEscapeErrorRate == 0 {
		cfg.Gateway.OpenAIScheduler.StickyEscapeErrorRate = 0.5
	}
	if !cfg.Gateway.OpenAIScheduler.StickyEscapeEnabled && !viper.IsSet("gateway.openai_scheduler.sticky_escape_enabled") {
		cfg.Gateway.OpenAIScheduler.StickyEscapeEnabled = true
	}

	cfg.RunMode = NormalizeRunMode(cfg.RunMode)
	cfg.Server.Mode = strings.ToLower(strings.TrimSpace(cfg.Server.Mode))
	if cfg.Server.Mode == "" {
		cfg.Server.Mode = "debug"
	}
	cfg.Server.FrontendURL = strings.TrimSpace(cfg.Server.FrontendURL)
	cfg.JWT.Secret = strings.TrimSpace(cfg.JWT.Secret)
	cfg.LinuxDo.ClientID = strings.TrimSpace(cfg.LinuxDo.ClientID)
	cfg.LinuxDo.ClientSecret = strings.TrimSpace(cfg.LinuxDo.ClientSecret)
	cfg.LinuxDo.AuthorizeURL = strings.TrimSpace(cfg.LinuxDo.AuthorizeURL)
	cfg.LinuxDo.TokenURL = strings.TrimSpace(cfg.LinuxDo.TokenURL)
	cfg.LinuxDo.UserInfoURL = strings.TrimSpace(cfg.LinuxDo.UserInfoURL)
	cfg.LinuxDo.Scopes = strings.TrimSpace(cfg.LinuxDo.Scopes)
	cfg.LinuxDo.RedirectURL = strings.TrimSpace(cfg.LinuxDo.RedirectURL)
	cfg.LinuxDo.FrontendRedirectURL = strings.TrimSpace(cfg.LinuxDo.FrontendRedirectURL)
	cfg.LinuxDo.TokenAuthMethod = strings.ToLower(strings.TrimSpace(cfg.LinuxDo.TokenAuthMethod))
	cfg.LinuxDo.UserInfoEmailPath = strings.TrimSpace(cfg.LinuxDo.UserInfoEmailPath)
	cfg.LinuxDo.UserInfoIDPath = strings.TrimSpace(cfg.LinuxDo.UserInfoIDPath)
	cfg.LinuxDo.UserInfoUsernamePath = strings.TrimSpace(cfg.LinuxDo.UserInfoUsernamePath)
	applyLegacyWeChatConnectEnvCompatibility(&cfg.WeChat)
	normalizeWeChatConnectConfig(&cfg.WeChat)
	cfg.OIDC.ProviderName = strings.TrimSpace(cfg.OIDC.ProviderName)
	cfg.OIDC.ClientID = strings.TrimSpace(cfg.OIDC.ClientID)
	cfg.OIDC.ClientSecret = strings.TrimSpace(cfg.OIDC.ClientSecret)
	cfg.OIDC.IssuerURL = strings.TrimSpace(cfg.OIDC.IssuerURL)
	cfg.OIDC.DiscoveryURL = strings.TrimSpace(cfg.OIDC.DiscoveryURL)

exec
/bin/bash -lc "sed -n '2320,2465p' backend/internal/config/config.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
		if c.Dashboard.StatsTTLSeconds < 0 {
			return fmt.Errorf("dashboard_cache.stats_ttl_seconds must be non-negative")
		}
		if c.Dashboard.StatsRefreshTimeoutSeconds < 0 {
			return fmt.Errorf("dashboard_cache.stats_refresh_timeout_seconds must be non-negative")
		}
	}
	if c.DashboardAgg.Enabled {
		if c.DashboardAgg.IntervalSeconds <= 0 {
			return fmt.Errorf("dashboard_aggregation.interval_seconds must be positive")
		}
		if c.DashboardAgg.LookbackSeconds < 0 {
			return fmt.Errorf("dashboard_aggregation.lookback_seconds must be non-negative")
		}
		if c.DashboardAgg.BackfillMaxDays < 0 {
			return fmt.Errorf("dashboard_aggregation.backfill_max_days must be non-negative")
		}
		if c.DashboardAgg.BackfillEnabled && c.DashboardAgg.BackfillMaxDays == 0 {
			return fmt.Errorf("dashboard_aggregation.backfill_max_days must be positive")
		}
		if c.DashboardAgg.Retention.UsageLogsDays <= 0 {
			return fmt.Errorf("dashboard_aggregation.retention.usage_logs_days must be positive")
		}
		if c.DashboardAgg.Retention.UsageBillingDedupDays <= 0 {
			return fmt.Errorf("dashboard_aggregation.retention.usage_billing_dedup_days must be positive")
		}
		if c.DashboardAgg.Retention.UsageBillingDedupDays < c.DashboardAgg.Retention.UsageLogsDays {
			return fmt.Errorf("dashboard_aggregation.retention.usage_billing_dedup_days must be greater than or equal to usage_logs_days")
		}
		if c.DashboardAgg.Retention.HourlyDays <= 0 {
			return fmt.Errorf("dashboard_aggregation.retention.hourly_days must be positive")
		}
		if c.DashboardAgg.Retention.DailyDays <= 0 {
			return fmt.Errorf("dashboard_aggregation.retention.daily_days must be positive")
		}
		if c.DashboardAgg.RecomputeDays < 0 {
			return fmt.Errorf("dashboard_aggregation.recompute_days must be non-negative")
		}
	} else {
		if c.DashboardAgg.IntervalSeconds < 0 {
			return fmt.Errorf("dashboard_aggregation.interval_seconds must be non-negative")
		}
		if c.DashboardAgg.LookbackSeconds < 0 {
			return fmt.Errorf("dashboard_aggregation.lookback_seconds must be non-negative")
		}
		if c.DashboardAgg.BackfillMaxDays < 0 {
			return fmt.Errorf("dashboard_aggregation.backfill_max_days must be non-negative")
		}
		if c.DashboardAgg.Retention.UsageLogsDays < 0 {
			return fmt.Errorf("dashboard_aggregation.retention.usage_logs_days must be non-negative")
		}
		if c.DashboardAgg.Retention.UsageBillingDedupDays < 0 {
			return fmt.Errorf("dashboard_aggregation.retention.usage_billing_dedup_days must be non-negative")
		}
		if c.DashboardAgg.Retention.UsageBillingDedupDays > 0 &&
			c.DashboardAgg.Retention.UsageLogsDays > 0 &&
			c.DashboardAgg.Retention.UsageBillingDedupDays < c.DashboardAgg.Retention.UsageLogsDays {
			return fmt.Errorf("dashboard_aggregation.retention.usage_billing_dedup_days must be greater than or equal to usage_logs_days")
		}
		if c.DashboardAgg.Retention.HourlyDays < 0 {
			return fmt.Errorf("dashboard_aggregation.retention.hourly_days must be non-negative")
		}
		if c.DashboardAgg.Retention.DailyDays < 0 {
			return fmt.Errorf("dashboard_aggregation.retention.daily_days must be non-negative")
		}
		if c.DashboardAgg.RecomputeDays < 0 {
			return fmt.Errorf("dashboard_aggregation.recompute_days must be non-negative")
		}
	}
	if c.UsageCleanup.Enabled {
		if c.UsageCleanup.MaxRangeDays <= 0 {
			return fmt.Errorf("usage_cleanup.max_range_days must be positive")
		}
		if c.UsageCleanup.BatchSize <= 0 {
			return fmt.Errorf("usage_cleanup.batch_size must be positive")
		}
		if c.UsageCleanup.WorkerIntervalSeconds <= 0 {
			return fmt.Errorf("usage_cleanup.worker_interval_seconds must be positive")
		}
		if c.UsageCleanup.TaskTimeoutSeconds <= 0 {
			return fmt.Errorf("usage_cleanup.task_timeout_seconds must be positive")
		}
	} else {
		if c.UsageCleanup.MaxRangeDays < 0 {
			return fmt.Errorf("usage_cleanup.max_range_days must be non-negative")
		}
		if c.UsageCleanup.BatchSize < 0 {
			return fmt.Errorf("usage_cleanup.batch_size must be non-negative")
		}
		if c.UsageCleanup.WorkerIntervalSeconds < 0 {
			return fmt.Errorf("usage_cleanup.worker_interval_seconds must be non-negative")
		}
		if c.UsageCleanup.TaskTimeoutSeconds < 0 {
			return fmt.Errorf("usage_cleanup.task_timeout_seconds must be non-negative")
		}
	}
	if c.Idempotency.DefaultTTLSeconds <= 0 {
		return fmt.Errorf("idempotency.default_ttl_seconds must be positive")
	}
	if c.Idempotency.SystemOperationTTLSeconds <= 0 {
		return fmt.Errorf("idempotency.system_operation_ttl_seconds must be positive")
	}
	if c.Idempotency.ProcessingTimeoutSeconds <= 0 {
		return fmt.Errorf("idempotency.processing_timeout_seconds must be positive")
	}
	if c.Idempotency.FailedRetryBackoffSeconds <= 0 {
		return fmt.Errorf("idempotency.failed_retry_backoff_seconds must be positive")
	}
	if c.Idempotency.MaxStoredResponseLen <= 0 {
		return fmt.Errorf("idempotency.max_stored_response_len must be positive")
	}
	if c.Idempotency.CleanupIntervalSeconds <= 0 {
		return fmt.Errorf("idempotency.cleanup_interval_seconds must be positive")
	}
	if c.Idempotency.CleanupBatchSize <= 0 {
		return fmt.Errorf("idempotency.cleanup_batch_size must be positive")
	}
	if c.Gateway.MaxBodySize <= 0 {
		return fmt.Errorf("gateway.max_body_size must be positive")
	}
	if c.Gateway.UpstreamResponseReadMaxBytes <= 0 {
		return fmt.Errorf("gateway.upstream_response_read_max_bytes must be positive")
	}
	if c.Gateway.ProxyProbeResponseReadMaxBytes <= 0 {
		return fmt.Errorf("gateway.proxy_probe_response_read_max_bytes must be positive")
	}
	if c.Gateway.ResponseHeaderTimeout < 0 {
		return fmt.Errorf("gateway.response_header_timeout must be non-negative")
	}
	if c.Gateway.OpenAIResponseHeaderTimeout < 0 {
		return fmt.Errorf("gateway.openai_response_header_timeout must be non-negative")
	}
	if strings.TrimSpace(c.Gateway.ConnectionPoolIsolation) != "" {
		switch c.Gateway.ConnectionPoolIsolation {
		case ConnectionPoolIsolationProxy, ConnectionPoolIsolationAccount, ConnectionPoolIsolationAccountProxy:
		default:
			return fmt.Errorf("gateway.connection_pool_isolation must be one of: %s/%s/%s",
				ConnectionPoolIsolationProxy, ConnectionPoolIsolationAccount, ConnectionPoolIsolationAccountProxy)
		}
	}
	if c.Gateway.ImageConcurrency.MaxConcurrentRequests < 0 {
		return fmt.Errorf("gateway.image_concurrency.max_concurrent_requests must be non-negative")
	}
	switch strings.TrimSpace(c.Gateway.ImageConcurrency.OverflowMode) {
	case "", ImageConcurrencyOverflowModeReject, ImageConcurrencyOverflowModeWait:
	default:

exec
/bin/bash -lc 'rg -n "''^gateway:|ua_auto_fetch|config'"\\.ya?ml|config.example|GatewayConfig\" -g'*.yml' -g'*.yaml' -g'*.go' -g'*.md' backend deploy README.md README_JA.md" in /mnt/x/project/sub2api
exec
/bin/bash -lc 'go env GOPATH GOMOD' in /mnt/x/project/sub2api/backend
 succeeded in 270ms:
backend/internal/repository/http_upstream_benchmark_test.go:27:		Gateway: config.GatewayConfig{ResponseHeaderTimeout: 300},
backend/internal/repository/http_upstream_test.go:70:	s.cfg.Gateway = config.GatewayConfig{ResponseHeaderTimeout: 7}
backend/internal/repository/http_upstream_test.go:87:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:105:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:121:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:136:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:150:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:167:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:183:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:219:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:265:	s.cfg.Gateway = config.GatewayConfig{ResponseHeaderTimeout: 1}
backend/internal/repository/http_upstream_test.go:307:	s.cfg.Gateway = config.GatewayConfig{ConnectionPoolIsolation: config.ConnectionPoolIsolationAccount}
backend/internal/repository/http_upstream_test.go:319:	s.cfg.Gateway = config.GatewayConfig{ConnectionPoolIsolation: config.ConnectionPoolIsolationAccountProxy}
backend/internal/repository/http_upstream_test.go:331:	s.cfg.Gateway = config.GatewayConfig{ConnectionPoolIsolation: config.ConnectionPoolIsolationAccount}
backend/internal/repository/http_upstream_test.go:344:	s.cfg.Gateway = config.GatewayConfig{ConnectionPoolIsolation: config.ConnectionPoolIsolationAccount}
backend/internal/repository/http_upstream_test.go:359:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:378:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/repository/http_upstream_test.go:398:	s.cfg.Gateway = config.GatewayConfig{
backend/internal/service/admin_openai_ws_management_test.go:22:			Gateway: config.GatewayConfig{
backend/internal/handler/image_concurrency_limiter_test.go:120:			Gateway: config.GatewayConfig{
backend/internal/handler/image_concurrency_limiter_test.go:168:		cfg: &config.Config{Gateway: config.GatewayConfig{ImageConcurrency: config.ImageConcurrencyConfig{
backend/internal/handler/image_concurrency_limiter_test.go:212:		cfg: &config.Config{Gateway: config.GatewayConfig{ImageConcurrency: config.ImageConcurrencyConfig{
backend/internal/service/antigravity_gateway_service_test.go:46:	svc := newAntigravityTestService(&config.Config{Gateway: config.GatewayConfig{
backend/internal/service/antigravity_gateway_service_test.go:244:		settingService: NewSettingService(&antigravitySettingRepoStub{}, &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}}),
backend/internal/service/antigravity_gateway_service_test.go:611:		settingService: NewSettingService(&antigravitySettingRepoStub{}, &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}}),
backend/internal/service/antigravity_gateway_service_test.go:664:		settingService: NewSettingService(&antigravitySettingRepoStub{}, &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}}),
backend/internal/service/antigravity_gateway_service_test.go:734:		settingService: NewSettingService(&antigravitySettingRepoStub{}, &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}}),
backend/internal/service/antigravity_gateway_service_test.go:841:		settingService: NewSettingService(&antigravitySettingRepoStub{}, &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}}),
backend/internal/service/antigravity_gateway_service_test.go:869:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:909:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:953:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1000:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1043:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1079:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1114:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1149:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1172:		Gateway: config.GatewayConfig{StreamDataIntervalTimeout: 1, MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1195:		Gateway: config.GatewayConfig{StreamDataIntervalTimeout: 1, MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1225:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1256:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1280:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1311:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/antigravity_gateway_service_test.go:1351:		Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/gateway_hotpath_optimization_test.go:202:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_hotpath_optimization_test.go:254:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_hotpath_optimization_test.go:588:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_hotpath_optimization_test.go:599:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_hotpath_optimization_test.go:710:		Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:141:		Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:223:		Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:406:					cfg:              &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}},
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:446:		cfg:              &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}},
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:502:		cfg:              &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}},
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:564:		cfg:              &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}},
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:651:					Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:728:			Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize},
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:785:				Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:866:		Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:921:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:958:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1206:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1234:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1267:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1315:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1360:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1390:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1433:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:1463:			Gateway: config.GatewayConfig{
backend/internal/service/gemini_messages_compat_service_test.go:332:			Gateway: config.GatewayConfig{
backend/internal/service/gateway_service_streaming_test.go:21:		Gateway: config.GatewayConfig{
backend/internal/service/setting_service.go:3074:	// - 兼容 config.yaml/env（避免老部署因为未迁移到数据库设置而被意外关闭）
backend/internal/service/setting_service.go:3106:	// - 兼容 config.yaml/env
backend/internal/service/setting_service.go:3236:	// - 兼容 config.yaml/env
backend/internal/service/setting_service.go:3886:// - 若对应系统设置键存在，则覆盖 config.yaml/env 的值
backend/internal/service/setting_service.go:3887:// - 否则回退到 config.yaml/env 的值
backend/internal/service/setting_service.go:3975:// - 若对应系统设置键存在，则覆盖 config.yaml/env 的值
backend/internal/service/setting_service.go:3976:// - 否则回退到 config.yaml/env 的值
backend/internal/service/setting_service.go:4255:// - 若对应系统设置键存在，则覆盖 config.yaml/env 的值
backend/internal/service/setting_service.go:4256:// - 否则回退到 config.yaml/env 的值
backend/internal/service/gateway_streaming_test.go:26:			Gateway: config.GatewayConfig{
backend/internal/service/openai_client_restriction_detector_test.go:111:			Gateway: config.GatewayConfig{ForceCodexCLI: true},
backend/internal/service/scheduler_snapshot_hydration_test.go:254:			Gateway: config.GatewayConfig{
backend/internal/service/openai_compat_model_test.go:1246:		cfg: &config.Config{Gateway: config.GatewayConfig{
backend/internal/service/openai_compat_model_test.go:1293:		cfg: &config.Config{Gateway: config.GatewayConfig{
backend/internal/service/openai_compat_model_test.go:1510:		cfg: &config.Config{Gateway: config.GatewayConfig{
backend/internal/service/openai_images_test.go:427:	cfg := &config.Config{Gateway: config.GatewayConfig{
backend/internal/service/openai_images_test.go:1278:			Gateway: config.GatewayConfig{
backend/internal/service/openai_images_test.go:1692:			Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1157:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1192:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1224:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1254:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1295:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1336:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1372:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1412:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1455:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1491:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1528:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1573:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1607:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1639:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1675:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1710:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1745:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1848:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:1896:		Gateway: config.GatewayConfig{
backend/internal/service/openai_gateway_service_test.go:2674:	cfg := &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}}
backend/internal/service/openai_gateway_service_test.go:2705:	cfg := &config.Config{Gateway: config.GatewayConfig{MaxLineSize: defaultMaxLineSize}}
backend/internal/service/openai_gateway_service_codex_cli_only_test.go:45:		svc := &OpenAIGatewayService{cfg: &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: true}}}
backend/internal/service/openai_gateway_service_codex_cli_only_test.go:268:			Gateway: config.GatewayConfig{ForceCodexCLI: false},
backend/internal/service/openai_gateway_service_codex_cli_only_test.go:319:			Gateway: config.GatewayConfig{ForceCodexCLI: false},
backend/internal/service/openai_gateway_service_codex_cli_only_test.go:367:			Gateway: config.GatewayConfig{ForceCodexCLI: false},
backend/internal/service/openai_oauth_passthrough_test.go:349:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:424:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:482:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:529:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:577:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:626:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:668:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:726:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:768:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:909:				cfg:              &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:957:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:992:		cfg: &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1048:				cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1096:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1152:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1197:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1246:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1287:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1333:		cfg:          &config.Config{Gateway: config.GatewayConfig{ForceCodexCLI: false}},
backend/internal/service/openai_oauth_passthrough_test.go:1379:		cfg: &config.Config{Gateway: config.GatewayConfig{
backend/internal/service/openai_ws_http_bridge_test.go:33:			Gateway: config.GatewayConfig{
backend/internal/service/openai_ws_http_bridge_test.go:71:			Gateway: config.GatewayConfig{
backend/internal/service/openai_ws_http_bridge_test.go:194:		Gateway: config.GatewayConfig{
backend/internal/service/openai_sticky_compat_test.go:23:			Gateway: config.GatewayConfig{
backend/internal/service/openai_sticky_compat_test.go:48:			Gateway: config.GatewayConfig{
backend/internal/service/openai_sticky_compat_test.go:71:			Gateway: config.GatewayConfig{

 succeeded in 201ms:
/home/mci777/go
/mnt/x/project/sub2api/backend/go.mod

**Editing and inspecting memory usage**

I need to focus on editing our resources, considering maybe how memory is utilized. I should refer to MEMORY.md for specifics. Also, it’s essential to capture final citations accurately. I can tackle that later. I’m reminded to inspect the configuration, especially the example in the gateway section. Maintaining clarity in these aspects would really help streamline the process!
exec
/bin/bash -lc "sed -n '148,230p' deploy/config.example.yaml" in /mnt/x/project/sub2api
 succeeded in 0ms:
gateway:
  # Timeout for waiting upstream response headers (seconds)
  # 等待上游响应头超时时间（秒）
  response_header_timeout: 600
  # OpenAI/Codex upstream response header timeout (seconds, 0=disabled)
  # OpenAI/Codex 等待上游响应头超时时间（秒，0=禁用本地响应头超时）
  openai_response_header_timeout: 0
  # Max request body size in bytes (default: 256MB)
  # 请求体最大字节数（默认 256MB）
  max_body_size: 268435456
  # Max bytes to read for non-stream upstream responses (default: 8MB)
  # 非流式上游响应体读取上限（默认 8MB）
  upstream_response_read_max_bytes: 8388608
  # Max bytes to read for proxy probe responses (default: 1MB)
  # 代理探测响应体读取上限（默认 1MB）
  proxy_probe_response_read_max_bytes: 1048576
  # Enable Gemini upstream response header debug logs (default: false)
  # 是否开启 Gemini 上游响应头调试日志（默认 false）
  gemini_debug_response_headers: false
  # Sora max request body size in bytes (0=use max_body_size)
  # Sora 请求体最大字节数（0=使用 max_body_size）
  sora_max_body_size: 268435456
  # Sora stream timeout (seconds, 0=disable)
  # Sora 流式请求总超时（秒，0=禁用）
  sora_stream_timeout_seconds: 900
  # Sora non-stream timeout (seconds, 0=disable)
  # Sora 非流式请求超时（秒，0=禁用）
  sora_request_timeout_seconds: 180
  # Sora stream enforcement mode: force/error
  # Sora stream 强制策略：force/error
  sora_stream_mode: "force"
  # Sora model filters
  # Sora 模型过滤配置
  sora_model_filters:
    # Hide prompt-enhance models by default
    # 默认隐藏 prompt-enhance 模型
    hide_prompt_enhance: true
  # Require API key for /sora/media proxy (default: false)
  # /sora/media 是否强制要求 API Key（默认 true）
  sora_media_require_api_key: true
  # Sora media temporary signing key (empty disables signed URL)
  # Sora 媒体临时签名密钥（为空则禁用签名）
  sora_media_signing_key: ""
  # Signed URL TTL seconds (<=0 disables)
  # 临时签名 URL 有效期（秒，<=0 表示禁用）
  sora_media_signed_url_ttl_seconds: 900
  # Connection pool isolation strategy:
  # 连接池隔离策略：
  # - proxy: Isolate by proxy, same proxy shares connection pool (suitable for few proxies, many accounts)
  # - proxy: 按代理隔离，同一代理共享连接池（适合代理少、账户多）
  # - account: Isolate by account, same account shares connection pool (suitable for few accounts, strict isolation)
  # - account: 按账户隔离，同一账户共享连接池（适合账户少、需严格隔离）
  # - account_proxy: Isolate by account+proxy combination (default, finest granularity)
  # - account_proxy: 按账户+代理组合隔离（默认，最细粒度）
  connection_pool_isolation: "account_proxy"
  # Force Codex CLI mode: treat all /openai/v1/responses requests as Codex CLI.
  # 强制按 Codex CLI 处理 /openai/v1/responses 请求（用于网关未透传/改写 User-Agent 的兜底）。
  #
  # 注意：开启后会影响所有客户端的行为（不仅限于 VS Code / Codex CLI），请谨慎开启。
  force_codex_cli: false
  # Enable Codex image-generation bridge injection for /openai/v1/responses.
  # 是否为 Codex /responses 请求自动注入 image_generation 工具与桥接指令。
  #
  # Default false keeps text-only Codex requests text-only. Explicit client-provided
  # image_generation tools are still forwarded when the group allows image generation.
  # 默认 false：保持纯文本 Codex 请求不被改写；客户端显式提供 image_generation tool 时，
  # 仍会在分组允许图片生成的情况下正常转发。
  codex_image_generation_bridge_enabled: false
  # Optional: template file used to build the final top-level Codex `instructions`.
  # 可选：用于构建最终 Codex 顶层 `instructions` 的模板文件路径。
  #
  # This is applied on the `/v1/messages -> Responses/Codex` conversion path,
  # after Claude `system` has already been normalized into Codex `instructions`.
  # 该模板作用于 `/v1/messages -> Responses/Codex` 转换链路，且发生在 Claude `system`
  # 已经被归一化为 Codex `instructions` 之后。
  #
  # The template can reference:
  # 模板可引用：
  # - {{ .ExistingInstructions }} : converted client instructions/system
  # - {{ .OriginalModel }}        : original requested model
  # - {{ .NormalizedModel }}      : normalized routing model
  # - {{ .BillingModel }}         : billing model
  # - {{ .UpstreamModel }}        : final upstream model

exec
/bin/bash -lc "sed -n '1,120p' backend/internal/service/wire.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
package service

import (
	"context"
	"database/sql"
	"time"

	dbent "github.com/Wei-Shaw/sub2api/ent"
	"github.com/Wei-Shaw/sub2api/internal/config"
	"github.com/Wei-Shaw/sub2api/internal/payment"
	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
	"github.com/google/wire"
	"github.com/redis/go-redis/v9"
)

// BuildInfo contains build information
type BuildInfo struct {
	Version   string
	BuildType string
}

// ProvidePricingService creates and initializes PricingService
func ProvidePricingService(cfg *config.Config, remoteClient PricingRemoteClient) (*PricingService, error) {
	svc := NewPricingService(cfg, remoteClient)
	if err := svc.Initialize(); err != nil {
		// Pricing service initialization failure should not block startup, use fallback prices
		println("[Service] Warning: Pricing service initialization failed:", err.Error())
	}
	return svc, nil
}

// ProvideUpdateService creates UpdateService with BuildInfo
func ProvideUpdateService(cache UpdateCache, githubClient GitHubReleaseClient, buildInfo BuildInfo) *UpdateService {
	return NewUpdateService(cache, githubClient, buildInfo.Version, buildInfo.BuildType)
}

// ProvideEmailQueueService creates EmailQueueService with default worker count
func ProvideEmailQueueService(emailService *EmailService) *EmailQueueService {
	return NewEmailQueueService(emailService, 3)
}

// ProvideOAuthRefreshAPI creates OAuthRefreshAPI with the default lock TTL.
func ProvideOAuthRefreshAPI(accountRepo AccountRepository, tokenCache GeminiTokenCache) *OAuthRefreshAPI {
	return NewOAuthRefreshAPI(accountRepo, tokenCache)
}

// ProvideOpenAIOAuthService creates OpenAIOAuthService with privacy/account enrichment support.
func ProvideOpenAIOAuthService(
	proxyRepo ProxyRepository,
	oauthClient OpenAIOAuthClient,
	privacyClientFactory PrivacyClientFactory,
) *OpenAIOAuthService {
	svc := NewOpenAIOAuthService(proxyRepo, oauthClient)
	svc.SetPrivacyClientFactory(privacyClientFactory)
	return svc
}

// ProvideTokenRefreshService creates and starts TokenRefreshService
func ProvideTokenRefreshService(
	accountRepo AccountRepository,
	oauthService *OAuthService,
	openaiOAuthService *OpenAIOAuthService,
	geminiOAuthService *GeminiOAuthService,
	antigravityOAuthService *AntigravityOAuthService,
	cacheInvalidator TokenCacheInvalidator,
	schedulerCache SchedulerCache,
	cfg *config.Config,
	tempUnschedCache TempUnschedCache,
	privacyClientFactory PrivacyClientFactory,
	proxyRepo ProxyRepository,
	refreshAPI *OAuthRefreshAPI,
	runtimeBlocker AccountRuntimeBlocker,
) *TokenRefreshService {
	svc := NewTokenRefreshService(accountRepo, oauthService, openaiOAuthService, geminiOAuthService, antigravityOAuthService, cacheInvalidator, schedulerCache, cfg, tempUnschedCache)
	// 注入 OpenAI privacy opt-out 依赖
	svc.SetPrivacyDeps(privacyClientFactory, proxyRepo)
	// 注入统一 OAuth 刷新 API（消除 TokenRefreshService 与 TokenProvider 之间的竞争条件）
	svc.SetRefreshAPI(refreshAPI)
	// 调用侧显式注入后台刷新策略，避免策略漂移
	svc.SetRefreshPolicy(DefaultBackgroundRefreshPolicy())
	svc.SetAccountRuntimeBlocker(runtimeBlocker)
	svc.Start()
	return svc
}

// ProvideClaudeTokenProvider creates ClaudeTokenProvider with OAuthRefreshAPI injection
func ProvideClaudeTokenProvider(
	accountRepo AccountRepository,
	tokenCache GeminiTokenCache,
	oauthService *OAuthService,
	refreshAPI *OAuthRefreshAPI,
) *ClaudeTokenProvider {
	p := NewClaudeTokenProvider(accountRepo, tokenCache, oauthService)
	executor := NewClaudeTokenRefresher(oauthService)
	p.SetRefreshAPI(refreshAPI, executor)
	p.SetRefreshPolicy(ClaudeProviderRefreshPolicy())
	return p
}

// ProvideOpenAITokenProvider creates OpenAITokenProvider with OAuthRefreshAPI injection
func ProvideOpenAITokenProvider(
	accountRepo AccountRepository,
	tokenCache GeminiTokenCache,
	openaiOAuthService *OpenAIOAuthService,
	refreshAPI *OAuthRefreshAPI,
) *OpenAITokenProvider {
	p := NewOpenAITokenProvider(accountRepo, tokenCache, openaiOAuthService)
	executor := NewOpenAITokenRefresher(openaiOAuthService, accountRepo)
	p.SetRefreshAPI(refreshAPI, executor)
	p.SetRefreshPolicy(OpenAIProviderRefreshPolicy())
	return p
}

// ProvideOpenAIQuotaService wires the OpenAI quota query/reset service.
// It depends on the OpenAI token provider for refreshed access tokens and the
// privacy client factory for the impersonated upstream HTTP client.
func ProvideOpenAIQuotaService(
	accountRepo AccountRepository,
	proxyRepo ProxyRepository,

exec
/bin/bash -lc "rg -n \"func Test.*Disabled|time.Sleep\\(50 \\* time.Millisecond\\)|require\\.Same|assert\\.Same\" backend/internal/service backend/internal/pkg -g'*_test.go'" in /mnt/x/project/sub2api
exec
/bin/bash -lc "rg -n \"type .* struct \\{.*stop|stopCh|sync.Once|Stop\\(\\)\" backend/internal/service -g'*.go' | head -80" in /mnt/x/project/sub2api
 succeeded in 295ms:
backend/internal/service/wire_test.go:36:	svc.Stop()
backend/internal/service/account_expiry_service.go:14:	stopCh      chan struct{}
backend/internal/service/account_expiry_service.go:15:	stopOnce    sync.Once
backend/internal/service/account_expiry_service.go:23:		stopCh:      make(chan struct{}),
backend/internal/service/account_expiry_service.go:35:		defer ticker.Stop()
backend/internal/service/account_expiry_service.go:42:			case <-s.stopCh:
backend/internal/service/account_expiry_service.go:49:func (s *AccountExpiryService) Stop() {
backend/internal/service/account_expiry_service.go:54:		close(s.stopCh)
backend/internal/service/idempotency_cleanup_service.go:18:	startOnce sync.Once
backend/internal/service/idempotency_cleanup_service.go:19:	stopOnce  sync.Once
backend/internal/service/idempotency_cleanup_service.go:20:	stopCh    chan struct{}
backend/internal/service/idempotency_cleanup_service.go:38:		stopCh:   make(chan struct{}),
backend/internal/service/idempotency_cleanup_service.go:52:func (s *IdempotencyCleanupService) Stop() {
backend/internal/service/idempotency_cleanup_service.go:57:		close(s.stopCh)
backend/internal/service/idempotency_cleanup_service.go:64:	defer ticker.Stop()
backend/internal/service/idempotency_cleanup_service.go:73:		case <-s.stopCh:
backend/internal/service/oauth_service_test.go:138:	svc.Stop()
backend/internal/service/oauth_service_test.go:145:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:185:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:206:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:230:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:270:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:340:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:370:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:407:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:437:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:449:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:473:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:508:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:557:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:592:	defer svc.Stop()
backend/internal/service/oauth_service_test.go:616:	svc.Stop()
backend/internal/service/oauth_service_test.go:619:	svc.Stop()
backend/internal/service/auth_pending_identity_service_test.go:296:	var firstCreate sync.Once
backend/internal/service/oauth_service.go:307:func (s *OAuthService) Stop() {
backend/internal/service/oauth_service.go:308:	s.sessionStore.Stop()
backend/internal/service/antigravity_oauth_service.go:483:func (s *AntigravityOAuthService) Stop() {
backend/internal/service/antigravity_oauth_service.go:484:	s.sessionStore.Stop()
backend/internal/service/content_moderation.go:1236:	defer timer.Stop()
backend/internal/service/content_moderation.go:1402:	defer timer.Stop()
backend/internal/service/concurrency_service.go:501:		defer ticker.Stop()
backend/internal/service/billing_cache_service_test.go:124:	svc.Stop()
backend/internal/service/user_platform_quota_flusher_test.go:247:// 场景 5: NilSafe — var f *UserPlatformQuotaUsageFlusher; f.flush(); f.Stop() 不 panic
backend/internal/service/user_platform_quota_flusher_test.go:254:	f.Stop()
backend/internal/service/antigravity_gateway_service.go:286:				timer.Stop()
backend/internal/service/antigravity_gateway_service.go:468:			timer.Stop()
backend/internal/service/antigravity_gateway_service.go:2578:		timer.Stop()
backend/internal/service/antigravity_gateway_service.go:3164:		defer intervalTicker.Stop()
backend/internal/service/antigravity_gateway_service.go:3179:		defer keepaliveTicker.Stop()
backend/internal/service/antigravity_gateway_service.go:3357:		defer intervalTicker.Stop()
backend/internal/service/antigravity_gateway_service.go:3823:		defer intervalTicker.Stop()
backend/internal/service/antigravity_gateway_service.go:4014:		defer intervalTicker.Stop()
backend/internal/service/antigravity_gateway_service.go:4029:		defer keepaliveTicker.Stop()
backend/internal/service/antigravity_gateway_service.go:4454:		defer intervalTicker.Stop()
backend/internal/service/antigravity_gateway_service.go:4469:		defer keepaliveTicker.Stop()
backend/internal/service/gateway_service.go:4063:		if !timer.Stop() {
backend/internal/service/gateway_service.go:5912:		defer intervalTicker.Stop()
backend/internal/service/gateway_service.go:5926:		defer keepaliveTicker.Stop()
backend/internal/service/gateway_service.go:8066:		defer intervalTicker.Stop()
backend/internal/service/gateway_service.go:8081:		defer keepaliveTicker.Stop()
backend/internal/service/user_platform_quota_flusher.go:260:func (s *UserPlatformQuotaUsageFlusher) Stop() {
backend/internal/service/user_msg_queue_service.go:46:	stopCh   chan struct{} // graceful shutdown
backend/internal/service/user_msg_queue_service.go:47:	stopOnce sync.Once     // 确保 Stop() 并发安全
backend/internal/service/user_msg_queue_service.go:56:		stopCh:   make(chan struct{}),
backend/internal/service/user_msg_queue_service.go:191:	defer timer.Stop()
backend/internal/service/user_msg_queue_service.go:284:		defer ticker.Stop()
backend/internal/service/user_msg_queue_service.go:287:			case <-s.stopCh:
backend/internal/service/user_msg_queue_service.go:297:func (s *UserMessageQueueService) Stop() {
backend/internal/service/user_msg_queue_service.go:298:	if s != nil && s.stopCh != nil {
backend/internal/service/user_msg_queue_service.go:300:			close(s.stopCh)
backend/internal/service/billing_cache_service.go:114:	cacheWriteStopOnce sync.Once
backend/internal/service/billing_cache_service.go:153:func (s *BillingCacheService) Stop() {
backend/internal/service/bedrock_stream.go:100:		defer intervalTicker.Stop()
backend/internal/service/email_queue_service.go:32:	stopChan     chan struct{}
backend/internal/service/email_queue_service.go:45:		stopChan:     make(chan struct{}),
backend/internal/service/email_queue_service.go:72:		case <-s.stopChan:
backend/internal/service/email_queue_service.go:140:func (s *EmailQueueService) Stop() {
backend/internal/service/email_queue_service.go:141:	close(s.stopChan)
backend/internal/service/gemini_oauth_service_test.go:491:	defer svc.Stop()
backend/internal/service/gemini_oauth_service_test.go:691:			defer svc.Stop()

 succeeded in 333ms:
backend/internal/service/user_service_test.go:524:func TestGetProfileIdentitySummaries_HidesBindActionWhenProviderExplicitlyDisabled(t *testing.T) {
backend/internal/pkg/apicompat/anthropic_responses_test.go:1044:func TestAnthropicToResponses_ThinkingDisabled(t *testing.T) {
backend/internal/service/channel_available_test.go:306:	require.Same(t, existing, models[0].Pricing)
backend/internal/service/billing_service_test.go:749:func TestCalculateCostWithLongContext_DisabledThreshold(t *testing.T) {
backend/internal/service/usage_record_worker_pool_test.go:279:func TestUsageRecordWorkerPool_AutoScaleDisabledKeepsFixedConcurrency(t *testing.T) {
backend/internal/service/usage_record_worker_pool_test.go:312:func TestUsageRecordWorkerPool_OptionsFromConfig_AutoScaleDisabled(t *testing.T) {
backend/internal/service/usage_record_worker_pool_test.go:398:func TestUsageRecordWorkerPool_NormalizeOptions_SampleAndAutoScaleDisabled(t *testing.T) {
backend/internal/service/usage_cleanup_service_test.go:327:func TestUsageCleanupServiceCreateTaskDisabled(t *testing.T) {
backend/internal/service/usage_cleanup_service_test.go:657:func TestUsageCleanupServiceCancelTaskDisabled(t *testing.T) {
backend/internal/service/account_websearch_test.go:20:func TestGetWebSearchEmulationMode_Disabled(t *testing.T) {
backend/internal/service/balance_notify_service_test.go:229:func TestCollectBalanceNotifyRecipients_FiltersDisabledAndUnverified(t *testing.T) {
backend/internal/service/auth_service_register_test.go:256:func TestAuthService_Register_Disabled(t *testing.T) {
backend/internal/service/auth_service_register_test.go:266:func TestAuthService_Register_DisabledByDefault(t *testing.T) {
backend/internal/service/auth_service_register_test.go:304:func TestAuthService_Register_DoesNotSnapshotOnDisabled(t *testing.T) {
backend/internal/service/auth_service_register_test.go:778:func TestCanBypassRegistrationDisabledForOAuth(t *testing.T) {
backend/internal/service/balance_notify_check_test.go:33:func TestCheckBalanceAfterDeduction_UserNotifyDisabled(t *testing.T) {
backend/internal/service/balance_notify_check_test.go:42:func TestCheckBalanceAfterDeduction_GlobalDisabled(t *testing.T) {
backend/internal/service/balance_notify_check_test.go:105:func TestCheckAccountQuotaAfterIncrement_GlobalDisabled(t *testing.T) {
backend/internal/service/balance_notify_check_test.go:137:func TestGetBalanceNotifyConfig_Disabled(t *testing.T) {
backend/internal/service/balance_notify_check_test.go:247:func TestCheckQuotaDimCrossings_DisabledDimension(t *testing.T) {
backend/internal/service/auth_service_turnstile_register_test.go:88:func TestAuthService_VerifyTurnstileForRegister_NoSkipWhenEmailVerifyDisabled(t *testing.T) {
backend/internal/service/dashboard_service_test.go:217:func TestDashboardService_CacheDisabled_SkipsCache(t *testing.T) {
backend/internal/service/dashboard_service_test.go:371:func TestDashboardService_AggDisabled_UsesUsageLogsFallback(t *testing.T) {
backend/internal/service/auth_service_platform_quota_test.go:145:func TestResolveSignupGrantPlan_DisabledAuthSourceStillCarriesGlobalQuota(t *testing.T) {
backend/internal/service/api_key_service_cache_test.go:540:			time.Sleep(50 * time.Millisecond)
backend/internal/service/error_passthrough_service_test.go:469:func TestMatchRule_DisabledRule(t *testing.T) {
backend/internal/service/gemini_multiplatform_test.go:720:func TestGeminiMessagesCompatService_SelectAccountForModelWithExclusions_SkipDisabledMixedScheduling(t *testing.T) {
backend/internal/service/gateway_request_test.go:1503:				require.Same(t, tt.effort, got, "non-nil effort must be returned unchanged (same pointer)")
backend/internal/service/gateway_anthropic_apikey_passthrough_test.go:842:func TestGatewayService_AnthropicOAuth_SystemPromptInjectionCanBeDisabled(t *testing.T) {
backend/internal/service/gemini_messages_compat_service_test.go:325:func TestGeminiHandleNativeNonStreamingResponse_DebugDisabledDoesNotEmitHeaderLogs(t *testing.T) {
backend/internal/service/content_moderation_test.go:1745:func TestBuildContentModerationAccountDisabledEmailBody_ContainsBanDetails(t *testing.T) {
backend/internal/service/channel_websearch_test.go:27:func TestChannel_IsWebSearchEmulationEnabled_Disabled(t *testing.T) {
backend/internal/service/gateway_channel_restriction_test.go:200:func TestCheckChannelPricingRestriction_RestrictModelsDisabled(t *testing.T) {
backend/internal/service/channel_monitor_runner_test.go:146:func TestSchedule_DisabledRedirectsToUnschedule(t *testing.T) {
backend/internal/service/gateway_multiplatform_test.go:3316:	require.Same(t, hydratedGroup, got)
backend/internal/service/content_moderation_cyber_test.go:67:func TestRecordCyberPolicyEvent_DisabledWhenRiskControlOff(t *testing.T) {
backend/internal/service/gateway_websearch_emulation_test.go:239:func TestShouldEmulateWebSearch_GlobalDisabled(t *testing.T) {
backend/internal/service/gateway_websearch_emulation_test.go:257:func TestShouldEmulateWebSearch_AccountDisabled(t *testing.T) {
backend/internal/service/gateway_websearch_emulation_test.go:318:func TestShouldEmulateWebSearch_DefaultMode_ChannelDisabled(t *testing.T) {
backend/internal/service/channel_service_test.go:971:func TestIsModelRestricted_RestrictDisabled(t *testing.T) {
backend/internal/service/channel_bedrock_cc_test.go:29:func TestChannel_IsBedrockCCCompatEnabled_Disabled(t *testing.T) {
backend/internal/service/claude_token_provider_test.go:822:		time.Sleep(50 * time.Millisecond)
backend/internal/service/idempotency_test.go:509:	require.Same(t, coordinator, DefaultIdempotencyCoordinator())
backend/internal/service/oauth_refresh_api_test.go:523:			time.Sleep(50 * time.Millisecond) // slow refresh
backend/internal/service/openai_account_scheduler_test.go:269:func TestOpenAIGatewayService_SelectAccountWithScheduler_DefaultDisabledUsesLegacyLoadAwareness(t *testing.T) {
backend/internal/service/openai_account_scheduler_test.go:326:func TestOpenAIGatewayService_SelectAccountWithScheduler_DefaultDisabled_RequiredWSV2_SkipsHTTPOnlyAccount(t *testing.T) {
backend/internal/service/openai_account_scheduler_test.go:380:func TestOpenAIGatewayService_SelectAccountWithScheduler_DefaultDisabled_RequiredWSV2_NoAvailableAccount(t *testing.T) {
backend/internal/service/openai_account_scheduler_test.go:420:func TestOpenAIGatewayService_SelectAccountWithScheduler_DefaultDisabled_EmbeddingsSkipsChatOnlyAccount(t *testing.T) {
backend/internal/service/openai_account_scheduler_test.go:678:func TestOpenAIGatewayService_OpenAIAccountSchedulerMetrics_DisabledNoOp(t *testing.T) {
backend/internal/service/openai_account_scheduler_test.go:1476:func TestOpenAIGatewayService_SelectAccountWithScheduler_SessionStickyEscapeDisabledKeepsLegacyBehavior(t *testing.T) {
backend/internal/service/openai_ws_protocol_forward_test.go:359:func TestOpenAIGatewayService_Forward_RemovePreviousResponseIDWhenWSDisabled(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:930:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledPrevResponseStrictDropToFullCreate(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:1066:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledPrevResponseStrictDropBeforePreflightPingFailReconnects(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:1350:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledPrevResponsePreflightSkipForFunctionCallOutput(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:1482:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledFunctionCallOutputAutoAttachPreviousResponseID(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:1614:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledToolSearchOutputAutoAttachesPreviousResponseID(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:1749:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledFunctionCallOutputSkipsAutoAttachWhenLastResponseIDMissing(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:1882:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledFunctionCallOutputSkipsAutoAttachWhenToolCallContextPresent(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:2016:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledFunctionCallOutputAutoAttachWhenOnlyItemReferencesPresent(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:2292:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledStrictAffinityPreflightPingFailAutoRecoveryReconnects(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:2444:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledPreflightPingFailReplaysFunctionCallOutputWithContext(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:2599:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledPreflightPingFailClosesWhenFunctionCallOutputNeedsPreviousResponseID(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:2745:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledPreflightPingFailClosesWhenReplayHasFunctionCallOutput(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_session_test.go:3203:func TestOpenAIGatewayService_ProxyResponsesWebSocketFromClient_StoreDisabledStrictAffinityPreviousResponseNotFoundLayer2Recovery(t *testing.T) {
backend/internal/service/pricing_service_test.go:52:	require.Same(t, sparkPricing, got)
backend/internal/service/pricing_service_test.go:65:	require.Same(t, gpt52CodexPricing, got)
backend/internal/service/pricing_service_test.go:80:	require.Same(t, gpt52CodexPricing, got)
backend/internal/service/pricing_service_test.go:174:	require.Same(t, imagePricing, got)
backend/internal/service/openai_ws_pool_test.go:565:func TestOpenAIWSConnPool_EffectiveMaxConnsDisabledFallbackHardCap(t *testing.T) {
backend/internal/service/openai_oauth_passthrough_test.go:558:func TestOpenAIGatewayService_OAuthPassthrough_DisabledUsesLegacyTransform(t *testing.T) {
backend/internal/service/openai_ws_forwarder_ingress_test.go:145:func TestAlignStoreDisabledPreviousResponseID(t *testing.T) {
backend/internal/service/openai_ws_v2/passthrough_relay_internal_test.go:207:func TestRunIdleWatchdog_NoTimeoutWhenDisabled(t *testing.T) {
backend/internal/service/openai_ws_fallback_test.go:206:func TestOpenAIWSStoreDisabledConnMode(t *testing.T) {
backend/internal/service/openai_ws_fallback_test.go:219:func TestShouldForceNewConnOnStoreDisabled(t *testing.T) {
backend/internal/service/subscription_expiry_service_test.go:145:func TestSubscriptionExpiryService_ExpiryReminderDisabledSkipsSubscriptionScan(t *testing.T) {
backend/internal/service/ops_system_log_service_test.go:47:func TestOpsServiceListSystemLogs_MonitoringDisabled(t *testing.T) {
backend/internal/service/openai_ws_client_test.go:21:	require.Same(t, c1, c2, "同一代理地址应复用同一个 HTTP 客户端")
backend/internal/service/openai_compat_model_test.go:705:func TestForwardAsAnthropic_APIKeyMetadataSessionSurvivesChangingCacheControlAnchorAfterContinuationDisabled(t *testing.T) {
backend/internal/service/openai_image_generation_controls_test.go:18:func TestOpenAIGatewayServiceForward_RejectsDisabledImageGenerationIntents(t *testing.T) {
backend/internal/service/openai_image_generation_controls_test.go:57:func TestOpenAIGatewayServiceForward_DisabledGroupAllowsTextOnlyResponses(t *testing.T) {
backend/internal/service/openai_image_generation_controls_test.go:123:func TestOpenAIGatewayServiceForward_ExplicitImageToolWorksWithBridgeDisabled(t *testing.T) {
backend/internal/service/rate_limit_429_cooldown_test.go:84:func TestHandle429_FallbackDisabledSkipsLocalMark(t *testing.T) {
backend/internal/service/overload_cooldown_test.go:159:func TestSetOverloadCooldownSettings_DisabledNormalizesOutOfRange(t *testing.T) {
backend/internal/service/overload_cooldown_test.go:210:func TestHandle529_DisabledFromDB_SkipsAccount(t *testing.T) {
backend/internal/service/openai_gateway_service_hotpath_test.go:59:func TestOpenAIRequestView_ApplyPatchesDisabled(t *testing.T) {
backend/internal/service/openai_gateway_service_test.go:1930:func TestOpenAIInvalidBaseURLWhenAllowlistDisabled(t *testing.T) {
backend/internal/service/openai_gateway_service_test.go:1955:func TestOpenAIValidateUpstreamBaseURLDisabledRequiresHTTPS(t *testing.T) {
backend/internal/service/openai_gateway_service_test.go:1975:func TestOpenAIValidateUpstreamBaseURLDisabledAllowsHTTP(t *testing.T) {
backend/internal/service/setting_service_wechat_config_test.go:112:func TestSettingService_GetWeChatConnectOAuthConfig_IgnoresSyntheticDisabledCapabilitiesFromMigration118(t *testing.T) {
backend/internal/service/openai_token_provider_test.go:695:		time.Sleep(50 * time.Millisecond)
backend/internal/service/openai_sticky_compat_test.go:66:func TestSetStickySessionAccountID_DualWriteOldDisabled(t *testing.T) {
backend/internal/service/openai_gateway_service_codex_cli_only_test.go:35:		require.Same(t, expected, got)

exec
/bin/bash -lc "sed -n '130,190p' backend/cmd/server/main.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
		log.Fatalf("Failed to start setup server: %v", err)
	}
}

func runMainServer() {
	cfg, err := config.LoadForBootstrap()
	if err != nil {
		log.Fatalf("Failed to load config: %v", err)
	}
	if err := logger.Init(logger.OptionsFromConfig(cfg.Log)); err != nil {
		log.Fatalf("Failed to initialize logger: %v", err)
	}
	if cfg.RunMode == config.RunModeSimple {
		log.Println("⚠️  WARNING: Running in SIMPLE mode - billing and quota checks are DISABLED")
	}

	buildInfo := handler.BuildInfo{
		Version:   Version,
		BuildType: BuildType,
	}

	app, err := initializeApplication(buildInfo)
	if err != nil {
		log.Fatalf("Failed to initialize application: %v", err)
	}
	defer app.Cleanup()

	// 启动服务器
	go func() {
		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
			log.Fatalf("Failed to start server: %v", err)
		}
	}()

	log.Printf("Server started on %s", app.Server.Addr)

	// 等待中断信号
	quit := make(chan os.Signal, 1)
	signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM)
	<-quit

	log.Println("Shutting down server...")

	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()

	if err := app.Server.Shutdown(ctx); err != nil {
		log.Fatalf("Server forced to shutdown: %v", err)
	}

	log.Println("Server exited")
}

exec
/bin/bash -lc "rg -n \"func Provide.*Start|New.*\\(.*\\) \\*.*Service \\{\\n\\s*svc := New.*\\n\\s*svc.Start\" backend/internal/service/wire.go backend/internal/service -g'*.go'" in /mnt/x/project/sub2api
 exited 2 in 0ms:
rg: the literal "\n" is not allowed in a regex

Consider enabling multiline mode with the --multiline flag (or -U for short).
When multiline mode is enabled, new line characters can be matched.

**Considering cleanup implementation**

I’ll need to add parameters for the cleanup step to ensure everything is managed properly. This also creates a consumer, which means Wire will instantiate as needed. If there's an Application field, I want to make sure it's accessible and avoid any unused elements. I should add a `versionFetcher` parameter to the cleanup step and then remember to include the `app.Start` call in `main.go`. It's all about keeping things organized!
**Evaluating potential issues**

I'm thinking about the test disabled calls, where Start works but Stop doesn't. It seems Cleanup Stop might close even if it never started. If I try to Start after Stop, that could cause a panic, but maybe not? That's fine. I'm concerned about a situation where Start runs with Enabled true, and then Stop closes before the goroutine even starts; that's tricky because select would return. It's definitely something to keep an eye on!
**Exploring Wire generation**

I’m thinking about adding a section near the force_codex_cli to handle the absence of "config.yaml." It seems like I’ll need to manage Wire generation. I could try running `go run github.com/google/wire/cmd/wire ./cmd/server`. I wonder if I’ll need to consider network issues or if a module is required. It might be a good idea to take a closer look at the go.mod file concerning Wire.
codex
我会做两点小偏离并说明：`clientidentity` 不能直接 import `service` 里的 codex 常量，否则会形成循环依赖，所以 codex 兜底值先在 registry 内按现有常量镜像；Wire 通过 `Application` 字段和 `main.go` 启动来保证 fetcher 真被构建并启动。
exec
/bin/bash -lc 'rg -n "github.com/google/wire|module " go.mod go.sum' in /mnt/x/project/sub2api/backend
 succeeded in 0ms:
go.sum:169:github.com/google/wire v0.7.0 h1:JxUKI6+CVBgCO2WToKy/nQk0sS+amI9z9EjVmdaocj4=
go.sum:170:github.com/google/wire v0.7.0/go.mod h1:n6YbUQD9cPKTnHXEBN2DXlOp/mVADhVErcMFb0v3J18=
go.mod:1:module github.com/Wei-Shaw/sub2api
go.mod:22:	github.com/google/wire v0.7.0

codex
现在开始改文件：补 `clientidentity.Registry`、`VersionFetcherService`、配置字段和测试，并更新 Wire。改动会保持默认关闭，不改变现有请求行为。
apply patch
patch: completed
/mnt/x/project/sub2api/backend/internal/pkg/clientidentity/registry.go
/mnt/x/project/sub2api/backend/internal/pkg/clientidentity/registry_test.go
/mnt/x/project/sub2api/backend/internal/service/version_fetcher_service.go
/mnt/x/project/sub2api/backend/internal/service/version_fetcher_service_test.go
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

codex
新增文件已落地。下一步改配置和 Wire；我会同时把示例配置补上默认 false，方便回滚含义明确。
2026-06-21T04:26:18.795872Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /mnt/x/project/sub2api/backend/cmd/server/wire.go:
	app, err := initializeApplication(buildInfo)
	if err != nil {
		log.Fatalf("Failed to initialize application: %v", err)
	}
	defer app.Cleanup()
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

codex
补丁有一处上下文放错了：启动逻辑在 `main.go`，不是 `wire.go`。我会分小块重新应用，避免误改。
exec
/bin/bash -lc 'rg -n "UAAutoFetch|clientidentity.NewRegistry|VersionFetcherService|type Application|ua_auto_fetch" backend/internal/config/config.go backend/internal/service/wire.go backend/cmd/server/wire.go backend/cmd/server/main.go deploy/config.example.yaml' in /mnt/x/project/sub2api
 succeeded in 0ms:
backend/cmd/server/wire.go:26:type Application struct {

exec
/bin/bash -lc "sed -n '24,42p' backend/cmd/server/wire.go && sed -n '145,162p' backend/cmd/server/main.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
)

type Application struct {
	Server  *http.Server
	Cleanup func()
}

func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
	wire.Build(
		// Infrastructure layer ProviderSets
		config.ProviderSet,

		// Business layer ProviderSets
		repository.ProviderSet,
		service.ProviderSet,
		payment.ProviderSet,
		middleware.ProviderSet,
		handler.ProviderSet,


	buildInfo := handler.BuildInfo{
		Version:   Version,
		BuildType: BuildType,
	}

	app, err := initializeApplication(buildInfo)
	if err != nil {
		log.Fatalf("Failed to initialize application: %v", err)
	}
	defer app.Cleanup()

	// 启动服务器
	go func() {
		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
			log.Fatalf("Failed to start server: %v", err)
		}
	}()

diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

codex
确认没有半截补丁写入。我现在按四个独立补丁应用：配置、service provider、Application 字段/启动、示例配置。
apply patch
patch: completed
/mnt/x/project/sub2api/backend/internal/config/config.go
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..eb27dc60dc8696cf72bda0cc3bee6f2cbb435efe
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..eb27dc60dc8696cf72bda0cc3bee6f2cbb435efe
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

apply patch
patch: completed
/mnt/x/project/sub2api/backend/internal/config/config.go
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..7c2c881371e9a7ba97b087923581b12fbc2bb9a7
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..7c2c881371e9a7ba97b087923581b12fbc2bb9a7
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

apply patch
patch: completed
/mnt/x/project/sub2api/backend/internal/config/config.go
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}

codex
配置已扩展完，默认 false 和 1h 间隔已加。接下来把 registry/fetcher 加进服务依赖图，并让应用启动后调用 `Start()`。
apply patch
patch: completed
/mnt/x/project/sub2api/backend/cmd/server/main.go
/mnt/x/project/sub2api/backend/cmd/server/wire.go
/mnt/x/project/sub2api/backend/internal/service/wire.go
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

codex
服务依赖已加，启动点在 `main.go`。我还需要同步生成的 `wire_gen.go`，优先尝试运行 wire；失败再手工改生成文件。
exec
/bin/bash -lc 'go run github.com/google/wire/cmd/wire ./cmd/server' in /mnt/x/project/sub2api/backend
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

 exited 1 in 2071ms:
/home/mci777/go/pkg/mod/github.com/google/wire@v0.7.0/cmd/wire/main.go:34:2: missing go.sum entry for module providing package github.com/google/subcommands (imported by github.com/google/wire/cmd/wire); to add:
	go get github.com/google/wire/cmd/wire@v0.7.0

codex
Wire 运行被 go.sum 缺项挡住；不做 `go get` 扩大依赖变更。我会手工同步 `wire_gen.go`，只加入新构造和 Application 字段。
exec
/bin/bash -lc "sed -n '1,28p' backend/cmd/server/wire_gen.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"context"
	"github.com/Wei-Shaw/sub2api/ent"
	"github.com/Wei-Shaw/sub2api/internal/config"
	"github.com/Wei-Shaw/sub2api/internal/handler"
	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
	"github.com/Wei-Shaw/sub2api/internal/payment"
	"github.com/Wei-Shaw/sub2api/internal/repository"
	"github.com/Wei-Shaw/sub2api/internal/server"
	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
	"github.com/Wei-Shaw/sub2api/internal/service"
	"github.com/redis/go-redis/v9"
	"log"
	"net/http"
	"sync"
	"time"
)

import (
	_ "embed"

exec
/bin/bash -lc "sed -n '190,260p' backend/cmd/server/wire_gen.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
	dbDumper := repository.NewPgDumper(configConfig)
	backupService := service.ProvideBackupService(settingRepository, configConfig, secretEncryptor, backupObjectStoreFactory, dbDumper)
	backupHandler := admin.NewBackupHandler(backupService, userService)
	oAuthHandler := admin.NewOAuthHandler(oAuthService)
	openAIQuotaService := service.ProvideOpenAIQuotaService(accountRepository, proxyRepository, openAITokenProvider, privacyClientFactory)
	openAIOAuthHandler := admin.NewOpenAIOAuthHandler(openAIOAuthService, adminService, openAIQuotaService)
	geminiOAuthHandler := admin.NewGeminiOAuthHandler(geminiOAuthService)
	antigravityOAuthHandler := admin.NewAntigravityOAuthHandler(antigravityOAuthService)
	proxyHandler := admin.NewProxyHandler(adminService)
	adminRedeemHandler := admin.NewRedeemHandler(adminService, redeemService)
	promoHandler := admin.NewPromoHandler(promoService)
	encryptionKey, err := payment.ProvideEncryptionKey(configConfig)
	if err != nil {
		return nil, err
	}
	paymentConfigService := service.ProvidePaymentConfigService(client, settingRepository, encryptionKey)
	registry := payment.ProvideRegistry()
	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
	opsHandler := admin.NewOpsHandler(opsService)
	updateCache := repository.NewUpdateCache(redisClient)
	gitHubReleaseClient := repository.ProvideGitHubReleaseClient(configConfig)
	serviceBuildInfo := provideServiceBuildInfo(buildInfo)
	updateService := service.ProvideUpdateService(updateCache, gitHubReleaseClient, serviceBuildInfo)
	idempotencyRepository := repository.NewIdempotencyRepository(client, db)
	systemOperationLockService := service.ProvideSystemOperationLockService(idempotencyRepository, configConfig)
	systemHandler := handler.ProvideSystemHandler(updateService, systemOperationLockService)
	adminSubscriptionHandler := admin.NewSubscriptionHandler(subscriptionService)
	usageCleanupRepository := repository.NewUsageCleanupRepository(client, db)
	usageCleanupService := service.ProvideUsageCleanupService(usageCleanupRepository, timingWheelService, dashboardAggregationService, configConfig)
	adminUsageHandler := admin.NewUsageHandler(usageService, apiKeyService, adminService, usageCleanupService)
	userAttributeHandler := admin.NewUserAttributeHandler(userAttributeService)
	errorPassthroughRepository := repository.NewErrorPassthroughRepository(client)
	errorPassthroughCache := repository.NewErrorPassthroughCache(redisClient)
	errorPassthroughService := service.NewErrorPassthroughService(errorPassthroughRepository, errorPassthroughCache)
	errorPassthroughHandler := admin.NewErrorPassthroughHandler(errorPassthroughService)
	tlsFingerprintProfileHandler := admin.NewTLSFingerprintProfileHandler(tlsFingerprintProfileService)
	adminAPIKeyHandler := admin.NewAdminAPIKeyHandler(adminService)
	scheduledTestPlanRepository := repository.NewScheduledTestPlanRepository(db)
	scheduledTestResultRepository := repository.NewScheduledTestResultRepository(db)
	scheduledTestService := service.ProvideScheduledTestService(scheduledTestPlanRepository, scheduledTestResultRepository)
	scheduledTestHandler := admin.NewScheduledTestHandler(scheduledTestService)
	channelHandler := admin.NewChannelHandler(channelService, billingService, pricingService)
	channelMonitorHandler := admin.NewChannelMonitorHandler(channelMonitorService)
	channelMonitorRequestTemplateRepository := repository.NewChannelMonitorRequestTemplateRepository(client, db)
	channelMonitorRequestTemplateService := service.NewChannelMonitorRequestTemplateService(channelMonitorRequestTemplateRepository)
	channelMonitorRequestTemplateHandler := admin.NewChannelMonitorRequestTemplateHandler(channelMonitorRequestTemplateService)
	contentModerationRepository := repository.NewContentModerationRepository(db)
	contentModerationHashCache := repository.NewContentModerationHashCache(redisClient)
	contentModerationService := service.NewContentModerationService(settingRepository, contentModerationRepository, contentModerationHashCache, groupRepository, userRepository, apiKeyAuthCacheInvalidator, emailService)
	contentModerationHandler := admin.NewContentModerationHandler(contentModerationService)
	paymentHandler := admin.NewPaymentHandler(paymentService, paymentConfigService)
	affiliateHandler := admin.NewAffiliateHandler(affiliateService, adminService)
	complianceHandler := admin.NewComplianceHandler(settingService)
	adminHandlers := handler.ProvideAdminHandlers(dashboardHandler, adminUserHandler, groupHandler, accountHandler, adminAnnouncementHandler, dataManagementHandler, backupHandler, oAuthHandler, openAIOAuthHandler, geminiOAuthHandler, antigravityOAuthHandler, proxyHandler, adminRedeemHandler, promoHandler, settingHandler, opsHandler, systemHandler, adminSubscriptionHandler, adminUsageHandler, userAttributeHandler, errorPassthroughHandler, tlsFingerprintProfileHandler, adminAPIKeyHandler, scheduledTestHandler, channelHandler, channelMonitorHandler, channelMonitorRequestTemplateHandler, contentModerationHandler, paymentHandler, affiliateHandler, complianceHandler)
	usageRecordWorkerPool := service.NewUsageRecordWorkerPool(configConfig)
	userMsgQueueCache := repository.NewUserMsgQueueCache(redisClient)
	userMessageQueueService := service.ProvideUserMessageQueueService(userMsgQueueCache, rpmCache, configConfig)
	gatewayHandler := handler.NewGatewayHandler(gatewayService, geminiMessagesCompatService, antigravityGatewayService, userService, concurrencyService, billingCacheService, usageService, apiKeyService, usageRecordWorkerPool, errorPassthroughService, contentModerationService, userMessageQueueService, configConfig, settingService)
	openAIGatewayHandler := handler.NewOpenAIGatewayHandler(openAIGatewayService, concurrencyService, billingCacheService, apiKeyService, usageRecordWorkerPool, errorPassthroughService, contentModerationService, opsService, configConfig)
	handlerSettingHandler := handler.ProvideSettingHandler(settingService, buildInfo, notificationEmailService)
	totpHandler := handler.NewTotpHandler(totpService)
	handlerPaymentHandler := handler.NewPaymentHandler(paymentService, paymentConfigService, channelService)
	paymentWebhookHandler := handler.NewPaymentWebhookHandler(paymentService, registry)
	availableChannelHandler := handler.NewAvailableChannelHandler(channelService, apiKeyService, settingService)
	idempotencyCoordinator := service.ProvideIdempotencyCoordinator(idempotencyRepository, configConfig)
	idempotencyCleanupService := service.ProvideIdempotencyCleanupService(idempotencyRepository, configConfig)
	handlers := handler.ProvideHandlers(authHandler, userHandler, apiKeyHandler, usageHandler, redeemHandler, subscriptionHandler, announcementHandler, channelMonitorUserHandler, adminHandlers, gatewayHandler, openAIGatewayHandler, handlerSettingHandler, totpHandler, handlerPaymentHandler, paymentWebhookHandler, availableChannelHandler, idempotencyCoordinator, idempotencyCleanupService)
	jwtAuthMiddleware := middleware.NewJWTAuthMiddleware(authService, userService)
	adminAuthMiddleware := middleware.NewAdminAuthMiddleware(authService, userService, settingService)

exec
/bin/bash -lc "sed -n '330,375p' backend/cmd/server/wire_gen.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
	backupSvc *service.BackupService,
	paymentOrderExpiry *service.PaymentOrderExpiryService,
	channelMonitorRunner *service.ChannelMonitorRunner,
	quotaFlusher *service.UserPlatformQuotaUsageFlusher,
) func() {
	return func() {
		ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
		defer cancel()

		type cleanupStep struct {
			name string
			fn   func() error
		}

		parallelSteps := []cleanupStep{
			{"OpsScheduledReportService", func() error {
				if opsScheduledReport != nil {
					opsScheduledReport.Stop()
				}
				return nil
			}},
			{"OpsCleanupService", func() error {
				if opsCleanup != nil {
					opsCleanup.Stop()
				}
				return nil
			}},
			{"OpsSystemLogSink", func() error {
				if opsSystemLogSink != nil {
					opsSystemLogSink.Stop()
				}
				return nil
			}},
			{"OpsAlertEvaluatorService", func() error {
				if opsAlertEvaluator != nil {
					opsAlertEvaluator.Stop()
				}
				return nil
			}},
			{"OpsAggregationService", func() error {
				if opsAggregation != nil {
					opsAggregation.Stop()
				}
				return nil
			}},
			{"OpsMetricsCollector", func() error {

exec
/bin/bash -lc "sed -n '375,455p' backend/cmd/server/wire_gen.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
			{"OpsMetricsCollector", func() error {
				if opsMetricsCollector != nil {
					opsMetricsCollector.Stop()
				}
				return nil
			}},
			{"SchedulerSnapshotService", func() error {
				if schedulerSnapshot != nil {
					schedulerSnapshot.Stop()
				}
				return nil
			}},
			{"UsageCleanupService", func() error {
				if usageCleanup != nil {
					usageCleanup.Stop()
				}
				return nil
			}},
			{"IdempotencyCleanupService", func() error {
				if idempotencyCleanup != nil {
					idempotencyCleanup.Stop()
				}
				return nil
			}},
			{"TokenRefreshService", func() error {
				tokenRefresh.Stop()
				return nil
			}},
			{"AccountExpiryService", func() error {
				accountExpiry.Stop()
				return nil
			}},
			{"ProxyExpiryService", func() error {
				proxyExpiry.Stop()
				return nil
			}},
			{"SubscriptionExpiryService", func() error {
				subscriptionExpiry.Stop()
				return nil
			}},
			{"SubscriptionService", func() error {
				if subscriptionService != nil {
					subscriptionService.Stop()
				}
				return nil
			}},
			{"PricingService", func() error {
				pricing.Stop()
				return nil
			}},
			{"EmailQueueService", func() error {
				emailQueue.Stop()
				return nil
			}},
			{"BillingCacheService", func() error {
				billingCache.Stop()
				return nil
			}},
			{"UsageRecordWorkerPool", func() error {
				if usageRecordWorkerPool != nil {
					usageRecordWorkerPool.Stop()
				}
				return nil
			}},
			{"OAuthService", func() error {
				oauth.Stop()
				return nil
			}},
			{"OpenAIOAuthService", func() error {
				openaiOAuth.Stop()
				return nil
			}},
			{"GeminiOAuthService", func() error {
				geminiOAuth.Stop()
				return nil
			}},
			{"AntigravityOAuthService", func() error {
				antigravityOAuth.Stop()
				return nil
			}},
			{"OpenAIWSPool", func() error {

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

codex
手工同步时发现 `payment` 也有 `registry` 变量名。为了避免混淆，我会把新变量命名为 `identityRegistry`。
apply patch
patch: completed
/mnt/x/project/sub2api/backend/cmd/server/wire_gen.go
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

codex
`wire_gen.go` 已同步构造新服务。现在补示例配置，随后跑 gofmt 和目标构建测试。
exec
/bin/bash -lc 'rg -n "image_concurrency:" -A16 deploy/config.example.yaml' in /mnt/x/project/sub2api
 succeeded in 0ms:
378:  image_concurrency:
379-    # Enable image-only concurrency protection; false keeps existing behavior unchanged
380-    # 是否启用图片独立并发保护；false 保持现有行为不变
381-    enabled: false
382-    # Max concurrent image generation requests in this process, 0=unlimited
383-    # 当前进程允许同时处理的图片生成请求数，0=不限制
384-    max_concurrent_requests: 0
385-    # Overflow mode when the image concurrency limit is full: reject/wait
386-    # 图片并发满时的处理方式：reject=立即拒绝，wait=等待槽位
387-    overflow_mode: "reject"
388-    # Wait timeout for overflow_mode=wait (seconds), 0=do not wait
389-    # wait 模式等待图片并发槽位的超时时间（秒），0=不等待
390-    wait_timeout_seconds: 30
391-    # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
392-    # wait 模式当前进程允许排队等待的图片请求数，0=不限制
393-    max_waiting_requests: 100
394-  # SSE max line size in bytes (default: 40MB)

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

2026-06-21T04:27:53.845275Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in /mnt/x/project/sub2api/deploy/config.example.yaml:
    # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
    # wait 模式当前进程允许排队等待的图片请求数，0=不限制
    max_waiting_requests: 100
  # SSE max line size in bytes (default: 40MB)
  # SSE 单行最大字节数（默认 40MB）
  max_line_size: 524288000
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

codex
示例配置那段后面内容和预期不同，我先读近邻再精确插入。
exec
/bin/bash -lc "sed -n '386,402p' deploy/config.example.yaml" in /mnt/x/project/sub2api
 succeeded in 0ms:
    # 图片并发满时的处理方式：reject=立即拒绝，wait=等待槽位
    overflow_mode: "reject"
    # Wait timeout for overflow_mode=wait (seconds), 0=do not wait
    # wait 模式等待图片并发槽位的超时时间（秒），0=不等待
    wait_timeout_seconds: 30
    # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
    # wait 模式当前进程允许排队等待的图片请求数，0=不限制
    max_waiting_requests: 100
  # SSE max line size in bytes (default: 40MB)
  # SSE 单行最大字节数（默认 40MB）
  max_line_size: 41943040
  # Log upstream error response body summary (safe/truncated; does not log request content)
  # 记录上游错误响应体摘要（安全/截断；不记录请求内容）
  log_upstream_error_body: true
  # Max bytes to log from upstream error body
  # 记录上游错误响应体的最大字节数
  log_upstream_error_body_max_bytes: 2048

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,

apply patch
patch: completed
/mnt/x/project/sub2api/deploy/config.example.yaml
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

codex
示例配置已补。现在运行格式化和目标测试，先看编译错误再修。
exec
/bin/bash -lc 'gofmt -w internal/pkg/clientidentity/registry.go internal/pkg/clientidentity/registry_test.go internal/service/version_fetcher_service.go internal/service/version_fetcher_service_test.go internal/config/config.go internal/service/wire.go cmd/server/wire.go cmd/server/main.go cmd/server/wire_gen.go' in /mnt/x/project/sub2api/backend
 succeeded in 0ms:
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

codex
格式化完成。我会按用户给的四条验证命令执行；如果 service 包构建暴露 Wire 或配置问题，会直接修。
exec
/bin/bash -lc 'go build ./internal/pkg/clientidentity' in /mnt/x/project/sub2api/backend
exec
/bin/bash -lc 'go test ./internal/pkg/clientidentity -run TestRegistryAtomicSwap -v' in /mnt/x/project/sub2api/backend
exec
/bin/bash -lc 'go test ./internal/service -run TestVersionFetcherDisabledDoesNotStart -v' in /mnt/x/project/sub2api/backend
 succeeded in 195ms:
 succeeded in 7929ms:
=== RUN   TestRegistryAtomicSwap
--- PASS: TestRegistryAtomicSwap (0.00s)
PASS
ok  	github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity	0.012s

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

 succeeded in 12269ms:
=== RUN   TestVersionFetcherDisabledDoesNotStart
--- PASS: TestVersionFetcherDisabledDoesNotStart (0.05s)
PASS
ok  	github.com/Wei-Shaw/sub2api/internal/service	0.063s

codex
前三项验证通过。还剩 `go build ./internal/service`，这个包较大，我现在单独跑，顺便检查是否有生成文件遗漏。
exec
/bin/bash -lc 'go build ./internal/service' in /mnt/x/project/sub2api/backend
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

 succeeded in 7651ms:
codex
目标验证都通过。为了满足变更范围检查，我会看本次涉及文件 diff 和状态；GitNexus 的 `detect_changes()` 仍因工具不可用无法执行。
exec
/bin/bash -lc 'git diff -- backend/internal/pkg/clientidentity/registry.go backend/internal/pkg/clientidentity/registry_test.go backend/internal/service/version_fetcher_service.go backend/internal/service/version_fetcher_service_test.go backend/internal/config/config.go backend/internal/service/wire.go backend/cmd/server/wire.go backend/cmd/server/main.go backend/cmd/server/wire_gen.go deploy/config.example.yaml' in /mnt/x/project/sub2api
 succeeded in 0ms:
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309f..22eadfb0 100644
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@ func runMainServer() {
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaa..f4d272d4 100644
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@ import (
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a4..9db4172b 100644
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@ import (
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@ func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@ func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@ func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2ca..d006d34a 100644
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@ type GatewayConfig struct {
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@ type GatewayConfig struct {
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@ func setDefaults() {
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@ func (c *Config) Validate() error {
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f..8995f7df 100644
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@ import (
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@ var ProviderSet = wire.NewSet(
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964..cfa7467e 100644
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@ gateway:
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

exec
/bin/bash -lc 'git status --short -- backend/internal/pkg/clientidentity/registry.go backend/internal/pkg/clientidentity/registry_test.go backend/internal/service/version_fetcher_service.go backend/internal/service/version_fetcher_service_test.go backend/internal/config/config.go backend/internal/service/wire.go backend/cmd/server/wire.go backend/cmd/server/main.go backend/cmd/server/wire_gen.go deploy/config.example.yaml' in /mnt/x/project/sub2api
 succeeded in 0ms:
 M backend/cmd/server/main.go
 M backend/cmd/server/wire.go
 M backend/cmd/server/wire_gen.go
 M backend/internal/config/config.go
 M backend/internal/service/wire.go
 M deploy/config.example.yaml
?? backend/internal/pkg/clientidentity/registry.go
?? backend/internal/pkg/clientidentity/registry_test.go
?? backend/internal/service/version_fetcher_service.go
?? backend/internal/service/version_fetcher_service_test.go

exec
/bin/bash -lc "rg -n \"type Application struct|VersionFetcherService|clientidentity.NewRegistry|UAAutoFetch|ua_auto_fetch|func \\(s \\*VersionFetcherService\\)\" backend/internal/pkg/clientidentity/registry.go backend/internal/service/version_fetcher_service.go backend/internal/config/config.go backend/internal/service/wire.go backend/cmd/server/wire.go backend/cmd/server/main.go backend/cmd/server/wire_gen.go deploy/config.example.yaml" in /mnt/x/project/sub2api
 succeeded in 0ms:
deploy/config.example.yaml:396:  ua_auto_fetch:
backend/cmd/server/wire_gen.go:210:	identityRegistry := clientidentity.NewRegistry()
backend/cmd/server/wire_gen.go:211:	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
backend/cmd/server/wire_gen.go:283:		VersionFetcherService: versionFetcherService,
backend/cmd/server/wire_gen.go:291:type Application struct {
backend/cmd/server/wire_gen.go:293:	VersionFetcherService *service.VersionFetcherService
backend/internal/service/wire.go:603:	clientidentity.NewRegistry,
backend/internal/service/wire.go:604:	NewVersionFetcherService,
backend/cmd/server/main.go:157:	if app.VersionFetcherService != nil {
backend/cmd/server/main.go:158:		app.VersionFetcherService.Start()
backend/internal/service/version_fetcher_service.go:17:type VersionFetcherService struct {
backend/internal/service/version_fetcher_service.go:24:func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
backend/internal/service/version_fetcher_service.go:25:	return &VersionFetcherService{
backend/internal/service/version_fetcher_service.go:32:// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
backend/internal/service/version_fetcher_service.go:33:func (s *VersionFetcherService) Start() {
backend/internal/service/version_fetcher_service.go:34:	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
backend/internal/service/version_fetcher_service.go:38:	interval := s.cfg.Gateway.UAAutoFetch.Interval
backend/internal/service/version_fetcher_service.go:58:func (s *VersionFetcherService) Stop() {
backend/internal/service/version_fetcher_service.go:67:func (s *VersionFetcherService) fetchAndUpdate() {
backend/internal/service/version_fetcher_service.go:100:func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
backend/internal/service/version_fetcher_service.go:105:func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
backend/internal/service/version_fetcher_service.go:113:func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
backend/cmd/server/wire.go:26:type Application struct {
backend/cmd/server/wire.go:28:	VersionFetcherService *service.VersionFetcherService
backend/internal/config/config.go:728:	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
backend/internal/config/config.go:729:	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
backend/internal/config/config.go:806:type UAAutoFetchConfig struct {
backend/internal/config/config.go:1891:	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
backend/internal/config/config.go:1892:	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
backend/internal/config/config.go:2484:	if c.Gateway.UAAutoFetch.Interval < 0 {
backend/internal/config/config.go:2485:		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

**Figuring out cleanup steps**

I need to keep going with my thoughts on the cleanup process. It seems like there might be an issue with git diff not showing new files. I should inspect those. The user asked about initializing arguments for starting, but cleanup doesn’t seem to stop the version fetcher. I think better to include a stop in the cleanup as a step since the application has a service field. I’ll need to find the best way to incorporate that without duplicating dependencies.
**Clarifying deferred execution**

I’m thinking about the order of the deferred commands. If I place `defer app.Cleanup`, the deferred `Stop` will execute before `Cleanup`, which might work, but it can cause confusion if the output says to stop. The user asked to append `versionFetcherService` in the `initializeApplication` parameter list and invoke `Start()` afterward. I realize it’s not straightforward since `initializeApplication` is an injector. I’ll ensure to integrate `Stop` through the app cleanup and add it in the main function while keeping interactions in mind.
**Considering service start management**

I’m noticing a potential issue with calling `VersionFetcherService.Start` multiple times, which could create multiple goroutines while `Stop` only closes once. It’s not critical, but it could cause problems in testing. I think adding `startOnce` using `sync.Once` would be a good solution to prevent repeated calls to `Start`. Also, if `Stop` is called, it seems like restarting after closure might not be ideal. I need to compile and check for YAML decoding of `time.Duration` with Viper, looking into `StringToTimeDurationHook` and relevant unmarshaling options.
codex
变更范围检查显示只涉及本任务文件和示例配置。还有一个收口点：服务已提供 `Stop()`，我会在 main 里启动后注册 defer，避免开启后退出时后台 goroutine 残留。
exec
/bin/bash -lc "rg -n \"DecodeHook|StringToTimeDurationHook|Unmarshal\\(&cfg|mapstructure\" backend/internal/config/config.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
62:	Server                  ServerConfig                  `mapstructure:"server"`
63:	Log                     LogConfig                     `mapstructure:"log"`
64:	CORS                    CORSConfig                    `mapstructure:"cors"`
65:	Security                SecurityConfig                `mapstructure:"security"`
66:	Billing                 BillingConfig                 `mapstructure:"billing"`
67:	Turnstile               TurnstileConfig               `mapstructure:"turnstile"`
68:	Database                DatabaseConfig                `mapstructure:"database"`
69:	Redis                   RedisConfig                   `mapstructure:"redis"`
70:	Ops                     OpsConfig                     `mapstructure:"ops"`
71:	JWT                     JWTConfig                     `mapstructure:"jwt"`
72:	Totp                    TotpConfig                    `mapstructure:"totp"`
73:	LinuxDo                 LinuxDoConnectConfig          `mapstructure:"linuxdo_connect"`
74:	WeChat                  WeChatConnectConfig           `mapstructure:"wechat_connect"`
75:	OIDC                    OIDCConnectConfig             `mapstructure:"oidc_connect"`
76:	DingTalk                DingTalkConnectConfig         `mapstructure:"dingtalk_connect"`
77:	GitHubOAuth             EmailOAuthProviderConfig      `mapstructure:"github_oauth"`
78:	GoogleOAuth             EmailOAuthProviderConfig      `mapstructure:"google_oauth"`
79:	Default                 DefaultConfig                 `mapstructure:"default"`
80:	RateLimit               RateLimitConfig               `mapstructure:"rate_limit"`
81:	Pricing                 PricingConfig                 `mapstructure:"pricing"`
82:	Gateway                 GatewayConfig                 `mapstructure:"gateway"`
83:	APIKeyAuth              APIKeyAuthCacheConfig         `mapstructure:"api_key_auth_cache"`
84:	SubscriptionCache       SubscriptionCacheConfig       `mapstructure:"subscription_cache"`
85:	SubscriptionMaintenance SubscriptionMaintenanceConfig `mapstructure:"subscription_maintenance"`
86:	Dashboard               DashboardCacheConfig          `mapstructure:"dashboard_cache"`
87:	DashboardAgg            DashboardAggregationConfig    `mapstructure:"dashboard_aggregation"`
88:	UsageCleanup            UsageCleanupConfig            `mapstructure:"usage_cleanup"`
89:	Concurrency             ConcurrencyConfig             `mapstructure:"concurrency"`
90:	TokenRefresh            TokenRefreshConfig            `mapstructure:"token_refresh"`
91:	RunMode                 string                        `mapstructure:"run_mode" yaml:"run_mode"`
92:	Timezone                string                        `mapstructure:"timezone"` // e.g. "Asia/Shanghai", "UTC"
93:	Gemini                  GeminiConfig                  `mapstructure:"gemini"`
94:	Update                  UpdateConfig                  `mapstructure:"update"`
95:	Idempotency             IdempotencyConfig             `mapstructure:"idempotency"`
99:	Level           string            `mapstructure:"level"`
100:	Format          string            `mapstructure:"format"`
101:	ServiceName     string            `mapstructure:"service_name"`
102:	Environment     string            `mapstructure:"env"`
103:	Caller          bool              `mapstructure:"caller"`
104:	StacktraceLevel string            `mapstructure:"stacktrace_level"`
105:	Output          LogOutputConfig   `mapstructure:"output"`
106:	Rotation        LogRotationConfig `mapstructure:"rotation"`
107:	Sampling        LogSamplingConfig `mapstructure:"sampling"`
111:	ToStdout bool   `mapstructure:"to_stdout"`
112:	ToFile   bool   `mapstructure:"to_file"`
113:	FilePath string `mapstructure:"file_path"`
117:	MaxSizeMB  int  `mapstructure:"max_size_mb"`
118:	MaxBackups int  `mapstructure:"max_backups"`
119:	MaxAgeDays int  `mapstructure:"max_age_days"`
120:	Compress   bool `mapstructure:"compress"`
121:	LocalTime  bool `mapstructure:"local_time"`
125:	Enabled    bool `mapstructure:"enabled"`
126:	Initial    int  `mapstructure:"initial"`
127:	Thereafter int  `mapstructure:"thereafter"`
131:	OAuth GeminiOAuthConfig `mapstructure:"oauth"`
132:	Quota GeminiQuotaConfig `mapstructure:"quota"`
136:	ClientID     string `mapstructure:"client_id"`
137:	ClientSecret string `mapstructure:"client_secret"`
138:	Scopes       string `mapstructure:"scopes"`
142:	Tiers  map[string]GeminiTierQuotaConfig `mapstructure:"tiers"`
143:	Policy string                           `mapstructure:"policy"`
147:	ProRPD          *int64 `mapstructure:"pro_rpd" json:"pro_rpd"`
148:	FlashRPD        *int64 `mapstructure:"flash_rpd" json:"flash_rpd"`
149:	CooldownMinutes *int   `mapstructure:"cooldown_minutes" json:"cooldown_minutes"`
156:	ProxyURL string `mapstructure:"proxy_url"`
161:	ObserveOnly bool `mapstructure:"observe_only"`
163:	DefaultTTLSeconds int `mapstructure:"default_ttl_seconds"`
165:	SystemOperationTTLSeconds int `mapstructure:"system_operation_ttl_seconds"`
167:	ProcessingTimeoutSeconds int `mapstructure:"processing_timeout_seconds"`
169:	FailedRetryBackoffSeconds int `mapstructure:"failed_retry_backoff_seconds"`
171:	MaxStoredResponseLen int `mapstructure:"max_stored_response_len"`
173:	CleanupIntervalSeconds int `mapstructure:"cleanup_interval_seconds"`
175:	CleanupBatchSize int `mapstructure:"cleanup_batch_size"`
179:	Enabled             bool   `mapstructure:"enabled"`
180:	ClientID            string `mapstructure:"client_id"`
181:	ClientSecret        string `mapstructure:"client_secret"`
182:	AuthorizeURL        string `mapstructure:"authorize_url"`
183:	TokenURL            string `mapstructure:"token_url"`
184:	UserInfoURL         string `mapstructure:"userinfo_url"`
185:	Scopes              string `mapstructure:"scopes"`
186:	RedirectURL         string `mapstructure:"redirect_url"`          // 后端回调地址（需在提供方后台登记）
187:	FrontendRedirectURL string `mapstructure:"frontend_redirect_url"` // 前端接收 token 的路由（默认：/auth/linuxdo/callback）
188:	TokenAuthMethod     string `mapstructure:"token_auth_method"`     // client_secret_post / client_secret_basic / none
189:	UsePKCE             bool   `mapstructure:"use_pkce"`
193:	UserInfoEmailPath    string `mapstructure:"userinfo_email_path"`
194:	UserInfoIDPath       string `mapstructure:"userinfo_id_path"`
195:	UserInfoUsernamePath string `mapstructure:"userinfo_username_path"`
199:	Enabled             bool   `mapstructure:"enabled"`
200:	AppID               string `mapstructure:"app_id"`
201:	AppSecret           string `mapstructure:"app_secret"`
202:	OpenAppID           string `mapstructure:"open_app_id"`
203:	OpenAppSecret       string `mapstructure:"open_app_secret"`
204:	MPAppID             string `mapstructure:"mp_app_id"`
205:	MPAppSecret         string `mapstructure:"mp_app_secret"`
206:	MobileAppID         string `mapstructure:"mobile_app_id"`
207:	MobileAppSecret     string `mapstructure:"mobile_app_secret"`
208:	OpenEnabled         bool   `mapstructure:"open_enabled"`
209:	MPEnabled           bool   `mapstructure:"mp_enabled"`
210:	MobileEnabled       bool   `mapstructure:"mobile_enabled"`
211:	Mode                string `mapstructure:"mode"`
212:	Scopes              string `mapstructure:"scopes"`
213:	RedirectURL         string `mapstructure:"redirect_url"`
214:	FrontendRedirectURL string `mapstructure:"frontend_redirect_url"`
218:	Enabled                 bool   `mapstructure:"enabled"`
219:	ProviderName            string `mapstructure:"provider_name"` // 显示名: "Keycloak" 等
220:	ClientID                string `mapstructure:"client_id"`
221:	ClientSecret            string `mapstructure:"client_secret"`
222:	IssuerURL               string `mapstructure:"issuer_url"`
223:	DiscoveryURL            string `mapstructure:"discovery_url"`
224:	AuthorizeURL            string `mapstructure:"authorize_url"`
225:	TokenURL                string `mapstructure:"token_url"`
226:	UserInfoURL             string `mapstructure:"userinfo_url"`
227:	JWKSURL                 string `mapstructure:"jwks_url"`
228:	Scopes                  string `mapstructure:"scopes"`                // 默认 "openid email profile"
229:	RedirectURL             string `mapstructure:"redirect_url"`          // 后端回调地址（需在提供方后台登记）
230:	FrontendRedirectURL     string `mapstructure:"frontend_redirect_url"` // 前端接收 token 的路由（默认：/auth/oidc/callback）
231:	TokenAuthMethod         string `mapstructure:"token_auth_method"`     // client_secret_post / client_secret_basic / none
232:	UsePKCE                 bool   `mapstructure:"use_pkce"`
233:	ValidateIDToken         bool   `mapstructure:"validate_id_token"`
234:	UsePKCEExplicit         bool   `mapstructure:"-" yaml:"-"`
235:	ValidateIDTokenExplicit bool   `mapstructure:"-" yaml:"-"`
236:	AllowedSigningAlgs      string `mapstructure:"allowed_signing_algs"`   // 默认 "RS256,ES256,PS256"
237:	ClockSkewSeconds        int    `mapstructure:"clock_skew_seconds"`     // 默认 120
238:	RequireEmailVerified    bool   `mapstructure:"require_email_verified"` // 默认 false
242:	UserInfoEmailPath    string `mapstructure:"userinfo_email_path"`
243:	UserInfoIDPath       string `mapstructure:"userinfo_id_path"`
244:	UserInfoUsernamePath string `mapstructure:"userinfo_username_path"`
248:	Enabled             bool   `mapstructure:"enabled"`
249:	ClientID            string `mapstructure:"client_id"`
250:	ClientSecret        string `mapstructure:"client_secret"`
251:	AuthorizeURL        string `mapstructure:"authorize_url"`
252:	TokenURL            string `mapstructure:"token_url"`
253:	UserInfoURL         string `mapstructure:"userinfo_url"`
254:	Scopes              string `mapstructure:"scopes"`
255:	RedirectURL         string `mapstructure:"redirect_url"`
256:	FrontendRedirectURL string `mapstructure:"frontend_redirect_url"`
259:	DingTalkAppKind string `mapstructure:"dingtalk_app_kind"` // 仅 "internal_app"（V4 fail-closed）
260:	AppType         string `mapstructure:"app_type"`          // "public" (default) | "internal"
263:	CorpRestrictionPolicy   string `mapstructure:"corp_restriction_policy"`
264:	InternalCorpID          string `mapstructure:"internal_corp_id"`
265:	BypassRegistration      bool   `mapstructure:"bypass_registration"`
266:	SyncCorpEmail           bool   `mapstructure:"sync_corp_email"`
267:	SyncDisplayName         bool   `mapstructure:"sync_display_name"`
268:	SyncDept                bool   `mapstructure:"sync_dept"`
269:	SyncCorpEmailAttrKey    string `mapstructure:"sync_corp_email_attr_key"`
270:	SyncDisplayNameAttrKey  string `mapstructure:"sync_display_name_attr_key"`
271:	SyncDeptAttrKey         string `mapstructure:"sync_dept_attr_key"`
272:	SyncCorpEmailAttrName   string `mapstructure:"sync_corp_email_attr_name"`
273:	SyncDisplayNameAttrName string `mapstructure:"sync_display_name_attr_name"`
274:	SyncDeptAttrName        string `mapstructure:"sync_dept_attr_name"`
277:	RequireEmail            bool   `mapstructure:"require_email"`
278:	UsernameOverwritePolicy string `mapstructure:"username_overwrite_policy"`
281:	UsernameAttributeKey         string   `mapstructure:"username_attribute_key"`
282:	EnableAttributeMatching      bool     `mapstructure:"enable_attribute_matching"`
283:	EnableAttributeSync          bool     `mapstructure:"enable_attribute_sync"`
284:	AttributeSyncFields          []string `mapstructure:"attribute_sync_fields"`
285:	AttributeSyncOverwritePolicy string   `mapstructure:"attribute_sync_overwrite_policy"`
289:	Enabled             bool   `mapstructure:"enabled"`
290:	ClientID            string `mapstructure:"client_id"`
291:	ClientSecret        string `mapstructure:"client_secret"`
292:	AuthorizeURL        string `mapstructure:"authorize_url"`
293:	TokenURL            string `mapstructure:"token_url"`
294:	UserInfoURL         string `mapstructure:"userinfo_url"`
295:	EmailsURL           string `mapstructure:"emails_url"`
296:	Scopes              string `mapstructure:"scopes"`
297:	RedirectURL         string `mapstructure:"redirect_url"`
298:	FrontendRedirectURL string `mapstructure:"frontend_redirect_url"`
523:	Enabled bool `mapstructure:"enabled"`
525:	CheckIntervalMinutes int `mapstructure:"check_interval_minutes"`
527:	RefreshBeforeExpiryHours float64 `mapstructure:"refresh_before_expiry_hours"`
529:	MaxRetries int `mapstructure:"max_retries"`
531:	RetryBackoffSeconds int `mapstructure:"retry_backoff_seconds"`
536:	RemoteURL string `mapstructure:"remote_url"`
538:	HashURL string `mapstructure:"hash_url"`
540:	DataDir string `mapstructure:"data_dir"`
542:	FallbackFile string `mapstructure:"fallback_file"`
544:	UpdateIntervalHours int `mapstructure:"update_interval_hours"`
546:	HashCheckIntervalMinutes int `mapstructure:"hash_check_interval_minutes"`
550:	Host               string    `mapstructure:"host"`
551:	Port               int       `mapstructure:"port"`
552:	Mode               string    `mapstructure:"mode"`                  // debug/release
553:	FrontendURL        string    `mapstructure:"frontend_url"`          // 前端基础 URL，用于生成邮件中的外部链接
554:	ReadHeaderTimeout  int       `mapstructure:"read_header_timeout"`   // 读取请求头超时（秒）
555:	IdleTimeout        int       `mapstructure:"idle_timeout"`          // 空闲连接超时（秒）
556:	TrustedProxies     []string  `mapstructure:"trusted_proxies"`       // 可信代理列表（CIDR/IP）
557:	MaxRequestBodySize int64     `mapstructure:"max_request_body_size"` // 全局最大请求体限制
558:	H2C                H2CConfig `mapstructure:"h2c"`                   // HTTP/2 Cleartext 配置
563:	Enabled                      bool   `mapstructure:"enabled"`                          // 是否启用 H2C
564:	MaxConcurrentStreams         uint32 `mapstructure:"max_concurrent_streams"`           // 最大并发流数量
565:	IdleTimeout                  int    `mapstructure:"idle_timeout"`                     // 空闲超时（秒）
566:	MaxReadFrameSize             int    `mapstructure:"max_read_frame_size"`              // 最大帧大小（字节）
567:	MaxUploadBufferPerConnection int    `mapstructure:"max_upload_buffer_per_connection"` // 每个连接的上传缓冲区（字节）
568:	MaxUploadBufferPerStream     int    `mapstructure:"max_upload_buffer_per_stream"`     // 每个流的上传缓冲区（字节）
572:	AllowedOrigins   []string `mapstructure:"allowed_origins"`
573:	AllowCredentials bool     `mapstructure:"allow_credentials"`
577:	URLAllowlist                     URLAllowlistConfig   `mapstructure:"url_allowlist"`
578:	ResponseHeaders                  ResponseHeaderConfig `mapstructure:"response_headers"`
579:	CSP                              CSPConfig            `mapstructure:"csp"`
580:	ProxyFallback                    ProxyFallbackConfig  `mapstructure:"proxy_fallback"`
581:	ProxyProbe                       ProxyProbeConfig     `mapstructure:"proxy_probe"`
582:	TrustForwardedIPForAPIKeyACL     bool                 `mapstructure:"trust_forwarded_ip_for_api_key_acl"`
583:	trustForwardedIPForAPIKeyACLLive *atomic.Bool         `mapstructure:"-"`
609:	Enabled           bool     `mapstructure:"enabled"`
610:	UpstreamHosts     []string `mapstructure:"upstream_hosts"`
611:	PricingHosts      []string `mapstructure:"pricing_hosts"`
612:	CRSHosts          []string `mapstructure:"crs_hosts"`
613:	AllowPrivateHosts bool     `mapstructure:"allow_private_hosts"`
615:	AllowInsecureHTTP bool `mapstructure:"allow_insecure_http"`
619:	Enabled           bool     `mapstructure:"enabled"`
620:	AdditionalAllowed []string `mapstructure:"additional_allowed"`
621:	ForceRemove       []string `mapstructure:"force_remove"`
625:	Enabled bool   `mapstructure:"enabled"`
626:	Policy  string `mapstructure:"policy"`
637:	AllowDirectOnError bool `mapstructure:"allow_direct_on_error"`
641:	InsecureSkipVerify bool `mapstructure:"insecure_skip_verify"` // 已禁用：禁止跳过 TLS 证书验证
645:	CircuitBreaker CircuitBreakerConfig `mapstructure:"circuit_breaker"`
651:	UserPlatformQuotaCacheTTLSeconds int `mapstructure:"user_platform_quota_cache_ttl_seconds"`
654:	UserPlatformQuotaSentinelTTLSeconds int `mapstructure:"user_platform_quota_sentinel_ttl_seconds"`
658:	Enabled             bool `mapstructure:"enabled"`
659:	FailureThreshold    int  `mapstructure:"failure_threshold"`
660:	ResetTimeoutSeconds int  `mapstructure:"reset_timeout_seconds"`
661:	HalfOpenRequests    int  `mapstructure:"half_open_requests"`
666:	PingInterval int `mapstructure:"ping_interval"`
671:	Enabled bool `mapstructure:"enabled"`
673:	MaxConcurrentRequests int `mapstructure:"max_concurrent_requests"`
675:	OverflowMode string `mapstructure:"overflow_mode"`
677:	WaitTimeoutSeconds int `mapstructure:"wait_timeout_seconds"`
679:	MaxWaitingRequests int `mapstructure:"max_waiting_requests"`
691:	ResponseHeaderTimeout int `mapstructure:"response_header_timeout"`
694:	OpenAIResponseHeaderTimeout int `mapstructure:"openai_response_header_timeout"`
696:	MaxBodySize int64 `mapstructure:"max_body_size"`
698:	UpstreamResponseReadMaxBytes int64 `mapstructure:"upstream_response_read_max_bytes"`
700:	ProxyProbeResponseReadMaxBytes int64 `mapstructure:"proxy_probe_response_read_max_bytes"`
702:	GeminiDebugResponseHeaders bool `mapstructure:"gemini_debug_response_headers"`
704:	ConnectionPoolIsolation string `mapstructure:"connection_pool_isolation"`
707:	ForceCodexCLI bool `mapstructure:"force_codex_cli"`
710:	CodexImageGenerationBridgeEnabled bool `mapstructure:"codex_image_generation_bridge_enabled"`
713:	ForcedCodexInstructionsTemplateFile string `mapstructure:"forced_codex_instructions_template_file"`
716:	ForcedCodexInstructionsTemplate string `mapstructure:"-"`
719:	OpenAIPassthroughAllowTimeoutHeaders bool `mapstructure:"openai_passthrough_allow_timeout_headers"`
721:	OpenAIWS GatewayOpenAIWSConfig `mapstructure:"openai_ws"`
723:	OpenAIScheduler GatewayOpenAISchedulerConfig `mapstructure:"openai_scheduler"`
725:	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
727:	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
729:	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
733:	MaxIdleConns int `mapstructure:"max_idle_conns"`
735:	MaxIdleConnsPerHost int `mapstructure:"max_idle_conns_per_host"`
737:	MaxConnsPerHost int `mapstructure:"max_conns_per_host"`
739:	IdleConnTimeoutSeconds int `mapstructure:"idle_conn_timeout_seconds"`
744:	MaxUpstreamClients int `mapstructure:"max_upstream_clients"`
748:	ClientIdleTTLSeconds int `mapstructure:"client_idle_ttl_seconds"`
751:	ConcurrencySlotTTLMinutes int `mapstructure:"concurrency_slot_ttl_minutes"`
755:	SessionIdleTimeoutMinutes int `mapstructure:"session_idle_timeout_minutes"`
758:	StreamDataIntervalTimeout int `mapstructure:"stream_data_interval_timeout"`
760:	StreamKeepaliveInterval int `mapstructure:"stream_keepalive_interval"`
762:	ImageStreamDataIntervalTimeout int `mapstructure:"image_stream_data_interval_timeout"`
764:	ImageStreamKeepaliveInterval int `mapstructure:"image_stream_keepalive_interval"`
766:	MaxLineSize int `mapstructure:"max_line_size"`
769:	LogUpstreamErrorBody bool `mapstructure:"log_upstream_error_body"`
771:	LogUpstreamErrorBodyMaxBytes int `mapstructure:"log_upstream_error_body_max_bytes"`
774:	InjectBetaForAPIKey bool `mapstructure:"inject_beta_for_apikey"`
777:	FailoverOn400 bool `mapstructure:"failover_on_400"`
780:	MaxAccountSwitches int `mapstructure:"max_account_switches"`
782:	MaxAccountSwitchesGemini int `mapstructure:"max_account_switches_gemini"`
785:	AntigravityFallbackCooldownMinutes int `mapstructure:"antigravity_fallback_cooldown_minutes"`
788:	Scheduling GatewaySchedulingConfig `mapstructure:"scheduling"`
791:	TLSFingerprint TLSFingerprintConfig `mapstructure:"tls_fingerprint"`
794:	UsageRecord GatewayUsageRecordConfig `mapstructure:"usage_record"`
797:	UserGroupRateCacheTTLSeconds int `mapstructure:"user_group_rate_cache_ttl_seconds"`
799:	ModelsListCacheTTLSeconds int `mapstructure:"models_list_cache_ttl_seconds"`
803:	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
807:	Enabled  bool          `mapstructure:"enabled"`
808:	Interval time.Duration `mapstructure:"interval"`
815:	Enabled bool `mapstructure:"enabled"`
817:	AllowProxyFallbackToHTTP1 bool `mapstructure:"allow_proxy_fallback_to_http1"`
819:	FallbackErrorThreshold int `mapstructure:"fallback_error_threshold"`
821:	FallbackWindowSeconds int `mapstructure:"fallback_window_seconds"`
823:	FallbackTTLSeconds int `mapstructure:"fallback_ttl_seconds"`
833:	Mode string `mapstructure:"mode"`
835:	Enabled bool `mapstructure:"enabled"`
837:	LockTTLMs int `mapstructure:"lock_ttl_ms"`
839:	WaitTimeoutMs int `mapstructure:"wait_timeout_ms"`
841:	MinDelayMs int `mapstructure:"min_delay_ms"`
843:	MaxDelayMs int `mapstructure:"max_delay_ms"`
845:	CleanupIntervalSeconds int `mapstructure:"cleanup_interval_seconds"`
872:	ModeRouterV2Enabled bool `mapstructure:"mode_router_v2_enabled"`
874:	IngressModeDefault string `mapstructure:"ingress_mode_default"`
876:	Enabled bool `mapstructure:"enabled"`
878:	OAuthEnabled bool `mapstructure:"oauth_enabled"`
880:	APIKeyEnabled bool `mapstructure:"apikey_enabled"`
882:	ForceHTTP bool `mapstructure:"force_http"`
884:	AllowStoreRecovery bool `mapstructure:"allow_store_recovery"`
886:	IngressPreviousResponseRecoveryEnabled bool `mapstructure:"ingress_previous_response_recovery_enabled"`
891:	StoreDisabledConnMode string `mapstructure:"store_disabled_conn_mode"`
894:	StoreDisabledForceNewConn bool `mapstructure:"store_disabled_force_new_conn"`
896:	PrewarmGenerateEnabled bool `mapstructure:"prewarm_generate_enabled"`
898:	ClientReadLimitBytes int64 `mapstructure:"client_read_limit_bytes"`
900:	HTTPBridgeEnabled bool `mapstructure:"http_bridge_enabled"`
902:	HTTPBridgeThresholdBytes int64 `mapstructure:"http_bridge_threshold_bytes"`
905:	ResponsesWebsockets   bool `mapstructure:"responses_websockets"`
906:	ResponsesWebsocketsV2 bool `mapstructure:"responses_websockets_v2"`
909:	MaxConnsPerAccount int `mapstructure:"max_conns_per_account"`
910:	MinIdlePerAccount  int `mapstructure:"min_idle_per_account"`
911:	MaxIdlePerAccount  int `mapstructure:"max_idle_per_account"`
913:	DynamicMaxConnsByAccountConcurrencyEnabled bool `mapstructure:"dynamic_max_conns_by_account_concurrency_enabled"`
915:	OAuthMaxConnsFactor float64 `mapstructure:"oauth_max_conns_factor"`
917:	APIKeyMaxConnsFactor  float64 `mapstructure:"apikey_max_conns_factor"`
918:	DialTimeoutSeconds    int     `mapstructure:"dial_timeout_seconds"`
919:	ReadTimeoutSeconds    int     `mapstructure:"read_timeout_seconds"`
920:	WriteTimeoutSeconds   int     `mapstructure:"write_timeout_seconds"`
921:	PoolTargetUtilization float64 `mapstructure:"pool_target_utilization"`
922:	QueueLimitPerConn     int     `mapstructure:"queue_limit_per_conn"`
924:	EventFlushBatchSize int `mapstructure:"event_flush_batch_size"`
926:	EventFlushIntervalMS int `mapstructure:"event_flush_interval_ms"`
928:	PrewarmCooldownMS int `mapstructure:"prewarm_cooldown_ms"`
930:	FallbackCooldownSeconds int `mapstructure:"fallback_cooldown_seconds"`
932:	RetryBackoffInitialMS int `mapstructure:"retry_backoff_initial_ms"`
934:	RetryBackoffMaxMS int `mapstructure:"retry_backoff_max_ms"`
936:	RetryJitterRatio float64 `mapstructure:"retry_jitter_ratio"`
938:	RetryTotalBudgetMS int `mapstructure:"retry_total_budget_ms"`
940:	PayloadLogSampleRate float64 `mapstructure:"payload_log_sample_rate"`
943:	LBTopK int `mapstructure:"lb_top_k"`
945:	StickySessionTTLSeconds int `mapstructure:"sticky_session_ttl_seconds"`
947:	SessionHashReadOldFallback bool `mapstructure:"session_hash_read_old_fallback"`
949:	SessionHashDualWriteOld bool `mapstructure:"session_hash_dual_write_old"`
951:	MetadataBridgeEnabled bool `mapstructure:"metadata_bridge_enabled"`
953:	StickyResponseIDTTLSeconds int `mapstructure:"sticky_response_id_ttl_seconds"`
955:	StickyPreviousResponseTTLSeconds int `mapstructure:"sticky_previous_response_ttl_seconds"`
957:	SchedulerScoreWeights GatewayOpenAIWSSchedulerScoreWeights `mapstructure:"scheduler_score_weights"`
962:	Priority  float64 `mapstructure:"priority"`
963:	Load      float64 `mapstructure:"load"`
964:	Queue     float64 `mapstructure:"queue"`
965:	ErrorRate float64 `mapstructure:"error_rate"`
966:	TTFT      float64 `mapstructure:"ttft"`
972:	StickyEscapeEnabled bool `mapstructure:"sticky_escape_enabled"`
974:	StickyEscapeTTFTMs int `mapstructure:"sticky_escape_ttft_ms"`
976:	StickyEscapeErrorRate float64 `mapstructure:"sticky_escape_error_rate"`
982:	WorkerCount int `mapstructure:"worker_count"`
984:	QueueSize int `mapstructure:"queue_size"`
986:	TaskTimeoutSeconds int `mapstructure:"task_timeout_seconds"`
988:	OverflowPolicy string `mapstructure:"overflow_policy"`
990:	OverflowSamplePercent int `mapstructure:"overflow_sample_percent"`
993:	AutoScaleEnabled bool `mapstructure:"auto_scale_enabled"`
995:	AutoScaleMinWorkers int `mapstructure:"auto_scale_min_workers"`
997:	AutoScaleMaxWorkers int `mapstructure:"auto_scale_max_workers"`
999:	AutoScaleUpQueuePercent int `mapstructure:"auto_scale_up_queue_percent"`
1001:	AutoScaleDownQueuePercent int `mapstructure:"auto_scale_down_queue_percent"`
1003:	AutoScaleUpStep int `mapstructure:"auto_scale_up_step"`
1005:	AutoScaleDownStep int `mapstructure:"auto_scale_down_step"`
1007:	AutoScaleCheckIntervalSeconds int `mapstructure:"auto_scale_check_interval_seconds"`
1009:	AutoScaleCooldownSeconds int `mapstructure:"auto_scale_cooldown_seconds"`
1016:	Enabled bool `mapstructure:"enabled"`
1019:	Profiles map[string]TLSProfileConfig `mapstructure:"profiles"`
1027:	Name string `mapstructure:"name"`
1029:	EnableGREASE bool `mapstructure:"enable_grease"`
1031:	CipherSuites []uint16 `mapstructure:"cipher_suites"`
1033:	Curves []uint16 `mapstructure:"curves"`
1035:	PointFormats []uint16 `mapstructure:"point_formats"`
1037:	SignatureAlgorithms []uint16 `mapstructure:"signature_algorithms"`
1039:	ALPNProtocols []string `mapstructure:"alpn_protocols"`
1041:	SupportedVersions []uint16 `mapstructure:"supported_versions"`
1043:	KeyShareGroups []uint16 `mapstructure:"key_share_groups"`
1045:	PSKModes []uint16 `mapstructure:"psk_modes"`
1049:	Extensions []uint16 `mapstructure:"extensions"`
1055:	StickySessionMaxWaiting  int           `mapstructure:"sticky_session_max_waiting"`
1056:	StickySessionWaitTimeout time.Duration `mapstructure:"sticky_session_wait_timeout"`
1059:	FallbackWaitTimeout time.Duration `mapstructure:"fallback_wait_timeout"`
1060:	FallbackMaxWaiting  int           `mapstructure:"fallback_max_waiting"`
1063:	FallbackSelectionMode string `mapstructure:"fallback_selection_mode"`
1066:	LoadBatchEnabled    bool `mapstructure:"load_batch_enabled"`
1067:	LoadBatchCacheTTLMS int  `mapstructure:"load_batch_cache_ttl_ms"`
1069:	SnapshotMGetChunkSize int `mapstructure:"snapshot_mget_chunk_size"`
1071:	SnapshotWriteChunkSize int `mapstructure:"snapshot_write_chunk_size"`
1074:	SlotCleanupInterval time.Duration `mapstructure:"slot_cleanup_interval"`
1077:	DbFallbackEnabled bool `mapstructure:"db_fallback_enabled"`
1079:	DbFallbackTimeoutSeconds int `mapstructure:"db_fallback_timeout_seconds"`
1081:	DbFallbackMaxQPS int `mapstructure:"db_fallback_max_qps"`
1085:	OutboxPollIntervalSeconds int `mapstructure:"outbox_poll_interval_seconds"`
1087:	OutboxLagWarnSeconds int `mapstructure:"outbox_lag_warn_seconds"`
1089:	OutboxLagRebuildSeconds int `mapstructure:"outbox_lag_rebuild_seconds"`
1091:	OutboxLagRebuildFailures int `mapstructure:"outbox_lag_rebuild_failures"`
1093:	OutboxBacklogRebuildRows int `mapstructure:"outbox_backlog_rebuild_rows"`
1097:	FullRebuildIntervalSeconds int `mapstructure:"full_rebuild_interval_seconds"`
1107:	Host     string `mapstructure:"host"`
1108:	Port     int    `mapstructure:"port"`
1109:	User     string `mapstructure:"user"`
1110:	Password string `mapstructure:"password"`
1111:	DBName   string `mapstructure:"dbname"`
1112:	SSLMode  string `mapstructure:"sslmode"`
1115:	MaxOpenConns int `mapstructure:"max_open_conns"`
1117:	MaxIdleConns int `mapstructure:"max_idle_conns"`
1119:	ConnMaxLifetimeMinutes int `mapstructure:"conn_max_lifetime_minutes"`
1121:	ConnMaxIdleTimeMinutes int `mapstructure:"conn_max_idle_time_minutes"`
1123:	UserPlatformQuotaFlusherEnabled bool `mapstructure:"user_platform_quota_flusher_enabled"`
1125:	UserPlatformQuotaFlushIntervalMs int `mapstructure:"user_platform_quota_flush_interval_ms"`
1128:	UserPlatformQuotaFlushBatchSize int `mapstructure:"user_platform_quota_flush_batch_size"`
1166:	Host     string `mapstructure:"host"`
1167:	Port     int    `mapstructure:"port"`
1168:	Password string `mapstructure:"password"`
1169:	DB       int    `mapstructure:"db"`
1172:	DialTimeoutSeconds int `mapstructure:"dial_timeout_seconds"`
1174:	ReadTimeoutSeconds int `mapstructure:"read_timeout_seconds"`
1176:	WriteTimeoutSeconds int `mapstructure:"write_timeout_seconds"`
1178:	PoolSize int `mapstructure:"pool_size"`
1180:	MinIdleConns int `mapstructure:"min_idle_conns"`
1182:	EnableTLS bool `mapstructure:"enable_tls"`
1194:	Enabled bool `mapstructure:"enabled"`
1197:	UsePreaggregatedTables bool `mapstructure:"use_preaggregated_tables"`
1200:	Cleanup OpsCleanupConfig `mapstructure:"cleanup"`
1203:	MetricsCollectorCache OpsMetricsCollectorCacheConfig `mapstructure:"metrics_collector_cache"`
1206:	Aggregation OpsAggregationConfig `mapstructure:"aggregation"`
1210:	Enabled  bool   `mapstructure:"enabled"`
1211:	Schedule string `mapstructure:"schedule"`
1216:	ErrorLogRetentionDays      int `mapstructure:"error_log_retention_days"`
1217:	MinuteMetricsRetentionDays int `mapstructure:"minute_metrics_retention_days"`
1218:	HourlyMetricsRetentionDays int `mapstructure:"hourly_metrics_retention_days"`
1222:	Enabled bool `mapstructure:"enabled"`
1226:	Enabled bool          `mapstructure:"enabled"`
1227:	TTL     time.Duration `mapstructure:"ttl"`
1231:	Secret     string `mapstructure:"secret"`
1232:	ExpireHour int    `mapstructure:"expire_hour"`
1236:	AccessTokenExpireMinutes int `mapstructure:"access_token_expire_minutes"`
1238:	RefreshTokenExpireDays int `mapstructure:"refresh_token_expire_days"`
1240:	RefreshWindowMinutes int `mapstructure:"refresh_window_minutes"`
1247:	EncryptionKey string `mapstructure:"encryption_key"`
1250:	EncryptionKeyConfigured bool `mapstructure:"-"`
1254:	Required bool `mapstructure:"required"`
1258:	AdminEmail      string  `mapstructure:"admin_email"`
1259:	AdminPassword   string  `mapstructure:"admin_password"`
1260:	UserConcurrency int     `mapstructure:"user_concurrency"`
1261:	UserBalance     float64 `mapstructure:"user_balance"`
1262:	APIKeyPrefix    string  `mapstructure:"api_key_prefix"`
1263:	RateMultiplier  float64 `mapstructure:"rate_multiplier"`
1267:	OverloadCooldownMinutes int `mapstructure:"overload_cooldown_minutes"`  // 529过载冷却时间(分钟)
1268:	OAuth401CooldownMinutes int `mapstructure:"oauth_401_cooldown_minutes"` // OAuth 401临时不可调度冷却(分钟)
1273:	L1Size             int  `mapstructure:"l1_size"`
1274:	L1TTLSeconds       int  `mapstructure:"l1_ttl_seconds"`
1275:	L2TTLSeconds       int  `mapstructure:"l2_ttl_seconds"`
1276:	NegativeTTLSeconds int  `mapstructure:"negative_ttl_seconds"`
1277:	JitterPercent      int  `mapstructure:"jitter_percent"`
1278:	Singleflight       bool `mapstructure:"singleflight"`
1283:	L1Size        int `mapstructure:"l1_size"`
1284:	L1TTLSeconds  int `mapstructure:"l1_ttl_seconds"`
1285:	JitterPercent int `mapstructure:"jitter_percent"`
1291:	WorkerCount int `mapstructure:"worker_count"`
1292:	QueueSize   int `mapstructure:"queue_size"`
1298:	Enabled bool `mapstructure:"enabled"`
1300:	KeyPrefix string `mapstructure:"key_prefix"`
1302:	StatsFreshTTLSeconds int `mapstructure:"stats_fresh_ttl_seconds"`
1304:	StatsTTLSeconds int `mapstructure:"stats_ttl_seconds"`
1306:	StatsRefreshTimeoutSeconds int `mapstructure:"stats_refresh_timeout_seconds"`
1312:	Enabled bool `mapstructure:"enabled"`
1314:	IntervalSeconds int `mapstructure:"interval_seconds"`
1316:	LookbackSeconds int `mapstructure:"lookback_seconds"`
1318:	BackfillEnabled bool `mapstructure:"backfill_enabled"`
1320:	BackfillMaxDays int `mapstructure:"backfill_max_days"`
1322:	Retention DashboardAggregationRetentionConfig `mapstructure:"retention"`
1324:	RecomputeDays int `mapstructure:"recompute_days"`
1329:	UsageLogsDays         int `mapstructure:"usage_logs_days"`
1330:	UsageBillingDedupDays int `mapstructure:"usage_billing_dedup_days"`
1331:	HourlyDays            int `mapstructure:"hourly_days"`
1332:	DailyDays             int `mapstructure:"daily_days"`
1338:	Enabled bool `mapstructure:"enabled"`
1340:	MaxRangeDays int `mapstructure:"max_range_days"`
1342:	BatchSize int `mapstructure:"batch_size"`
1344:	WorkerIntervalSeconds int `mapstructure:"worker_interval_seconds"`
1346:	TaskTimeoutSeconds int `mapstructure:"task_timeout_seconds"`
1404:	if err := viper.Unmarshal(&cfg); err != nil {

exec
/bin/bash -lc "sed -n '1,140p' backend/internal/pkg/clientidentity/registry.go && sed -n '1,150p' backend/internal/service/version_fetcher_service.go" in /mnt/x/project/sub2api
 succeeded in 0ms:
package clientidentity

import (
	"sync/atomic"

	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
)

const (
	defaultCodexCLIVersion = "0.125.0"
	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
)

// Registry holds the active complete Claude and Codex identity snapshots.
type Registry struct {
	snapshotPtr atomic.Pointer[Snapshots]
}

// Snapshots is the complete baseline for both client families.
type Snapshots struct {
	Claude ClaudeSnapshot
	Codex  CodexSnapshot
}

// ClaudeSnapshot keeps all linked Claude identity fields together.
type ClaudeSnapshot struct {
	Headers        map[string]string
	VersionFields  VersionFields
	TLSProfileName string
}

// CodexSnapshot keeps all linked Codex identity fields together.
type CodexSnapshot struct {
	Headers        map[string]string
	VersionFields  VersionFields
	TLSProfileName string
}

func NewRegistry() *Registry {
	r := &Registry{}
	r.snapshotPtr.Store(&Snapshots{
		Claude: r.defaultClaudeSnapshot(),
		Codex:  r.defaultCodexSnapshot(),
	})
	return r
}

// Get returns the active immutable snapshot pointer atomically.
func (r *Registry) Get() *Snapshots {
	return r.snapshotPtr.Load()
}

// Swap atomically replaces both Claude and Codex snapshots as one unit.
func (r *Registry) Swap(snapshots *Snapshots) {
	if snapshots == nil {
		return
	}
	r.snapshotPtr.Store(snapshots)
}

func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
	headers := cloneHeaders(claude.DefaultHeaders)
	return ClaudeSnapshot{
		Headers: headers,
		VersionFields: VersionFields{
			CLIVersion: claude.CLICurrentVersion,
			SDKVersion: headers["X-Stainless-Package-Version"],
			CCVersion:  claude.CLICurrentVersion,
		},
		TLSProfileName: TLSProfileClaudeCLIDefault,
	}
}

func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
	return CodexSnapshot{
		Headers: map[string]string{
			"User-Agent": defaultCodexCLIUA,
			"originator": "codex_cli_rs",
		},
		VersionFields: VersionFields{
			CLIVersion: defaultCodexCLIVersion,
		},
		TLSProfileName: TLSProfileCodexCLIDefault,
	}
}
package service

import (
	"context"
	"sync"
	"time"

	"github.com/Wei-Shaw/sub2api/internal/config"
	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
)

const (
	defaultVersionFetchInterval = time.Hour
	versionFetchTimeout         = 30 * time.Second
)

type VersionFetcherService struct {
	registry *clientidentity.Registry
	cfg      *config.Config
	stopCh   chan struct{}
	stopOnce sync.Once
}

func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
	return &VersionFetcherService{
		registry: registry,
		cfg:      cfg,
		stopCh:   make(chan struct{}),
	}
}

// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
func (s *VersionFetcherService) Start() {
	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
		return
	}

	interval := s.cfg.Gateway.UAAutoFetch.Interval
	if interval == 0 {
		interval = defaultVersionFetchInterval
	}

	go func() {
		ticker := time.NewTicker(interval)
		defer ticker.Stop()

		for {
			select {
			case <-ticker.C:
				s.fetchAndUpdate()
			case <-s.stopCh:
				return
			}
		}
	}()
}

func (s *VersionFetcherService) Stop() {
	if s == nil {
		return
	}
	s.stopOnce.Do(func() {
		close(s.stopCh)
	})
}

func (s *VersionFetcherService) fetchAndUpdate() {
	if s == nil || s.registry == nil {
		return
	}

	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
	defer cancel()

	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
		return
	}

	s.registry.Swap(&clientidentity.Snapshots{
		Claude: clientidentity.ClaudeSnapshot{
			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
			VersionFields: clientidentity.VersionFields{
				CLIVersion: claudeVersion,
				SDKVersion: claudeSDKVersion,
				CCVersion:  claudeVersion,
			},
			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
		},
		Codex: clientidentity.CodexSnapshot{
			Headers: s.buildCodexHeaders(codexVersion),
			VersionFields: clientidentity.VersionFields{
				CLIVersion: codexVersion,
			},
			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
		},
	})
}

func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
	_ = ctx
	return "", "", ""
}

func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
	return map[string]string{
		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
		"X-Stainless-Package-Version": sdkVer,
		"X-App":                       "cli",
	}
}

func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
	return map[string]string{
		"User-Agent": "codex_cli_rs/" + cliVer,
		"originator": "codex_cli_rs",
	}
}

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..22eadfb07bcd1d52a55215ebe6a58a930571ce3a
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,10 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

**Evaluating project requirements**

I noticed that our recent reading and search returned irrelevant hits, which isn't ideal. It seems we can omit citations since we didn't use any relevant memory files for this task. The requirement mentions using relevant memory files, but they simply aren't applicable here. I’m also considering if we should use a trellis-finish, although the user didn't specifically ask for that. The project has a blackhole ledger ready, but there's no immediate need for it. I just need to remember to apply the patch for defer Stop.
codex
我会再补一个关闭钩子：当开关开启时，服务退出会先停 fetcher，再走其它 cleanup。默认关闭时这个调用是空操作。
apply patch
patch: completed
/mnt/x/project/sub2api/backend/cmd/server/main.go
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..17c926cdedcabffa0ddc320973074598458620df
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,11 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+		defer app.VersionFetcherService.Stop()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..17c926cdedcabffa0ddc320973074598458620df
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,11 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+		defer app.VersionFetcherService.Stop()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

codex
我还注意到配置的 `time.Duration` 是否能从 YAML 的 `1h` 解析，现有 viper 没显式 decode hook。为避免示例配置加载失败风险，我会用项目已有 default 保障，同时跑配置测试验证；若失败就改成秒数字段或加 decode hook。
exec
/bin/bash -lc 'gofmt -w cmd/server/main.go' in /mnt/x/project/sub2api/backend
 succeeded in 0ms:
exec
/bin/bash -lc 'go test ./internal/config -run Test -count=1' in /mnt/x/project/sub2api/backend
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..17c926cdedcabffa0ddc320973074598458620df
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,11 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+		defer app.VersionFetcherService.Stop()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

 succeeded in 1216ms:
ok  	github.com/Wei-Shaw/sub2api/internal/config	0.648s

codex
配置测试通过。现在重新执行用户指定四条验证，确认最后状态。
exec
/bin/bash -lc 'go build ./internal/pkg/clientidentity' in /mnt/x/project/sub2api/backend
exec
/bin/bash -lc 'go build ./internal/service' in /mnt/x/project/sub2api/backend
exec
/bin/bash -lc 'go test ./internal/pkg/clientidentity -run TestRegistryAtomicSwap -v' in /mnt/x/project/sub2api/backend
exec
/bin/bash -lc 'go test ./internal/service -run TestVersionFetcherDisabledDoesNotStart -v' in /mnt/x/project/sub2api/backend
 succeeded in 240ms:
 succeeded in 1467ms:
=== RUN   TestRegistryAtomicSwap
--- PASS: TestRegistryAtomicSwap (0.00s)
PASS
ok  	github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity	(cached)

 succeeded in 2548ms:
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..17c926cdedcabffa0ddc320973074598458620df
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,11 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+		defer app.VersionFetcherService.Stop()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

 succeeded in 3222ms:
=== RUN   TestVersionFetcherDisabledDoesNotStart
--- PASS: TestVersionFetcherDisabledDoesNotStart (0.05s)
PASS
ok  	github.com/Wei-Shaw/sub2api/internal/service	(cached)

codex
目标验证全部通过。最后我检查一下最终 diff 和是否有意外文件被 gofmt 改动。
exec
/bin/bash -lc 'git status --short -- backend/internal/pkg/clientidentity/registry.go backend/internal/pkg/clientidentity/registry_test.go backend/internal/service/version_fetcher_service.go backend/internal/service/version_fetcher_service_test.go backend/internal/config/config.go backend/internal/service/wire.go backend/cmd/server/wire.go backend/cmd/server/main.go backend/cmd/server/wire_gen.go deploy/config.example.yaml' in /mnt/x/project/sub2api
 succeeded in 0ms:
 M backend/cmd/server/main.go
 M backend/cmd/server/wire.go
 M backend/cmd/server/wire_gen.go
 M backend/internal/config/config.go
 M backend/internal/service/wire.go
 M deploy/config.example.yaml
?? backend/internal/pkg/clientidentity/registry.go
?? backend/internal/pkg/clientidentity/registry_test.go
?? backend/internal/service/version_fetcher_service.go
?? backend/internal/service/version_fetcher_service_test.go

exec
/bin/bash -lc 'git diff --stat -- backend/internal/pkg/clientidentity/registry.go backend/internal/pkg/clientidentity/registry_test.go backend/internal/service/version_fetcher_service.go backend/internal/service/version_fetcher_service_test.go backend/internal/config/config.go backend/internal/service/wire.go backend/cmd/server/wire.go backend/cmd/server/main.go backend/cmd/server/wire_gen.go deploy/config.example.yaml' in /mnt/x/project/sub2api
 succeeded in 0ms:
 backend/cmd/server/main.go        |  5 +++++
 backend/cmd/server/wire.go        |  5 +++--
 backend/cmd/server/wire_gen.go    | 13 +++++++++----
 backend/internal/config/config.go | 12 ++++++++++++
 backend/internal/service/wire.go  |  3 +++
 deploy/config.example.yaml        |  5 +++++
 6 files changed, 37 insertions(+), 6 deletions(-)

exec
/bin/bash -lc "git diff -- backend/internal/pkg/clientidentity/registry.go backend/internal/service/version_fetcher_service.go backend/internal/config/config.go backend/cmd/server/main.go backend/cmd/server/wire.go backend/cmd/server/wire_gen.go backend/internal/service/wire.go deploy/config.example.yaml | sed -n '1,260p'" in /mnt/x/project/sub2api
 succeeded in 0ms:
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309f..17c926cd 100644
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,11 @@ func runMainServer() {
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+		defer app.VersionFetcherService.Stop()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaa..f4d272d4 100644
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@ import (
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a4..9db4172b 100644
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@ import (
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@ func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@ func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@ func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2ca..d006d34a 100644
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@ type GatewayConfig struct {
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@ type GatewayConfig struct {
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@ func setDefaults() {
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@ func (c *Config) Validate() error {
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f..8995f7df 100644
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@ import (
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@ var ProviderSet = wire.NewSet(
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964..cfa7467e 100644
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@ gateway:
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..17c926cdedcabffa0ddc320973074598458620df
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,11 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+		defer app.VersionFetcherService.Stop()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

codex
已完成 R1 第一步框架，默认行为不变：`gateway.ua_auto_fetch.enabled=false` 时不会启动后台拉取，registry 保持内置兜底；`fetchVersions` 仍返回空，不会联网，也不会改现有请求热路径落点。

1. **IdentityRegistry**
   - 新增 [registry.go](/mnt/x/project/sub2api/backend/internal/pkg/clientidentity/registry.go:14)
   - `Registry` 使用 `atomic.Pointer[Snapshots]` 持有 Claude + Codex 两族完整 snapshot。
   - `NewRegistry()` 初始化为内置兜底：
     - Claude 从 `claude.DefaultHeaders` / `claude.CLICurrentVersion` 读取。
     - Codex 因 `clientidentity` 不能 import `service` 避免循环依赖，先镜像现有 `codexCLIUserAgent/codexCLIVersion` 常量值。
   - `Get()` 原子读取，`Swap()` 原子替换完整 snapshot，nil 会被忽略。

2. **VersionFetcherService**
   - 新增 [version_fetcher_service.go](/mnt/x/project/sub2api/backend/internal/service/version_fetcher_service.go:17)
   - `Start()` 仅在 `cfg.Gateway.UAAutoFetch.Enabled` 为 true 时启动后台 ticker。
   - 默认 interval 为 `1h`，单次 fetch timeout 为 `30s`。
   - `fetchAndUpdate()` 要求 `claudeVersion`、`claudeSDKVersion`、`codexVersion` 三者都非空才组装完整 snapshot 并 `Swap()`。
   - `fetchVersions()` 当前是占位实现，返回空，触发丢弃保留旧值。
   - `Stop()` 用 `sync.Once` 防重复关闭 channel。

3. **配置扩展**
   - 扩展 [config.go](/mnt/x/project/sub2api/backend/internal/config/config.go:729)
   - 新增：
     - `GatewayConfig.UAAutoFetch`
     - `UAAutoFetchConfig{Enabled bool, Interval time.Duration}`
   - 默认值：
     - `gateway.ua_auto_fetch.enabled=false`
     - `gateway.ua_auto_fetch.interval=1h`
   - 增加 interval 非负校验。
   - 示例配置补到 [deploy/config.example.yaml](/mnt/x/project/sub2api/deploy/config.example.yaml:396)。

4. **Wire 注入点**
   - 在 [service/wire.go](/mnt/x/project/sub2api/backend/internal/service/wire.go:600) 加入：
     - `clientidentity.NewRegistry`
     - `NewVersionFetcherService`
   - 在 [cmd/server/wire.go](/mnt/x/project/sub2api/backend/cmd/server/wire.go:26) 的 `Application` 加入 `VersionFetcherService`。
   - 在 [cmd/server/main.go](/mnt/x/project/sub2api/backend/cmd/server/main.go:157) 启动后调用 `Start()`，并 defer `Stop()`。
   - `wire` 命令因现有 `go.sum` 缺 `github.com/google/subcommands` 条目未能运行；为避免引入额外依赖变更，我手工同步了 [wire_gen.go](/mnt/x/project/sub2api/backend/cmd/server/wire_gen.go:210)。

5. **验证结果**
   - `cd backend && go build ./internal/pkg/clientidentity`：通过
   - `cd backend && go build ./internal/service`：通过
   - `cd backend && go test ./internal/pkg/clientidentity -run TestRegistryAtomicSwap -v`：通过
   - `cd backend && go test ./internal/service -run TestVersionFetcherDisabledDoesNotStart -v`：通过
   - 额外验证：`go test ./internal/config -run Test -count=1`：通过

限制说明：项目要求的 GitNexus `impact/detect_changes` 工具本会话没有暴露，无法实际执行；我用本地 `rg` 查了调用面和变更范围，确认本次没有接入 DefaultHeaders / billing block / identity_service / codex headers 等下一步落点。
diff --git a/backend/cmd/server/main.go b/backend/cmd/server/main.go
index 784f309fce8ba1c2045b2fb63c4ede1dc8208379..17c926cdedcabffa0ddc320973074598458620df
--- a/backend/cmd/server/main.go
+++ b/backend/cmd/server/main.go
@@ -154,6 +154,11 @@
 	}
 	defer app.Cleanup()
 
+	if app.VersionFetcherService != nil {
+		app.VersionFetcherService.Start()
+		defer app.VersionFetcherService.Stop()
+	}
+
 	// 启动服务器
 	go func() {
 		if err := app.Server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
diff --git a/backend/cmd/server/wire.go b/backend/cmd/server/wire.go
index da119aaaca333435fa909b4e22e8e3fece5b0d99..f4d272d4995b467faf85cc8200f190f79366a333
--- a/backend/cmd/server/wire.go
+++ b/backend/cmd/server/wire.go
@@ -24,8 +24,9 @@
 )
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func initializeApplication(buildInfo handler.BuildInfo) (*Application, error) {
diff --git a/backend/cmd/server/wire_gen.go b/backend/cmd/server/wire_gen.go
index 18e246a42d6f8d4c40b3722ebbc446754487bc45..9db4172be4978cfdfcdceae6453dce50d607d3a1
--- a/backend/cmd/server/wire_gen.go
+++ b/backend/cmd/server/wire_gen.go
@@ -13,6 +13,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/handler"
 	"github.com/Wei-Shaw/sub2api/internal/handler/admin"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/repository"
 	"github.com/Wei-Shaw/sub2api/internal/server"
 	"github.com/Wei-Shaw/sub2api/internal/server/middleware"
@@ -206,6 +207,8 @@
 	registry := payment.ProvideRegistry()
 	defaultLoadBalancer := payment.ProvideDefaultLoadBalancer(client, encryptionKey)
 	paymentService := service.ProvidePaymentService(client, registry, defaultLoadBalancer, redeemService, subscriptionService, paymentConfigService, userRepository, groupRepository, affiliateService, notificationEmailService)
+	identityRegistry := clientidentity.NewRegistry()
+	versionFetcherService := service.NewVersionFetcherService(identityRegistry, configConfig)
 	settingHandler := handler.ProvideAdminSettingHandler(settingService, emailService, turnstileService, opsService, paymentConfigService, paymentService, userAttributeService, notificationEmailService)
 	opsHandler := admin.NewOpsHandler(opsService)
 	updateCache := repository.NewUpdateCache(redisClient)
@@ -276,8 +279,9 @@
 	userPlatformQuotaUsageFlusher := service.ProvideUserPlatformQuotaUsageFlusher(configConfig, billingCache, serviceUserPlatformQuotaRepository, timingWheelService)
 	v := provideCleanup(client, redisClient, opsMetricsCollector, opsAggregationService, opsAlertEvaluatorService, opsCleanupService, opsScheduledReportService, opsSystemLogSink, schedulerSnapshotService, tokenRefreshService, accountExpiryService, proxyExpiryService, subscriptionExpiryService, usageCleanupService, idempotencyCleanupService, pricingService, emailQueueService, billingCacheService, usageRecordWorkerPool, subscriptionService, oAuthService, openAIOAuthService, geminiOAuthService, antigravityOAuthService, openAIGatewayService, scheduledTestRunnerService, backupService, paymentOrderExpiryService, channelMonitorRunner, userPlatformQuotaUsageFlusher)
 	application := &Application{
-		Server:  httpServer,
-		Cleanup: v,
+		Server:                httpServer,
+		VersionFetcherService: versionFetcherService,
+		Cleanup:               v,
 	}
 	return application, nil
 }
@@ -285,8 +289,9 @@
 // wire.go:
 
 type Application struct {
-	Server  *http.Server
-	Cleanup func()
+	Server                *http.Server
+	VersionFetcherService *service.VersionFetcherService
+	Cleanup               func()
 }
 
 func providePrivacyClientFactory() service.PrivacyClientFactory {
diff --git a/backend/internal/config/config.go b/backend/internal/config/config.go
index 2422b2cab0c14c3715abf3dfbd593b3ab32c8d26..d006d34aad8f450aaffec4b652066d20ec60c4c2
--- a/backend/internal/config/config.go
+++ b/backend/internal/config/config.go
@@ -725,6 +725,8 @@
 	OpenAIHTTP2 GatewayOpenAIHTTP2Config `mapstructure:"openai_http2"`
 	// ImageConcurrency: 图片生成独立并发限制配置（默认关闭）
 	ImageConcurrency ImageConcurrencyConfig `mapstructure:"image_concurrency"`
+	// UAAutoFetch: Claude/Codex 客户端身份版本后台拉取配置（默认关闭）
+	UAAutoFetch UAAutoFetchConfig `mapstructure:"ua_auto_fetch"`
 
 	// HTTP 上游连接池配置（性能优化：支持高并发场景调优）
 	// MaxIdleConns: 所有主机的最大空闲连接总数
@@ -801,6 +803,11 @@
 	UserMessageQueue UserMessageQueueConfig `mapstructure:"user_message_queue"`
 }
 
+type UAAutoFetchConfig struct {
+	Enabled  bool          `mapstructure:"enabled"`
+	Interval time.Duration `mapstructure:"interval"`
+}
+
 // GatewayOpenAIHTTP2Config OpenAI HTTP 上游协议配置。
 // 默认启用 HTTP/2；在部分代理不兼容时按策略回退 HTTP/1.1。
 type GatewayOpenAIHTTP2Config struct {
@@ -1881,6 +1888,8 @@
 	viper.SetDefault("gateway.image_concurrency.overflow_mode", ImageConcurrencyOverflowModeReject)
 	viper.SetDefault("gateway.image_concurrency.wait_timeout_seconds", 30)
 	viper.SetDefault("gateway.image_concurrency.max_waiting_requests", 100)
+	viper.SetDefault("gateway.ua_auto_fetch.enabled", false)
+	viper.SetDefault("gateway.ua_auto_fetch.interval", time.Hour)
 	viper.SetDefault("gateway.antigravity_fallback_cooldown_minutes", 1)
 	viper.SetDefault("gateway.antigravity_extra_retries", 10)
 	viper.SetDefault("gateway.max_body_size", int64(256*1024*1024))
@@ -2472,6 +2481,9 @@
 	if c.Gateway.ImageConcurrency.MaxWaitingRequests < 0 {
 		return fmt.Errorf("gateway.image_concurrency.max_waiting_requests must be non-negative")
 	}
+	if c.Gateway.UAAutoFetch.Interval < 0 {
+		return fmt.Errorf("gateway.ua_auto_fetch.interval must be non-negative")
+	}
 	if c.Gateway.MaxIdleConns <= 0 {
 		return fmt.Errorf("gateway.max_idle_conns must be positive")
 	}
diff --git a/backend/internal/pkg/clientidentity/registry.go b/backend/internal/pkg/clientidentity/registry.go
new file mode 100644
index 0000000000000000000000000000000000000000..71e70f58a0b917c3f9e7a9c28a4e21a976377821
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry.go
@@ -0,0 +1,85 @@
+package clientidentity
+
+import (
+	"sync/atomic"
+
+	"github.com/Wei-Shaw/sub2api/internal/pkg/claude"
+)
+
+const (
+	defaultCodexCLIVersion = "0.125.0"
+	defaultCodexCLIUA      = "codex_cli_rs/0.125.0 (Ubuntu 22.4.0; x86_64) xterm-256color"
+)
+
+// Registry holds the active complete Claude and Codex identity snapshots.
+type Registry struct {
+	snapshotPtr atomic.Pointer[Snapshots]
+}
+
+// Snapshots is the complete baseline for both client families.
+type Snapshots struct {
+	Claude ClaudeSnapshot
+	Codex  CodexSnapshot
+}
+
+// ClaudeSnapshot keeps all linked Claude identity fields together.
+type ClaudeSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+// CodexSnapshot keeps all linked Codex identity fields together.
+type CodexSnapshot struct {
+	Headers        map[string]string
+	VersionFields  VersionFields
+	TLSProfileName string
+}
+
+func NewRegistry() *Registry {
+	r := &Registry{}
+	r.snapshotPtr.Store(&Snapshots{
+		Claude: r.defaultClaudeSnapshot(),
+		Codex:  r.defaultCodexSnapshot(),
+	})
+	return r
+}
+
+// Get returns the active immutable snapshot pointer atomically.
+func (r *Registry) Get() *Snapshots {
+	return r.snapshotPtr.Load()
+}
+
+// Swap atomically replaces both Claude and Codex snapshots as one unit.
+func (r *Registry) Swap(snapshots *Snapshots) {
+	if snapshots == nil {
+		return
+	}
+	r.snapshotPtr.Store(snapshots)
+}
+
+func (r *Registry) defaultClaudeSnapshot() ClaudeSnapshot {
+	headers := cloneHeaders(claude.DefaultHeaders)
+	return ClaudeSnapshot{
+		Headers: headers,
+		VersionFields: VersionFields{
+			CLIVersion: claude.CLICurrentVersion,
+			SDKVersion: headers["X-Stainless-Package-Version"],
+			CCVersion:  claude.CLICurrentVersion,
+		},
+		TLSProfileName: TLSProfileClaudeCLIDefault,
+	}
+}
+
+func (r *Registry) defaultCodexSnapshot() CodexSnapshot {
+	return CodexSnapshot{
+		Headers: map[string]string{
+			"User-Agent": defaultCodexCLIUA,
+			"originator": "codex_cli_rs",
+		},
+		VersionFields: VersionFields{
+			CLIVersion: defaultCodexCLIVersion,
+		},
+		TLSProfileName: TLSProfileCodexCLIDefault,
+	}
+}
diff --git a/backend/internal/pkg/clientidentity/registry_test.go b/backend/internal/pkg/clientidentity/registry_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..520eed067b3e33342c64f2b53041d7f19d000fb7
--- /dev/null
+++ b/backend/internal/pkg/clientidentity/registry_test.go
@@ -0,0 +1,20 @@
+package clientidentity
+
+import (
+	"testing"
+
+	"github.com/stretchr/testify/assert"
+)
+
+func TestRegistryAtomicSwap(t *testing.T) {
+	registry := NewRegistry()
+	initial := registry.Get()
+
+	newSnapshots := &Snapshots{
+		Claude: ClaudeSnapshot{VersionFields: VersionFields{CLIVersion: "1.0.0"}},
+	}
+	registry.Swap(newSnapshots)
+
+	assert.Equal(t, "1.0.0", registry.Get().Claude.VersionFields.CLIVersion)
+	assert.NotEqual(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/version_fetcher_service.go b/backend/internal/service/version_fetcher_service.go
new file mode 100644
index 0000000000000000000000000000000000000000..15a3bba4c8a03dce637c5451883d2deab1312953
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service.go
@@ -0,0 +1,118 @@
+package service
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+)
+
+const (
+	defaultVersionFetchInterval = time.Hour
+	versionFetchTimeout         = 30 * time.Second
+)
+
+type VersionFetcherService struct {
+	registry *clientidentity.Registry
+	cfg      *config.Config
+	stopCh   chan struct{}
+	stopOnce sync.Once
+}
+
+func NewVersionFetcherService(registry *clientidentity.Registry, cfg *config.Config) *VersionFetcherService {
+	return &VersionFetcherService{
+		registry: registry,
+		cfg:      cfg,
+		stopCh:   make(chan struct{}),
+	}
+}
+
+// Start runs the periodic version fetch loop only when ua_auto_fetch is enabled.
+func (s *VersionFetcherService) Start() {
+	if s == nil || s.cfg == nil || !s.cfg.Gateway.UAAutoFetch.Enabled {
+		return
+	}
+
+	interval := s.cfg.Gateway.UAAutoFetch.Interval
+	if interval == 0 {
+		interval = defaultVersionFetchInterval
+	}
+
+	go func() {
+		ticker := time.NewTicker(interval)
+		defer ticker.Stop()
+
+		for {
+			select {
+			case <-ticker.C:
+				s.fetchAndUpdate()
+			case <-s.stopCh:
+				return
+			}
+		}
+	}()
+}
+
+func (s *VersionFetcherService) Stop() {
+	if s == nil {
+		return
+	}
+	s.stopOnce.Do(func() {
+		close(s.stopCh)
+	})
+}
+
+func (s *VersionFetcherService) fetchAndUpdate() {
+	if s == nil || s.registry == nil {
+		return
+	}
+
+	ctx, cancel := context.WithTimeout(context.Background(), versionFetchTimeout)
+	defer cancel()
+
+	claudeVersion, claudeSDKVersion, codexVersion := s.fetchVersions(ctx)
+	if claudeVersion == "" || claudeSDKVersion == "" || codexVersion == "" {
+		return
+	}
+
+	s.registry.Swap(&clientidentity.Snapshots{
+		Claude: clientidentity.ClaudeSnapshot{
+			Headers: s.buildClaudeHeaders(claudeVersion, claudeSDKVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: claudeVersion,
+				SDKVersion: claudeSDKVersion,
+				CCVersion:  claudeVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileClaudeCLIDefault,
+		},
+		Codex: clientidentity.CodexSnapshot{
+			Headers: s.buildCodexHeaders(codexVersion),
+			VersionFields: clientidentity.VersionFields{
+				CLIVersion: codexVersion,
+			},
+			TLSProfileName: clientidentity.TLSProfileCodexCLIDefault,
+		},
+	})
+}
+
+func (s *VersionFetcherService) fetchVersions(ctx context.Context) (claudeVersion, claudeSDKVersion, codexVersion string) {
+	_ = ctx
+	return "", "", ""
+}
+
+func (s *VersionFetcherService) buildClaudeHeaders(cliVer, sdkVer string) map[string]string {
+	return map[string]string{
+		"User-Agent":                  "claude-cli/" + cliVer + " (external, cli)",
+		"X-Stainless-Package-Version": sdkVer,
+		"X-App":                       "cli",
+	}
+}
+
+func (s *VersionFetcherService) buildCodexHeaders(cliVer string) map[string]string {
+	return map[string]string{
+		"User-Agent": "codex_cli_rs/" + cliVer,
+		"originator": "codex_cli_rs",
+	}
+}
diff --git a/backend/internal/service/version_fetcher_service_test.go b/backend/internal/service/version_fetcher_service_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..d41572eeba8d644c72138d2bf983501d949e35cf
--- /dev/null
+++ b/backend/internal/service/version_fetcher_service_test.go
@@ -0,0 +1,26 @@
+package service
+
+import (
+	"testing"
+	"time"
+
+	"github.com/Wei-Shaw/sub2api/internal/config"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
+	"github.com/stretchr/testify/assert"
+)
+
+func TestVersionFetcherDisabledDoesNotStart(t *testing.T) {
+	cfg := &config.Config{
+		Gateway: config.GatewayConfig{
+			UAAutoFetch: config.UAAutoFetchConfig{Enabled: false},
+		},
+	}
+	registry := clientidentity.NewRegistry()
+	initial := registry.Get()
+	svc := NewVersionFetcherService(registry, cfg)
+
+	svc.Start()
+	time.Sleep(50 * time.Millisecond)
+
+	assert.Same(t, initial, registry.Get())
+}
diff --git a/backend/internal/service/wire.go b/backend/internal/service/wire.go
index bc0cf46f35818ca135cea2bac2d53f44e12e85f3..8995f7dffa7451651ca44164337a1972db488db5
--- a/backend/internal/service/wire.go
+++ b/backend/internal/service/wire.go
@@ -9,6 +9,7 @@
 	"github.com/Wei-Shaw/sub2api/internal/config"
 	"github.com/Wei-Shaw/sub2api/internal/payment"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/antigravity"
+	"github.com/Wei-Shaw/sub2api/internal/pkg/clientidentity"
 	"github.com/Wei-Shaw/sub2api/internal/pkg/logger"
 	"github.com/google/wire"
 	"github.com/redis/go-redis/v9"
@@ -599,6 +600,8 @@
 	NewModelPricingResolver,
 	NewContentModerationService,
 	NewAffiliateService,
+	clientidentity.NewRegistry,
+	NewVersionFetcherService,
 	ProvidePaymentConfigService,
 	ProvidePaymentService,
 	ProvidePaymentOrderExpiryService,
diff --git a/deploy/config.example.yaml b/deploy/config.example.yaml
index 35c76964230ae40c23b20403efe10fa9ab35899f..cfa7467ef73899259e33d57955825c75c23960d0
--- a/deploy/config.example.yaml
+++ b/deploy/config.example.yaml
@@ -391,6 +391,11 @@
     # Max image requests waiting in this process when overflow_mode=wait, 0=unlimited
     # wait 模式当前进程允许排队等待的图片请求数，0=不限制
     max_waiting_requests: 100
+  # Claude/Codex UA auto fetcher. Default false keeps built-in hardcoded identity.
+  # Claude/Codex UA 自动拉取。默认 false，保持内置硬编码身份。
+  ua_auto_fetch:
+    enabled: false
+    interval: 1h
   # SSE max line size in bytes (default: 40MB)
   # SSE 单行最大字节数（默认 40MB）
   max_line_size: 41943040

hook: Stop
hook: Stop Completed
tokens used
260,077
已完成 R1 第一步框架，默认行为不变：`gateway.ua_auto_fetch.enabled=false` 时不会启动后台拉取，registry 保持内置兜底；`fetchVersions` 仍返回空，不会联网，也不会改现有请求热路径落点。

1. **IdentityRegistry**
   - 新增 [registry.go](/mnt/x/project/sub2api/backend/internal/pkg/clientidentity/registry.go:14)
   - `Registry` 使用 `atomic.Pointer[Snapshots]` 持有 Claude + Codex 两族完整 snapshot。
   - `NewRegistry()` 初始化为内置兜底：
     - Claude 从 `claude.DefaultHeaders` / `claude.CLICurrentVersion` 读取。
     - Codex 因 `clientidentity` 不能 import `service` 避免循环依赖，先镜像现有 `codexCLIUserAgent/codexCLIVersion` 常量值。
   - `Get()` 原子读取，`Swap()` 原子替换完整 snapshot，nil 会被忽略。

2. **VersionFetcherService**
   - 新增 [version_fetcher_service.go](/mnt/x/project/sub2api/backend/internal/service/version_fetcher_service.go:17)
   - `Start()` 仅在 `cfg.Gateway.UAAutoFetch.Enabled` 为 true 时启动后台 ticker。
   - 默认 interval 为 `1h`，单次 fetch timeout 为 `30s`。
   - `fetchAndUpdate()` 要求 `claudeVersion`、`claudeSDKVersion`、`codexVersion` 三者都非空才组装完整 snapshot 并 `Swap()`。
   - `fetchVersions()` 当前是占位实现，返回空，触发丢弃保留旧值。
   - `Stop()` 用 `sync.Once` 防重复关闭 channel。

3. **配置扩展**
   - 扩展 [config.go](/mnt/x/project/sub2api/backend/internal/config/config.go:729)
   - 新增：
     - `GatewayConfig.UAAutoFetch`
     - `UAAutoFetchConfig{Enabled bool, Interval time.Duration}`
   - 默认值：
     - `gateway.ua_auto_fetch.enabled=false`
     - `gateway.ua_auto_fetch.interval=1h`
   - 增加 interval 非负校验。
   - 示例配置补到 [deploy/config.example.yaml](/mnt/x/project/sub2api/deploy/config.example.yaml:396)。

4. **Wire 注入点**
   - 在 [service/wire.go](/mnt/x/project/sub2api/backend/internal/service/wire.go:600) 加入：
     - `clientidentity.NewRegistry`
     - `NewVersionFetcherService`
   - 在 [cmd/server/wire.go](/mnt/x/project/sub2api/backend/cmd/server/wire.go:26) 的 `Application` 加入 `VersionFetcherService`。
   - 在 [cmd/server/main.go](/mnt/x/project/sub2api/backend/cmd/server/main.go:157) 启动后调用 `Start()`，并 defer `Stop()`。
   - `wire` 命令因现有 `go.sum` 缺 `github.com/google/subcommands` 条目未能运行；为避免引入额外依赖变更，我手工同步了 [wire_gen.go](/mnt/x/project/sub2api/backend/cmd/server/wire_gen.go:210)。

5. **验证结果**
   - `cd backend && go build ./internal/pkg/clientidentity`：通过
   - `cd backend && go build ./internal/service`：通过
   - `cd backend && go test ./internal/pkg/clientidentity -run TestRegistryAtomicSwap -v`：通过
   - `cd backend && go test ./internal/service -run TestVersionFetcherDisabledDoesNotStart -v`：通过
   - 额外验证：`go test ./internal/config -run Test -count=1`：通过

限制说明：项目要求的 GitNexus `impact/detect_changes` 工具本会话没有暴露，无法实际执行；我用本地 `rg` 查了调用面和变更范围，确认本次没有接入 DefaultHeaders / billing block / identity_service / codex headers 等下一步落点。
