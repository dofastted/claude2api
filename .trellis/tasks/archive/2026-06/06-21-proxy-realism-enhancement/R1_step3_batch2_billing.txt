Active task: .trellis/tasks/06-21-proxy-realism-enhancement

你是 Go 后端工程师，为 sub2api 实施 **R1 step3 批次2：billing block cc_version 改造**。

## 上下文
- 仓库根 `/mnt/x/project/sub2api`，后端 `backend/`，分支 `feat/proxy-realism`。
- R1 step3 批次1（codex headers）已完成。
- 目标：把 billing block 的 `cc_version` 来源从 `claude.CLICurrentVersion` 常量改为 `registry.Get().Claude.VersionFields.CLIVersion`。

## 改造范围（来自前置分析）

### 当前逻辑
- `gateway_billing_block.go:88`：`computeClaudeCodeFingerprint(body, cliVersion)`
- `gateway_billing_block.go:90`：写入 billing header，格式 `cc_version=<cli_version>.<fp>`
- `fp` 由 `computeClaudeCodeFingerprint` 动态计算（SHA256 + 盐 + 首条 user text）
- `cliVersion` 来源：
  - `gateway_service.go:4322`：`buildBillingAttributionText(body, claude.CLICurrentVersion)`
  - `gateway_service.go:4326`：`computeClaudeCodeFingerprint(body, claude.CLICurrentVersion)`
  - `gateway_service.go:4329`：`{cc_version}` 模板替换也直接用 `claude.CLICurrentVersion`

### 关键改造点
1. `buildBillingAttributionText` 函数改为接收 `cliVersion` 参数（而非内部读 `claude.CLICurrentVersion`）
2. `expandClaudeOAuthSystemPromptTextTemplate` 函数改为接收 `cliVersion` 参数
3. 调用链：`gateway_service.go:4322/4326/4329` 三处改为传 `registry.Get().Claude.VersionFields.CLIVersion`

## 改造策略

### 步骤1：修改 gateway_billing_block.go
在 `backend/internal/service/gateway_billing_block.go`：

```go
// buildBillingAttributionText 已经接收 cliVersion 参数，无需改签名
// 但确保调用点传的是 registry 来源而非常量

// computeClaudeCodeFingerprint 已经接收 cliVersion 参数，无需改
```

### 步骤2：修改 gateway_service.go
在 `backend/internal/service/gateway_service.go`：

```go
// GatewayService 注入 registry（如果还没有）
type GatewayService struct {
	// ... 现有字段
	identityRegistry *clientidentity.Registry // 新增
}

// NewGatewayService 构造函数增加 registry 参数

// 行4322/4326/4329 改为从 registry 读取
func (s *GatewayService) someMethod(...) {
	claudeSnapshot := s.identityRegistry.Get().Claude
	cliVersion := claudeSnapshot.VersionFields.CLIVersion

	// 行4322
	billingText := buildBillingAttributionText(body, cliVersion)

	// 行4326
	fp := computeClaudeCodeFingerprint(body, cliVersion)

	// 行4329: expandClaudeOAuthSystemPromptTextTemplate 需传 cliVersion
	// 当前如果模板内部用 claude.CLICurrentVersion，需改为参数传入
}

// expandClaudeOAuthSystemPromptTextTemplate 改造（如果需要）
func expandClaudeOAuthSystemPromptTextTemplate(..., cliVersion string) string {
	// 替换 {cc_version} 占位符时用 cliVersion 参数
	// 而非内部读 claude.CLICurrentVersion
}
```

### 步骤3：同步 count_tokens 路径
根据前置分析，`gateway_service.go:10204-10207` count_tokens 路径也有同类同步：

```go
// 行10204-10207 也改为从 registry 读取
func (s *GatewayService) countTokensMethod(...) {
	claudeSnapshot := s.identityRegistry.Get().Claude
	cliVersion := claudeSnapshot.VersionFields.CLIVersion
	// 同步已有 billing header 的 semver
}
```

### 步骤4：Wire 注入同步
在 `backend/cmd/server/wire_gen.go` / `backend/internal/service/wire.go`：

- 确保 `GatewayService` 构造函数接收 `identityRegistry *clientidentity.Registry`
- wire 装配时传入 registry 单例

## 验证
```bash
cd backend
go build ./internal/service
go test ./internal/service -run TestGatewayService -v
go test ./internal/service -run TestBillingBlock -v
```

## 输出
完成后报告：
1. 改造的文件（gateway_service.go / gateway_billing_block.go）
2. 改造的函数（buildBillingAttributionText / expandClaudeOAuthSystemPromptTextTemplate / computeClaudeCodeFingerprint 调用点）
3. Wire 注入同步情况
4. 验证结果

## 注意
- **保留 fp 动态计算逻辑**：不引入固定 `claude.CCFP` 常量，继续用现有 `computeClaudeCodeFingerprint` 算法。
- **只改版本来源**：`cliVersion` 从 `claude.CLICurrentVersion` 常量改为 `registry.Get().Claude.VersionFields.CLIVersion`。
- **模板替换**：如果 `expandClaudeOAuthSystemPromptTextTemplate` 内部用了 `{cc_version}` 占位符，确保替换时用传入的 `cliVersion` 参数。

开始执行。
